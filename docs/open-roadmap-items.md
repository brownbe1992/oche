# Open Roadmap Items — Central Tracker

> This is the **single centralized place to track every roadmap request across the
> project** — replacing `docs/archive/existing-app-prep-roadmap.md`'s old "Roadmap
> sequencing" table, which is now frozen/historical. See `CLAUDE.md`'s "Roadmap
> docs" section for the standing convention that keeps this file current.
>
> **No item on this tracker is ever "Partially Completed."** A roadmap doc that
> ships part of its design and defers the rest gets split into separate,
> independently-tracked items here — one line per shippable phase (a "v1"/"v2",
> a numbered build-order step, or any other genuinely separable piece of work) —
> each cleanly **Done** or **Not started**. When a roadmap doc's own status
> header would otherwise say "Partial," that's the signal to split it here
> instead of writing "Partial" as this tracker's status.
>
> A roadmap doc moves to `docs/archive/` only once **every** item split out from
> it below is Done — matching the existing archiving convention. A doc can have
> some items Done and others still open here without being archived; it archives
> once nothing about it remains on the open list.

---

## Open items (not yet started, ordered by complexity — lowest first)

| # | Item | Source doc | Complexity |
|---|---|---|---|
| 1 | Mobile: multiple saved server profiles (stretch goal, step 7) | `docs/mobile-app-roadmap.md` | Low |
| 2 | Mobile: "Scoreboard Mode" toggle (step 5) | `docs/mobile-app-roadmap.md` | Low |
| 3 | Mobile: native chrome — change-server access, haptics, biometric unlock (step 4) | `docs/mobile-app-roadmap.md` | Low-Medium |
| 4 | Mobile: distribution decision — App Store/Play Store listing vs. simpler sideload distribution (step 6) | `docs/mobile-app-roadmap.md` | Low-Medium |
| 5 | Localize voice announcements beyond hardcoded English phrases | `docs/voice-announcements-i18n-roadmap.md` | Low-Medium |
| 6 | UI Overhaul design phase: create comprehensive plan for player page reorganization (step 1) | `docs/ui-overhaul-roadmap.md` | Medium |
| 34 | Game-modes: decide whether to generalize the per-game-type backend stat functions (a single parameterized stat-bubble/leaderboard query keyed by each type's formula definitions, plus the Home page's per-type `Promise.all` fetch list) rather than hand-writing a bespoke SQL function set per type — the explicitly-open design thread in `game-modes-roadmap.md`. Every game type shipped so far (X01/Cricket/Baseball/drills/Killer and all the later modes) has its stats fully viewable via per-type functions, so this is an optional architecture refactor to reduce the per-type cost, NOT a missing feature — genuinely undecided (the per-type shape has been kept ~10 times running, which may itself be the right answer). This is why `game-modes-roadmap.md` stays in `docs/` | `docs/game-modes-roadmap.md` | Medium |
| 36 | One `winSectionHtml()` helper for the five identical "Most X Wins" Home templates | `docs/code-quality-roadmap.md` | Low |
| 38 | `savedGamePositionLabel()`: dispatch on `sg.gameType` instead of field presence (removes the reserved-field-name contract) | `docs/code-quality-roadmap.md` | Low-Medium |
| 39 | Derive `NON_SAVABLE_GAME_TYPES` from the backend registry (or pin the two lists together with a committed test) | `docs/code-quality-roadmap.md` | Low-Medium |
| 40 | Declare `newMatchPlayer`'s second-arg shape on the `GAME_TYPES` registry (startGame/resumeGame's hand lists already differ) | `docs/code-quality-roadmap.md` | Low-Medium |
| 41 | `games.category` as a registry member (kills the 14-branch ternary whose `String(startScore)` fallthrough writes bad rows permanently) | `docs/code-quality-roadmap.md` | Low-Medium |
| 35 | Consolidate the 16 copy-pasted `undoLastTurn*` trailers into `_finishUndo()` (copies have already drifted) | `docs/code-quality-roadmap.md` | Medium |
| 37 | Registry-driven resume dispatch — replace `resumeGame()`'s 13-branch chain and `_savedGamePosition()`'s parallel copy (a missed future branch destroys the pause) | `docs/code-quality-roadmap.md` | Medium-High |
| 42 | Live-state keys: per-mode container key (or registry-derived allowlist) — the silent-strip failure class has shipped twice | `docs/code-quality-roadmap.md` | Medium |
| 43 | Id-keyed killer configs — one migration replaces the three name-rewrite compensators (rename/merge, import re-key, boot reconcile) | `docs/code-quality-roadmap.md` | Medium |
| 44 | Whole-darts-table scan pass over hot stat queries (`_first9`/`_trebleLess`, recap per-player scans, personal-bests double scan, shanghai/halve-it O(players×history) legs loop) — overlaps item 34, decide together | `docs/code-quality-roadmap.md` | Medium |
| 45 | Home page: lazy per-combo fetches instead of the ~47-request burst per navigation (SWR paint + keep-cache catch already shipped; the burst remains) | `docs/code-quality-roadmap.md` | Medium |
| 52 | Small shared-pattern helper batch for index.html: `jsArg()` (~24 escapeHtml(escapeJs()) sites), `openModal()` (12 builders), milestone-ladder registration helper (~12 loops), countdown-timer factory (Blitz + No Warmup), aria-pressed group helper (~10 controls) | `docs/code-quality-roadmap.md` | Low-Medium |
| 46 | Per-mode option-section wiring as a registry member (setMode/setGameType/markup sprawl that produced the Step-3 drift; fold in the contexts-vs-soloOnly/h2hOnly mirror and setMode's three mode lists) | `docs/code-quality-roadmap.md` | Medium |
| 47 | One `h2hStatsHtml(winner, scope)` with per-type rows from the registry (5 clones + finishUnit's twin 5-way ternary chains) | `docs/code-quality-roadmap.md` | Medium |
| 48 | Declarative personal-bests renderers (17 near-identical functions → per-type spec + one generic renderer, ~280 lines → ~50) | `docs/code-quality-roadmap.md` | Medium |
| 49 | One leaderboard-row template helper for the ~20 inline hof-list boards — supersedes item 36's five "Most X Wins" copies, implement together | `docs/code-quality-roadmap.md` | Medium |
| 50 | One-shot badge award helper (`awardOnceBadge()`) — ~10 hand-rolled copies with already-drifted cache/queue behavior | `docs/code-quality-roadmap.md` | Medium |
| 51 | Badge-progress fetch-baseline-once (ATW per-visit + Doubles per-dart scans), profile-navigation stats SWR + tab-scoped refetch, tournament-seeding batch read | `docs/code-quality-roadmap.md` | Medium |
| 53 | Game-start construction factory — 4 hand-written copies of the runtime-state literal + badge prefetch + boot tail (tournament/marathon copies already drift) | `docs/code-quality-roadmap.md` | Medium |
| 54 | One `advanceLegSetGame()` helper — the leg/set/game progression cascade is pasted in 8 `onLegWon*` handlers (~180 lines; the Shanghai copy is 100% generic) | `docs/code-quality-roadmap.md` | Medium |
| 55 | Scoreboard/pad renderer scaffolding — shared cs-table builders (5 chalkboard renderers), one parameterized single-target pad (5 clones), `renderPad()` lookup dispatch | `docs/code-quality-roadmap.md` | Medium |
| 56 | Dart input/record helpers on the hottest path — `pushThrownDarts()` (4 copies of dart construction + zone stamping) and `recordSingleDartTurn()` (5 payload clones) | `docs/code-quality-roadmap.md` | Low-Medium |
| 57 | Frontend efficiency batch — moment-card/webhook config gating (no-op canvas+POST per celebration), pad build-once (22 buttons rebuilt per dart), Chuckin heatmap serialization caching | `docs/code-quality-roadmap.md` | Medium |
| 58 | Declarative settings field table — load/save hand-maintain parallel per-field lists (13 HA webhook + 7 voice + 8 misc) | `docs/code-quality-roadmap.md` | Low-Medium |
| 59 | Conventions: move the 10 inline badge predicates to scoring.js (+tests, per the file's own stated convention) and settle the `DB.*` wrapper boundary rule | `docs/code-quality-roadmap.md` | Low-Medium |
| 7 | Mobile: Capacitor scaffold (iOS + Android) with the native Server Setup screen (step 2) | `docs/mobile-app-roadmap.md` | Medium |
| 8 | Mobile: ATS/cleartext config + self-signed cert trust-prompt (step 3) | `docs/mobile-app-roadmap.md` | Medium |
| 9 | Environmental logging (new inbound HA auth model; explicitly scoped as a niche, manually-enabled feature) | `docs/environmental-logging-roadmap.md` | Medium |
| 14 | Online multiplayer (needs someone else running their own Oche instance too — a real adoption chicken-and-egg problem) | `docs/online-multiplayer-roadmap.md` | Very high |
| 15 | Camera/ML scoring (genuinely novel CV engineering; only useful to whoever mounts the hardware) | `docs/camera-scoring-roadmap.md` | Extremely high |
| 16 | Companion website (persistent hosted infrastructure — accounts, matchmaking, a cloud database — the one item requiring the project to operate long-term infra, not just be self-hostable) | `docs/companion-website-roadmap.md` | Extremely high |

### Build-order notes that still apply

- **Tournament mode (single-elimination), core league mode, league
  fixtures / pending matches, AND the New Game page revamp are all done**, and
  the first two are the real, shipped precedent for the "games link into a
  context table" pattern (see `CLAUDE.md`) any future context (online
  multiplayer, or anything not yet designed) can now follow directly — a
  separate `context_matches.game_id` junction table for a context with its
  own match-level lifecycle state (tournament's shape, and now league
  fixtures' too), or a direct nullable `games.<context>_id` column for a
  context with none (league's original shape). League fixtures added the
  `tournament_matches`-style junction-table shape *on top of* league mode's
  existing direct-column shape, for the "pending match" lifecycle state a
  plain nullable column can't represent — both shapes now coexist within one
  feature, which is fine: they answer different questions (which league did
  this game count toward, vs. is this specific pairing's match still owed).
- **Mobile app's steps are sequential as listed** (step 2 → 3 → 4 → 5 → 6 → 7) per `docs/mobile-app-roadmap.md`'s own suggested build order; its one prerequisite (the responsive CSS pass) is already done.
- **Row 5** (voice announcement i18n) is the one remaining order-independent Low-Medium item — it can be interleaved anywhere, including ahead of the bigger lifts.

---

## Done (completion ledger — kept for history, not re-verified here)

Every item below is fully shipped. This section exists so nothing's completion
history is lost when a doc still has open items elsewhere on this tracker (and
therefore hasn't been archived yet) — see each source doc for full detail.

| Item | Source doc |
|---|---|
| Halve-It custom target editor (item 19) — the v2 split-out of the core Halve-It game, letting a player replace the classic 7-round default (`config.targets`) with their own sector+ring sequence via a "customize targets" screen reusing Cricket's custom-numbers picker pattern. New Game setup gets a Classic/Custom toggle (`#halve-it-options-section`); Classic omits `config.targets` (server falls back to `HALVE_IT_DEFAULT_TARGETS`), Custom reveals a per-round editor building an ordered 1–20-round sequence of `{sector, ring?}` rows — a native `<select>` for the sector (Bull 25, or 20…1) and one for the required ring (Any / Single / Double / Treble, Treble hidden for Bull, and a Bull+Treble pick snapped back to Double client-side); a custom set makes the game's category label read "Custom Halve-It". `createGame()` (`backend/db.js`) validates and normalises `config.targets` server-side — array of 1–20 entries, sector 1–20 or 25, ring single/double/treble, treble-Bull rejected as unwinnable, extra fields stripped; frontend wiring (`setHalveItPreset`/`addHalveItTargetRow`/`updateHalveItTarget`/`renderHalveItTargetRows`/`resolveHalveItTargets`) in `frontend/index.html`; committed validation tests in `backend/test/db.halve-it-stats.test.js`; headless-verified the editor renders, seeds from classic, adds/edits/removes rows, snaps treble-Bull to double, and resolves classic→omit vs custom→list. Completes the Halve-It roadmap, now moved to `docs/archive/` | `docs/archive/halve-it-roadmap.md` |
| Pressure Chamber self-declare honesty mechanic (item 32) — the v2 split-out of the core Pressure Chamber game (item 28): a before-the-throw self-declaration and an informational Honesty% stat. A new nullable `turns.declared_hit` column (`1`=declared hit, `0`=declared miss, `NULL` otherwise, purely additive like `target_score`/`declared_unsolvable`/`affected_player_id`); a declare screen (`renderPadPressureChamber()` shows two declare buttons and hides the number pad / S·D·T multi-row until a call is made, No Warmup's 5-second clock only arming once the call is committed) driven by `declarePressureHit()` + transient per-visit `game.pressureDeclared` state (reset each turn, never saved/resumed, restored by undo); `enterTurnPressureChamber()` sends `declaredHit` to `DB.recordTurn()`; `getPressureChamberStatBubbles`' new `honestyPct`/`declaredRounds` compare each declaration against the round's real outcome (declared hit honest iff `checkout=1`, declared miss iff `bust=1`), surfaced as a new "Honesty %" Player-Profile bubble. Explicitly NOT a scoring input and carries no leaderboard weight; the one part of the design that can never be made tamper-resistant by a single atomic write (a determined client can submit a declaration matching the outcome in hindsight), so it ships as a best-effort honor-system signal with **no consistency guard** — `addTurn()` validates only the `0`/`1` shape and rejects it outside `pressure_chamber` games, the same gating `declared_unsolvable` uses for Checkout Trainer. Resolves the roadmap doc's own "does Honesty% feed the Composure Rating" open question in favour of **informational only**. Committed tests in `backend/test/db.pressure-chamber-stats.test.js` (honest→100%, mixed→50%, undeclared→null, plus the game-type/shape guards); headless-verified the full declare→throw→next-round flow end-to-end (declare buttons show, pad appears after the call, declaration resets each round, Honesty% computes to 50% on a mixed pair). `REFERENCE.md` §34 updated. Completes the Pressure Chamber roadmap, now moved to `docs/archive/` | `docs/archive/pressure-chamber-roadmap.md` |
| Tournament mode: double-elimination bracket support (item 13) — losers bracket + grand final/reset logic layered on the shipped single-elimination, on the same schema (the `winner_next_*`/`loser_next_*` pointer pair). Restricted to exact powers of two (4/8/16/32/64/128, `TOURNAMENT_DOUBLE_ELIM_COUNTS`) per the roadmap's §2 de-risking, so there are zero byes and no cascading-bye complexity. A shared `doubleElimStructure(k)` (`frontend/scoring.js`, used by both backend generation and the frontend format table so counts/labels can't drift) lays out `k` winners rounds, `2k−2` alternating minor/drop losers rounds, and the grand final + conditional reset decider (`3k` rounds, `2N−1` matches). `_generateDoubleElimBracket()` (`backend/db.js`) wires the pointer pairs; `_advanceTournamentMatch()` drops a winners-bracket loser via `loser_next_match_id` (rather than eliminating) and `_resolveGrandFinal()` handles the bracket reset — if the losers-bracket champion (grand-final slot 2) wins game one, both finalists hold one loss and the pre-created reset match is played; if the winners-bracket champion wins game one, the tournament ends. Giant Slayer is now awarded per real match (so a winners-bracket upset counts even though that loser only drops), except never on a game-one grand-final loss that triggers a reset. Frontend: a Single/Double toggle on the setup screen (double-elim blocked unless the count is a valid power of two; per-round format table derives from `doubleElimStructure`), plus a functional grouped-column bracket view (Winners / Losers / Grand Final sections; single-elim renders unchanged). `getTournamentStats()` reports best-finish from the stored round label so a double-elim "Losers Final"/"Grand Final" reads correctly. Committed tests in `backend/test/tournament-double-elim.test.js` (structure, generation, validation, a full play-through forcing a bracket reset, the no-reset path, an 8-player play-to-completion proving no dead-end pointers, and best-finish labels); headless-verified the setup toggle, power-of-two gating, and the grouped bracket view. `REFERENCE.md` §15 updated. The winners/losers-**tabbed** visual tree (build-order step 3) shipped separately as item 33 (its own ledger row below), completing the tournament roadmap — now moved to `docs/archive/` | `docs/archive/tournament-mode-roadmap.md` |
| Tournament mode: tabbed visual bracket view (item 33, build-order step 3) — the winners/losers-tabbed double-elimination bracket view that finishes tournament mode. `renderTournamentDetail()` (`frontend/index.html`) now renders a **Winners / Losers / Grand Final** tab switcher (`.tourney-tabs`, `role="tablist"`) for double-elim, showing one bracket panel (`role="tabpanel"`) at a time so the deep double-elim tree — up to ~19 rounds combined at 128 players — stays readable rather than one long stacked scroll (the roadmap's §4 "two scrollable panels with a tab switcher"). `tournamentBracketTab` holds the active tab (module-level, persists across the re-renders an action triggers, reset in `openTournament()`); `setTournamentBracketTab()` re-renders from `tournamentDetailCache` with no network round-trip; `tournamentTabKeydown()` gives the tablist standard roving-tabindex WAI-ARIA keyboard support (ArrowLeft/Right + Home/End move both selection and focus). Single-elimination keeps its single-column view with no tabs (unchanged). The always-visible "Up Next" list and the linearized "Full bracket (list view)" remain the non-spatial way to follow either bracket type. Headless-verified: the double-elim tabs render with correct ARIA/roving-tabindex, switching tabs swaps the panel to that bracket's rounds, keyboard ArrowRight moves selection+focus, and single-elim renders without tabs. `REFERENCE.md` §15 and README updated. Completes the tournament roadmap → archived | `docs/archive/tournament-mode-roadmap.md` |
| Saved games / pause & resume — `saved_games` context table (`game_id UNIQUE ... ON DELETE CASCADE`, per CLAUDE.md's convention); "⏸ Save for later" in both Pad and Dartboard modes (header + footer, survives the leg-complete transition screen); one saved game per (participant set, game type); a resume-or-abandon prompt at New Game Start time plus a browsable Saved Games list (both driven by `GET /api/saved-games`, whose one-line position summaries reuse the real rebuild functions); resume via `rebuildX01State()`/`rebuildCricketState()`/`rebuildBaseballState()`/`rebuildAroundTheClockState()`/`rebuildAroundTheWorldState()` (`frontend/scoring.js`) — pure functions replaying every recorded turn through the same `evaluateVisit*`/`evaluateDartAroundTheClock` engines that scored it live, including a trailing-leg-win edge case (saved before "Next leg" was tapped); a two-device divergence guard (`GET .../resume-state` atomically consumes the pause); tournament/league-fixture matches savable, with tournament-linked abandons routed to the bracket's walkover control instead of a plain delete; player-deletion guard; a merge-collision block (`_savedGameCollisions()`); committed tests (`backend/test/scoring.test.js`'s rebuild-function suite plus `backend/test/db.saved-games.test.js`'s full endpoint/guard coverage) | `docs/archive/saved-games-roadmap.md` |
| Guided Around the Clock / Around the World practice drill mode — two new game types (`around_the_clock`, `around_the_world`) turning the existing passive completion tracking into active drills with live progress feedback, reusing the Around the World Progress grid's UI investment; new `guided_clock`/`guided_world` badges; Home page leaderboards; committed tests | `docs/game-modes-roadmap.md` |
| Tournament mode: single-elimination (schema, bracket generation with cascading byes, match lifecycle/walkover, setup screen, bracket tree + Up Next view, live-scoreboard round label, player-deletion guard, committed tests) — double-elimination remains a separate open item above | `docs/archive/tournament-mode-roadmap.md` |
| Tournament: two new badges — 🏆 Champion (win a bracket) and ⚔️ Giant Slayer (Tournament) (beat an opponent seeded 3+ slots better), both awarded inline from `_advanceTournamentMatch()`, live-celebration detected via an `earnedBadgeCache` diff since neither the award nor the completion hook has a response channel back to the frontend; committed tests | `docs/archive/tournament-mode-roadmap.md` |
| Tournament: "Tournaments" stat block on the Player Profile (wins, runner-up count, best finish reached) — `getTournamentStats()`, `GET /api/players/tournament-stats`, committed tests | `docs/archive/tournament-mode-roadmap.md` |
| League mode (schema — `leagues`/`league_players` plus a nullable `games.league_id`; live-computed standings with no maintained tally; an `onGameCreated` auto-tag hook with a New Game "log to league?" picker for genuine multi-league ambiguity; season lifecycle (active/ended, reversible); a Leagues nav tab (list/setup/detail); a Home page teaser; a Player Profile "Leagues" stat block; committed tests) — X01 and Cricket support, and league fixtures/pending matches, both now shipped, see below | `docs/archive/league-mode-roadmap.md` |
| Dartboard zone/miss/bounce-out tracking — `darts.zone`/`miss_zone`/`miss_depth`/`bounced` columns; the generalized dartboard heatmap (`getDartHeatmap()`, `GET /api/players/dart-heatmap`) now shown on all four game-type Player Profile tabs instead of Just Chuckin' It only, with a "zone unspecified" hatch overlay for Pad-mode/pre-feature singles; Dartboard mode's flat Miss button replaced by a two-band (near/far) positional miss ring; a v1 flat-count "Bounce Out" button in every game type and both input modes except Checkout Trainer, which hides it (along with Miss and Undo Last Turn) as a live-match-only control (`getBounceOutCount()`, `GET /api/players/bounce-outs`) — v2 positional bounce-out capture remains gated on `docs/camera-scoring-roadmap.md`; committed tests, verified end-to-end with Playwright | `docs/archive/dartboard-zone-tracking-roadmap.md` |
| Coaching Insights — weak-number, checkout-route, bust-parity, and form-trend insights (`getCoachingInsights`, Player Profile X01 tab, committed tests) | `docs/archive/coaching-insights-roadmap.md` |
| Home Assistant automation recipe book (`docs/home-assistant-recipes.md`, linked from README) | `docs/archive/ha-recipes-roadmap.md` |
| Colorblind-friendly palette | `docs/archive/colorblind-mode-roadmap.md` |
| Admin login rate limiting + `OCHE_REQUIRE_AUTH` zero-trust default; SEC-7 webhook auth | `docs/security-hardening-roadmap.md` |
| Full-database admin JSON export (Settings → Data Export) | `docs/archive/data-export-roadmap.md` |
| Per-player data export AND import (admin-only, JSON) — "Export a player…"/"Import a player" on the same `#screen-player-export` admin page (Settings → Data Export); `getPlayerExport()`/`GET /api/players/export` bundles games/turns/darts for every game the player is in, including opponents' turns within those same games (H2H isn't stored anywhere, only derivable) plus minimal `{id,uuid,name}` opponent stubs; new `players.uuid` (v4 UUID) portable identity backing it; `importPlayerExport()`/`POST /api/players/import` resolves players by uuid first (uniquifying a name collision rather than merging), inserts games/turns/darts directly (bypassing lifecycle hooks), and skips already-present games by fingerprint so re-import is a safe no-op and an opponent stub upgrades in place when their own export is imported later; committed tests plus a real two-independent-server browser verification | `docs/archive/data-export-roadmap.md` |
| Per-player CSV export ("your own stats as a spreadsheet", admin-only) — `getPlayerCsvExport()`/`GET /api/players/export-csv?name=...&kind=games|turns`, "Games CSV" (one row per game with player-scoped aggregates: points, avg/turn, best turn, busts, checkouts, highest checkout, opponents, result) and "Turns CSV" (one row per turn thrown, with per-dart `T20 S5 D16` notation) buttons on the same `#screen-player-export` admin page; deliberately non-portable (no uuids, no opponents' turns, no import path — the JSON export above is the portable one); RFC-4180 encoding with CSV-formula-injection neutralization for hostile player names; committed tests — the last of this doc's items, doc now archived | `docs/archive/data-export-roadmap.md` |
| Player merge tool (admin-only) — `getMergePreview()`/`mergePlayers()`, `GET /api/players/merge-preview` + `POST /api/players/merge` (rate-limited, logged), Settings → Admin & Danger Zone → Merge Players with a REQUIRED preview modal (per-table move counts, auto-resolutions, ⛔ conflict list); reassigns every FK-into-`players` table in one transaction (including `league_fixtures` with its canonical player1<player2 ordering re-established, a table added after the doc's original inventory); hard-blocks on a shared game/tournament/league or an ambiguous same-day Daily Challenge pair; auto-resolves shared badges (MAX count, MIN earned_at) and exactly-one-completed challenge pairs; target's name/finish rule/PIN/uuid always win; new `player_uuid_aliases` table + `resolveStub()` fallback keeps old exports of the merged-away player importable onto the survivor (chained merges resolve in one hop); committed tests — the doc's only item, doc archived | `docs/archive/player-merge-roadmap.md` |
| Standalone `backend/admin-recovery.js` CLI (`list`/`reset-password`/`clear-lockout`) for a forgotten admin password or a stuck lockout via direct filesystem/container access; `changeAdminPassword()` now also clears lockout | `docs/archive/admin-account-recovery-roadmap.md` |
| Progressive admin-login lockout delay — replaces the flat 5-minute lockout with a doubling-per-failure backoff past a grace window, never fully blocking a correct password | `docs/archive/admin-login-backoff-roadmap.md` |
| Voice announcements (browser speech synthesis call-outs) | `docs/archive/voice-announcements-roadmap.md` |
| Backups v1: `backend/backup.js` script, retention pruning, documented restore procedure | `docs/archive/backups-roadmap.md` |
| Backups v2: Settings → Backups (download/retention/on-demand backup/restore-from-existing/upload-restore) | `docs/archive/backups-roadmap.md` |
| Backups: opt-in Docker Compose-profile sidecar (`docker-compose.yml`'s `backups` service, `profiles: ["backups"]`) | `docs/archive/backups-roadmap.md` |
| Shareable Moments: card generation, every trigger point (achievements, match win, Personal Bests), HA webhook | `docs/archive/shareable-moments-roadmap.md` |
| Shareable Moments: Player Profile "Moments" gallery (found already built, 2026-07 — every earned Badge Case tile has a Share button, `shareEarnedBadge()`) | `docs/archive/shareable-moments-roadmap.md` |
| Shareable Moments — every item, now archived (card generation + Moments gallery above; X auto-post explicitly rejected, not a gap) | `docs/archive/shareable-moments-roadmap.md` |
| 22 of 22 achievements/badges shipped (21 original candidates + Staircase Finish) | `docs/archive/achievements-badges-roadmap.md` |
| Checkout Trainer: difficulty tiers (Under 40 / Under 100 / Over 100 / Full Range 2-170), a session-scoped setup-screen toggle baked into `games.config.difficulty`; committed tests | `docs/archive/checkout-trainer-roadmap.md` |
| League mode: Cricket support (`leagues.game_type` column alongside X01, a setup-screen game-type selector, X01/Cricket cross-isolation in the auto-tag hook and `getEligibleLeagues()`, standings computation unchanged since it was already game-type-agnostic; committed tests) | `docs/archive/league-mode-roadmap.md` |
| League fixtures / pending matches — a new `league_fixtures` table (`game_id` FK into `games`, status derived not stored, same precedent as `tournament_matches`), single round-robin generation at league creation and again for new pairings on enrollment, `GET /api/leagues/pending-fixture` (order-independent pending-fixture lookup by player pair), `createGame()`'s `leagueFixtureId` (explicit fixture-to-game linking that bypasses the fuzzy `onGameCreated` auto-tag hook), and a read-only Fixtures list on the League detail screen; committed tests | `docs/archive/league-mode-roadmap.md` |
| New Game page revamp — the single all-controls-visible setup screen replaced by a 3-step wizard (Who's playing? → Choose a game → More options, Back buttons restoring not resetting prior-step state, `aria-live` step announcements + focus management); Step 1's select → "Add someone else?" loop reuses the existing player-add handlers unmodified; Step 2's one flat, player-count-filtered `NEW_GAME_MODE_OPTIONS` dropdown (including Baseball, shipped after this doc was first written) replaces the old Mode row/Practice-type sub-toggle/X01-Cricket-Baseball toggle, with Daily Challenge's already-attempted check moved to selection time and a how-to-play blurb per entry; Step 3 relocates every mode's existing options block unmodified; the "League Game" top-of-dropdown entry consumes `GET /api/leagues/pending-fixture` and `createGame()`'s `leagueFixtureId` (both shipped above); the old H2H record banner and category-based "log to league?" picker were both deleted as dead code (`GET /api/players/h2h` HTTP route removed with it, `getH2HRecord()` itself kept — still used internally); every mode verified end-to-end with ad hoc Playwright against a live server (no committed Playwright suite exists in this repo yet) | `docs/archive/new-game-flow-roadmap.md` |
| Badge award count threaded into the live achievement overlay (`patchAchievementCount()`, both `index.html` and `display.html`), not just the shareable moment card | `docs/archive/achievements-badges-roadmap.md` |
| Expanded Achievements & Badges — every item, now archived (all 22 badges + the notifications/count work above) | `docs/archive/achievements-badges-roadmap.md` |
| Simultaneous-achievements overlay fix (multi-badge queue) | `docs/archive/simultaneous-achievements-roadmap.md` |
| Daily/Weekly Challenge — all 6 formats, streak tracking, shareable card, Player Profile history tab, 3 challenge-specific badges | `docs/archive/daily-challenge-roadmap.md` |
| Ghost Opponent (X01 only) — leg-script replay, New Game leg picker, opponent-badge suppression (win/loss tracking is a separate, not-yet-built open item above) | `docs/archive/ghost-opponent-roadmap.md` |
| Server-side error logging + persistent rotating log + Settings "Server Errors" view (Part A); committed `node:test` suite + CI (Part B) | `docs/testing-and-observability-roadmap.md` |
| Colorblind mode, WCAG contrast audit fixes, `aria-live` announcements, accessible-input-path framing, type-size pass (standing checklist, not archived — ongoing per CLAUDE.md) | `docs/accessibility-roadmap.md` |
| Mobile: phone-responsive CSS pass (step 1) | `docs/mobile-app-roadmap.md` |
| All 14 security-audit findings (SEC-1 through SEC-14) fixed | `docs/security-audit-roadmap.md` |
| All 3 functional-defect findings (BUG-1 through BUG-3) fixed | `docs/bug-roadmap.md` |
| BUG-8: static frontend files served with no `Cache-Control` header, plus an unguarded `await DB.loadAll()` in the boot sequence — together could make a live server with intact data look fully broken to a user on a stale-cached device (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-16: `importPlayerExport()`'s duplicate-game guard correctly skipped re-inserting an already-imported game, but not its turns/darts underneath it — re-importing the same player export doubled their darts/turns silently (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-17: Ghost mode's past-leg picker (and race label) showed "Invalid Date" — two call sites read a raw SQLite timestamp straight into `new Date()` instead of using the sanitization pattern already established elsewhere in the same file (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-18: Ghost mode incorrectly offered a "Next leg" button instead of ending the race (game.practice being unconditionally true for Ghost mode blocked the set/match-win gate), and never marked its game complete server-side as a result (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| SEC-25 (seventh-pass audit): `addTurn()`'s SEC-22 scored-vs-darts consistency guard was never extended to Baseball, so a hostile client could poison every Baseball stat with a `scored` the darts can't produce (max real runs per visit is 9, but only the X01-shaped 0-180 range guarded it) — now derives the inning server-side and rejects a mismatch | `docs/security-audit-roadmap.md` |
| BUG-19 (seventh-pass audit): `getPlayerExport()` built one SQL bound variable per turn/dart, so exporting a player with more than ~32k turns threw "too many SQL variables" and 500'd — now batches the `IN (...)` reads | `docs/bug-roadmap.md` |
| BUG-20: Just Chuckin' It's live scoreboard heatmap lit up both single regions (inner/outer) of a number for any single hit — the live session tally and `buildChuckinLiveHeatmap()` were zone-blind even though dart recording and the lifetime Player Profile heatmap were already zone-aware (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-21: re-sharing an already-earned badge from the Badge Case produced a moment card with no explanation at all — `shareEarnedBadge()` never read the badge's own `desc` field; every card-building path now resolves a real explanation via `achDescFor()` (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-22: practice-mode Baseball games never called `DB.completeGame()` (a `!game.practice` gate copy-pasted from X01/Cricket's open-ended-practice template, which Baseball's own `completed_at`-dependent stat functions can't tolerate) — Games Played/Win Rate/every Personal Best stayed empty no matter how many practice games were played; practice Baseball now completes after its single forced leg, like Ghost mode (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-23: Cricket's scoring pad had no way to log a real off-target number hit (e.g. 1-14 in classic Cricket) — every one was forced through `Miss`, corrupting Dart Analytics' sector/treble-rate stats; added a collapsible "hit a different number" picker that records the real sector/multiplier instead, with zero scoring-logic changes needed (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-24: Cricket's (and Baseball's) lifetime dartboard heatmap silently hid every single hit, including on real target numbers, because `buildDartHeatmap()`'s "zone-unspecified single: excluded" rule assumed that always meant a Pad-mode choice — untrue for these two game types, which have no Dartboard-mode input at all; both single sub-regions now merge into one honest total instead (found while verifying BUG-23's fix) | `docs/bug-roadmap.md` |
| BUG-25: the New Game wizard's Step 2 "Continue" always advanced to Step 3 ("More options"), even for modes with nothing to configure there (X01/Baseball practice, Chuckin, Around the Clock, Around the World, Daily Challenge) — landing on an effectively blank page requiring an extra click; `setupGoToStep3()` now skips straight to `startGame()` when none of Step 3's five conditional sections are visible, leaving Cricket/Ghost/Doubles Practice/Checkout Trainer/H2H unaffected (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| Game Modes step 1: X01-to-plugin refactor, no behavior change | `docs/game-modes-roadmap.md` |
| Game Modes step 2: Cricket engine + customizable numbers, dedicated scoring screen, live scoreboard | `docs/game-modes-roadmap.md` |
| Game Modes step 3: Cricket stats parity (MPR, 9-Marks, Personal Bests, metric history, 2 achievements) | `docs/game-modes-roadmap.md` |
| Game Modes step 4: Home/Player Profile game-type navigation (X01/Cricket toggles + Cricket leaderboard set) | `docs/game-modes-roadmap.md` |
| Game Modes step 6: generalized N-way Player Profile/Home page game-type toggle mechanism | `docs/game-modes-roadmap.md` |
| Game Modes step 7: Doubles Practice (per-dart evaluation, stats, Personal Bests, undo, Home leaderboard set) | `docs/game-modes-roadmap.md` |
| Game Modes step 8: 101 as a fourth X01 starting score | `docs/game-modes-roadmap.md` |
| Game Modes step 9: Just Chuckin' It (heatmap-first stats, 18 laddered milestone achievements) | `docs/game-modes-roadmap.md` |
| Game Modes step 5: Baseball — core playable game (turn engine, fixed-9-innings New Game setup, dedicated scoring screen, `renderers.baseball` live scoreboard, 16-case committed unit suite for the scoring/round/match-completion rules). Stats/achievements parity split out as its own open item (#12) | `docs/game-modes-roadmap.md` |
| Game Modes: Baseball — Player Profile stats parity (RPI/Perfect Innings/Win Rate/Games Played/Darts Thrown/Best Inning stat bubbles, 5-field Personal Bests, 6 metric-history cases, `getBaseballWonLegs()`'s query-time leg-winner derivation in place of a stored `leg_won` signal) | `docs/game-modes-roadmap.md` |
| Game Modes: Baseball — achievements (🔥 Perfect Inning, 🏆 Perfect Game), matchwin moment card + Share button + per-leg comparison table (`h2hStatsHtmlBaseball()`), and Home page leaderboards (RPI/Perfect Innings/Wins/Perfect Game, `renderHomeTabBodyBaseball()`) — the last open item (#12) for Baseball, full stats/achievements parity now shipped (a `pracStatsHtmlBaseball()` practice-mode panel shipped alongside this too, later removed by BUG-22 once practice Baseball was fixed to always complete in 1 leg) | `docs/game-modes-roadmap.md` |
| Orientation-aware live scoreboard (portrait vs. landscape), built ahead of Cricket's scoreboard | `docs/archive/existing-app-prep-roadmap.md` item 11 |
| Stats query scope helper (`_scope()` in `backend/db.js`), now used by every stat query in the file | `docs/archive/existing-app-prep-roadmap.md` item 1 |
| Player-deletion-guard extensibility (`registerDeletePlayerGuard`) | `docs/archive/existing-app-prep-roadmap.md` item 6 |
| Game-lifecycle hook mechanism (`onGameCreated`/`onGameCompleted`) | `docs/archive/existing-app-prep-roadmap.md` item 4 |
| Settings page regrouping into 4 tabs (Account & Access, Gameplay & Display, Integrations, Admin & Danger Zone) | `docs/archive/existing-app-prep-roadmap.md` item 7 |
| `games.game_type`/`games.config` columns | `docs/archive/existing-app-prep-roadmap.md` item 2 |
| Context tables link into `games` via FK, never a new boolean on `games` (adopted convention) | `docs/archive/existing-app-prep-roadmap.md` item 3, see `CLAUDE.md` |
| Docker Compose profiles for future optional services (adopted convention) | `docs/archive/existing-app-prep-roadmap.md` item 9 |
| `throwDart(sector, multiplier)` input-source separation, confirmed protected through the plugin refactor | `docs/archive/existing-app-prep-roadmap.md` item 5 |
| Preparing the Existing App for Future Roadmaps — all 11 of its own items | `docs/archive/existing-app-prep-roadmap.md` |
| Dart Builder — every item, now archived: v1 (`dart_components`/`loadouts` schema, component/loadout CRUD, `game_players.loadout_id` game-creation integration, PIN-gated default-loadout + loadout-customization actions, per-loadout stats, Dart Builder screen, Player Profile "Default Loadout" selector, New Game "Change Loadout" picker), the loadout comparison view ("⚖️ Compare Loadouts", side-by-side stats via the existing `getLoadoutStats()`, no new backend query), the accessibility icon set (barrel shape/grip + flight shape as an accessible icon-button group, closing the "torpedo"/"knurled"/"kite" self-explanatory-name gap), and "⚡ Quick Add Full Set" (name + every barrel/shaft/flight field on one screen, one save). Optional photo upload per component was considered and explicitly dropped, not built — it was framed as an alternative to the icon set, not additive to one | `docs/archive/dart-builder-roadmap.md` |
| Cricket badge parity: Night Owl/Early Bird now fire from Cricket turns too, via a shared `awardTimeOfDayBadges(p)` helper called from both `enterTurn()` and `enterTurnCricket()` — previously X01-only by accident of code structure, not design | `docs/game-modes-roadmap.md` |
| Cricket-native badges: 🧹 Whitewash (won without the opponent closing a single number) and 🔥 Comeback Kid (Cricket) (won after trailing on points by 20+, `p.legWorstPointsDeficit` tracked per-visit in `enterTurnCricket()`) — both 2-player only, both checked in `onLegWonCricket()`, both with pure trigger-condition functions (`isCricketWhitewash()`/`cricketComebackAchieved()`) unit-tested in `backend/test/scoring.test.js` | `docs/game-modes-roadmap.md` |
| Ghost Opponent — every item, now archived: the original race feature, race win/loss tracking (`ghost_races` table, `recordGhostRace()`/`getGhostRaceRecord()` server-side re-validating the source leg so a client can't fabricate a fake win history, `POST /api/ghost-races` + `GET /api/players/ghost-race-record`, a "👻 Ghost races: W–L" line on the Player Profile), and the 👻 Ghost Slayer first-win badge (awarded inline from `recordGhostRace()`, idempotent via `awardBadge()`'s existing `once` mode) | `docs/archive/ghost-opponent-roadmap.md` |
| Checkout Trainer, both sub-modes (schema — `turns.target_score`, `games.config.mode`/`durationSec`, the generalized `NOT_HYPOTHETICAL_DARTS` physical-stat exclusion; `pickCheckoutTarget()`/`gradeCheckoutAttempt()` pure grading logic; untimed Freeform plus the 60-second Checkout Blitz sprint with its wall-clock countdown and server-derived scoring; `getCheckoutBlitzLeaderboard()`; 4 Freeform + 1 Blitz milestone ladders (28 tiers) plus 5 one-off badges, all reusing the existing Chuckin milestone engine; New Game screen integration; Home page teaser + Player Profile stat bubbles/Personal Bests; committed tests) — difficulty tiers and the trick-question/Bogey Buster variant now also shipped, see their own entries | `docs/archive/checkout-trainer-roadmap.md` |
| "Drill this checkout" deep link — a 🎯 Drill button on Top Finishes rows (Player Profile and Home) and on Coaching Insights' `checkout_route` card jumping into Checkout Trainer with `games.config.pinnedTarget` set (every round serves that number, Freeform only, trick questions force-disabled); `raceLeg()`'s preselect-then-confirm pattern reused for the deep link (`drillCheckout()`, one-shot `_checkoutDrillPin`/`_enteringSetupFromDrill`, jump straight to setup Step 3 with focus on Start); setup-screen "Drilling: N ✕" chip (`renderCheckoutTrainerPinChip()`/`clearCheckoutTrainerPin()`); server-side `pinnedTarget` integer-2-170 validation in `createGame()`; Toughest Checkout Solved excludes pinned rounds via a `games.config` scope, no schema change; committed tests | `docs/archive/checkout-drill-link-roadmap.md` |
| First-9 average — the standard pro scoring stat (3-dart avg over each leg's first 9 darts), scoped to 501/301/170/101 (`OPENING_CATS`, now module-level in `backend/db.js` and shared by all 4 surfaces): the `first9avg` Player Profile bubble and `getMetricHistory()` chart metric already existed from an earlier "opening exchange" stats pass but had a stale "(501/301 only)" UI label (fixed) and no Personal Best or leaderboard; this change added `bestFirst9` to `getPersonalBests()` (MAX per-leg, deliberately not won-legs-only unlike `bestLegAvg`, no Ghost Opponent race-this-leg button) and a "Best First-9 Average" Home leaderboard (`first9Rows`, `HAVING legs >= 20`, ranked descending); committed tests | `docs/archive/first-nine-average-roadmap.md` |
| Darts-culture badges + 180s ladder (Badge Expansion 2 Part A) — 🍳 Bed & Breakfast (exact S20/S5/S1 visit), 🏚️ Madhouse (won on double 1), 🀄 Shanghai (S+D+T of one number in an X01 visit), each a pure predicate in `frontend/scoring.js` (`isBedAndBreakfast()`/`isMadhouseFinish()`/`isShanghaiVisit()`, unit-tested) wired into `enterTurn()`'s existing `CHAIN_CHECKS`; plus a 5-tier lifetime-180s milestone ladder (10/25/50/100/250, `ONE_EIGHTY_MILESTONE_LADDERS`) reusing `CHUCKIN_MILESTONE_LADDERS`'s data-driven shape and `checkChuckinMilestoneTier()` wholesale, permanent/non-revocable tiers, no category flag (folds into the existing X01 Badge Case section); badge total 89→99 (X01 23→33), updated in REFERENCE.md §4 and all 3+1 README mentions | `docs/archive/culture-badges-roadmap.md` |
| Badge-coverage parity for Baseball & Doubles Practice (Badge Expansion 2 Part B) — Baseball: ⚾ Walk-Off (`game.baseballInning > 9` at leg-win), 🔄 The Cycle (`isBaseballCycle()`, `frontend/scoring.js`, unit-tested — S+D+T of the inning's own target number in one visit), and a 4-tier lifetime-runs ladder (100/500/1,500/5,000, `BASEBALL_RUNS_MILESTONE_LADDERS`); Doubles Practice (previously zero badges): a 4-tier lifetime doubles-hit ladder (50/250/1,000/5,000, `DOUBLES_HIT_MILESTONE_LADDERS`) plus 🎪 Ring Master (every double D1–D20 + bull hit lifetime, `getDoublesPracticeHitSectors()`/`GET /api/players/doubles-hit-sectors`, direct structural analog of `getAroundTheWorldProgress()`), both explicitly one-off/permanent/non-undo-revocable per the roadmap doc; new `doublesPractice:true` Badge Case section (`renderPlayerBadges()`); badge total 99→110 (Baseball 2→8, Doubles Practice 0→5), updated in REFERENCE.md §4 and README's Achievements/Player Profile sections; doc fully done, moved to `docs/archive/` | `docs/archive/culture-badges-roadmap.md` |
| Checkout Trainer: trick-question/bogey-number difficulty variant — opt-in New Game toggle (`games.config.trickQuestions`, off by default) serving an actual bogey number ~1 round in 8 (`listUnsolvableTargets()` derived from `checkoutHint()`'s own unfinishable signal, difficulty-tier-aware); a "🚫 No possible checkout" answer button (`declareUnsolvable()`/`gradeCheckoutDeclaration()`) whose correct call grades as that round's optimal answer (2 Blitz points, counts toward every ladder/streak/percentage) and whose wrong call reveals the real route; declarations recorded as `turns.declared_unsolvable=1` with zero dart rows (the one `addTurn()` shape allowed to, locked to this game type) and excluded from Toughest Checkout Solved and the route-specific one-offs; the conditional 💣 Bogey Buster badge (first correct call); committed tests — the last of this doc's items, doc now archived | `docs/archive/checkout-trainer-roadmap.md` |
| Cut-throat Cricket — the variant deferred at Cricket v1: `config.variant: 'standard' \| 'cutthroat'` flag on the existing `game_type='cricket'` (not a new game type), same marks/closing rules but bonus points land on EVERY open opponent instead of the shooter (full amount each, not a split), lowest score (all closed) wins instead of highest (`evaluateVisitCricket()`/`rebuildCricketState()`, `frontend/scoring.js`); Standard/Cut-throat toggle in New Game Step 3 (blurb nudges toward 3+ players without blocking 2); a "puts N on X and Y" screen-reader announcement + "Pts (lowest wins)" scoreboard label (live screen and `/display`, via a new `cricketVariant` live-payload field); Comeback Kid (Cricket)'s deficit direction flips per variant, Whitewash unchanged; server-side `config.variant` validation in `createGame()`; the 🔪 Stone Cold badge (win a 3+ player cut-throat game having received zero points across the whole match, tracked via a game-scoped `gamePointsReceived` that survives leg resets); mark-based Cricket stats (MPR, 9 Marks) unaffected — no points-based Cricket leaderboard existed to need variant-scoping; found and fixed a pre-existing, unrelated gap while adding the new badge — `display.html`'s own hand-copied ACH_LABELS/ACH_DURATION/ACH_DESC maps were missing 9 static badges added by earlier features, silently blanking their live-overlay headline on the /display screen (BUG-26, `docs/bug-roadmap.md`); committed engine tests (both open questions from the design doc resolved with tests either way) plus a `display.html`/`index.html` badge-map parity test | `docs/archive/cutthroat-cricket-roadmap.md` |
| Bob's 27 doubles routine (Part A) — new solo-only `bobs_27` game type mirroring Baseball's structure: start on 27, 3 darts per round at that round's own double D1→D20, every hit adds double its value and a miss-all round subtracts it instead, dead at ≤0 or survived past D20; store-the-gain/derive-the-penalty data model (`turns.scored` only ever holds the round's positive gain, the penalty derived at read time from `scored===0` — a fresh pattern for this codebase, proposed but not yet built for `docs/archive/halve-it-roadmap.md`); round derived from prior-turn count (SEC-25/Baseball-inning pattern), a write-time consistency guard re-deriving both the expected gain and the `bust` flag from replayed prior turns; "a run IS the game" (`legsPerSet`/`setsPerGame` forced to 1, practice-Baseball's BUG-22 shape); 5 stat bubbles, 2-field Personal Bests, a peak-single-run Home leaderboard (no minimum floor); 7 badges — 🎯 Full House, 🏔️ The Full Anderson (a perfect 1287), and a 5-tier survival/score ladder checked against each run's own final score rather than a lifetime total (`checkChuckinMilestoneTier()` reused wholesale — confirmed to be value-source-agnostic); savable/resumable (`rebuildBobs27State()`, pure replay); badge total 111→118; found and fixed a second instance of BUG-26 while wiring badges into `display.html` — three entire pre-existing ladders (X01 180s, Baseball runs, Doubles Practice hits, 13 badge IDs) had never been mirrored there at all (only Chuckin's ladder had the mirror treatment), so their live-overlay headlines had been rendering blank on the `/display` screen since each shipped; fixed alongside Bob's 27's own ladder via a shared mirrored `forEach` + a new `extractLadderIds()`-based parity test hardened to catch a whole-ladder omission, not just a drifted threshold; committed tests (`scoring.test.js`, `db.turn-consistency-guard.test.js`, `db.bobs27-stats.test.js`, `display.ach-labels-parity.test.js`); verified end-to-end with Playwright (setup, Full House, miss, undo, save/resume, death path, badges, stats, leaderboard) — see Part B (the 121 Checkout Ladder) below, also done | `docs/archive/practice-ladders-roadmap.md` (Part A) |
| Household Elo rating (Part A) — live-computed, never stored: `getEloRatings()` walks every completed, non-practice, 2-player game (across every competitive game type combined into one rating) in `(created_at, id)` order, K=32 from 1000, `expected=1/(1+10^((theirs-mine)/400))`, a zero-sum delta (not two independently-rounded formulas); Home page "📈 Household Ratings" leaderboard (rating+W/L, min 5 rated games, piggybacked onto `getHomeExtra()`'s payload so it's visible regardless of the per-game-type toggle); Player Profile rating/rank/rating-over-time sparkline section (a bespoke minimal chart rather than reusing `getMetricHistory()`'s period-bucketed one, which doesn't fit an event-driven per-game series); a match-win delta patched into a `#elo-delta-banner` placeholder once the async post-match fetch resolves (the same "celebrate now, patch in extra detail once confirmed" pattern `#challenge-pb-banner` uses); 👑 Top of the House (reach #1, once-badge) and 🗡️ Upset (beat someone rated 150+ above you, recurring) badges, checked via the same fetch, wired into X01/Cricket/Baseball's own "game (match) won" branches; heals automatically after undo/merge/game-deletion/import since nothing is stored; badge total 118→120; `game_players.start_score` column added as shared groundwork for Part B (Handicapping), which the rating walk already excludes on; committed tests (`backend/test/db.elo.test.js` — hand-verified K=32 arithmetic for a single win and a rematch, plus the Upset threshold/games-floor/every exclusion) and a live Playwright pass (a real 2-player X01 match through the actual UI, confirming the rating/Home/Profile all match the hand-computed numbers exactly) — see Part B (Handicapping) below, also done | `docs/archive/rating-and-handicap-roadmap.md` (Part A) |
| Handicapping (Part B) — nullable `game_players.start_score` per-player, per-game X01 starting-score override (`NULL` = the game's own `config.startingScore`), server-side-validated in `createGame()` (integer, `101 <= startScore < category`, X01-only, the same "never trust the client" precedent `pinnedTarget`/Cricket's variant validation set); a collapsed-by-default "Handicap" disclosure in the X01 New Game options step (`renderHandicapOptions()`, one per-player `<select>`); `newMatchPlayer()`/`resetPlayerForNextLegX01()` seed/reset from each player's own start instead of the shared category; a "STARTED 401" live-scoreboard tag (`renderers.x01.card()`); win-based stats count a handicapped win normally (the whole point is a fair contest), but a new `NOT_HANDICAPPED` SQL fragment excludes a handicapped leg's shortened-start finish from all 6 nine-darter-detection call sites plus `fewestDartsCheckout` (mechanically easier, not a skill feat) while leaving average-based stats untouched (starting-score-agnostic already); Household Elo's own exclusion (shipped with Part A) reuses the same column; committed tests (`backend/test/db.handicap.test.js`) plus a live Playwright pass (a real 2-player X01 match set up through the actual New Game UI, one player handicapped to 401, confirming both starting scores, match completion, and the handicapped winner's Elo correctly excluding the game) — both halves of this roadmap doc are now done, moved to `docs/archive/` | `docs/archive/rating-and-handicap-roadmap.md` (Part B) |
| 121 Checkout Ladder (Part B) — new solo-only `checkout_ladder` game type: start on 121, double out, up to 3 visits (9 darts) per attempt; each attempt is its own leg (`leg_no` increments per attempt), reusing `evaluateVisit()` — the shared X01 visit evaluator — completely unmodified; ladder movement (+1 rung on a win, −1 on a fail, floored at 61, capped at 170 since `turns.target_score`'s valid range tops out there) is never stored, re-derived at write time (a new consistency-guard branch validating the 3-visit cap and the derived `targetScore`), at read time (`_checkoutLadderAttempts()`), and at resume time (`rebuildCheckoutLadderState()`, pure replay) — three independent call sites that must agree; leg transitions reuse `startNextLeg()`, the same generic function X01 itself uses, rather than a bespoke reimplementation; 4 stat bubbles (Attempts, Success Rate, Current Ladder Position, Darts Thrown), a 2-field Personal Bests (Highest Target Reached counts a failed peak attempt; Fewest Darts on the Highest Checkout only looks at the highest attempt actually won), a peak-single-run Home leaderboard (no minimum floor); 7 badges — a 6-tier highest-rung ladder (125/130/140/150/160/170, checked against the position just climbed to) plus 🧗 Peak Bagged (recurring — actually checking out 170 itself, not just reaching that rung); a `renderers.checkout_ladder` live-scoreboard card in `display.html` (mirrors X01's own card, swapping the leg/set standing line for target/attempt/visit); savable/resumable; badge total 120→127; committed tests (`backend/test/scoring.test.js`, `backend/test/db.turn-consistency-guard.test.js`, `backend/test/db.checkout-ladder-stats.test.js`); verified end-to-end with Playwright (climbing 121→126 across 6 real checkouts confirming the 125 badge, a failed attempt dropping the target, undo mid-attempt, save/resume round-tripping a mid-attempt position exactly, and the live `/display` scoreboard) — both halves of this roadmap doc are now done, moved to `docs/archive/` | `docs/archive/practice-ladders-roadmap.md` (Part B) |
| The Gauntlet — new solo-only `gauntlet` game type: 20 stations in the fixed, hardcoded `GAUNTLET_STATION_ORDER` (the standard clockwise dartboard walk, no seed/generation needed); 3 positionally-graded darts per station via a new pure `evaluateGauntletStation(stationNumber, darts)` (dart 1 the single, dart 2 the treble, dart 3 the double, strictly by throw order, no re-matching across positions); a Scar/repeat rule (2 misses → one repeat, the retry's own result final regardless; 3 misses → an immediate Deep Scar, doubled at tally time, `gauntletTotalScars()`) landing on a derived result tier (`gauntletResultTier()`: Unmarked → The Gauntlet Wins); reuses `turns.target_score`/`scored` only, no new columns — station identity lives in `target_score` directly rather than being derived from `leg_no`/prior-turn-count the way every other new game type this batch used, since a repeat attempt breaks that trick; a single shared `rebuildGauntletState()` (frontend/scoring.js) derives "which station is next, is one awaiting its repeat, is the run done" for the write-time guard, every stats query, AND saved-game resume, so it's computed exactly once; the write-time guard collapses the sequence + repeat-count checks into one comparison against that shared derivation, plus a separate 0-3 scored-range check; a run always completes after all 20 stations settle (`legsPerSet`/`setsPerGame` forced to 1, the practice-Baseball/Bob's 27 "a run IS the game" shape) — unlike Checkout Ladder's perpetual shape, this is the one new game type this batch that reaches a real `finishUnit('game', ...)` ending; 5 stat bubbles (Runs Completed, Avg Total Scars, Clean Station Rate, Deep Scar Rate, Retry Rate), an ascending-is-better Personal Best (Lowest Total Scars, completed runs only) and Home leaderboard (the one leaderboard in this app sorted ascending); **the Scar Map** (`getGauntletScarMap()`) — average final miss count per station across every completed run, rendered as a severity-shaded grid in `GAUNTLET_STATION_ORDER`'s own order (already the standard clockwise board walk) plus a text-table accessibility fallback; 14 badges — a 4-tier lifetime-runs ladder, a 4-tier lifetime-clean-stations ladder, a 3-tier per-run clean-streak ladder (checked once at run end against that run's own peak streak, the Bob's 27 final-score pattern, not a lifetime count), plus 💎 Flawless Gauntlet, 🥋 Unmarked, and 🩹 Second Wind (all three recurring); no live-scoreboard sync needed (same reasoning Checkout Trainer's own reached — no `renderers.gauntlet` in `display.html`); savable/resumable; badge total 127→141; found and fixed an unrelated bug while wiring this game type's own `bubbleKeyMap` onto the shared assignment list — Checkout Ladder's (item 22, shipped just before this one) had never actually been added, silently blanking its own Player Profile stat bubbles; committed tests (`backend/test/scoring.test.js`, `backend/test/db.turn-consistency-guard.test.js`, `backend/test/db.gauntlet-stats.test.js`); verified end-to-end with Playwright (a full 20-station run — 17 clean, one 2-miss station resolved by a clean repeat earning Second Wind, one Deep Scar, one final clean station — landing on 6 total Scars/"Scarred but Standing" and the correct streak-ladder badges; a partial run saved and resumed with its exact station/repeat/miss state; undo mid-attempt) | `docs/archive/gauntlet-roadmap.md` |
| Killer — new H2H-only `killer` game type (elimination format, real best-of-N legs/sets, unlike every solo drill this batch): server-assigned per-player numbers (`assignKillerNumbers()`, a Fisher-Yates shuffle of 1-20 zipped to player names, computed inside `createGame()` and never trusted from the client, returned back to the client as `config.numbers` and assigned onto `game.players[i].number` before the scoreboard's first render); a per-dart evaluator (`evaluateDartKiller()`) since one visit's three darts can each affect a *different* player — own-number hits build lives toward the configurable become-a-killer threshold (2/3/5, standard 3) at the ring-scaled rate every dart scores at elsewhere in this app, then a killer's hits on an opponent's number remove lives at the same scaled rate, and a self-kill (own double, already a killer) costs a flat 1 life; a genuine schema addition — a new nullable `turns.affected_player_id` column, since a single `turns` row per visit could never represent per-dart cross-player effects, making Killer the one game type in this app storing one `turns` row per dart, not per visit; the write-time guard replays the whole leg via the shared `rebuildKillerState()` and rejects any turn whose claimed `affectedPlayer`/`scored` doesn't match `evaluateDartKiller()`'s own independent recomputation; the existing interactive Dartboard SVG scoring screen reused unmodified (`throwDartKiller()` has the same per-dart-commit signature Doubles Practice/Chuckin already use); 5 stat bubbles (Games Played, Win Rate, Avg Kills/Leg, Avg Lives Lost/Leg, Survived Without Killer Rate), a Personal Best (Most Kills in a Leg), and a win-rate Home leaderboard reusing `getBaseballWinLeaderboard()`'s exact shape unmodified; a new `renderers.killer` live-scoreboard card (number, lives as pips with an `aria-label`, killer/eliminated status) plus a Killer-shaped `game.legSummary` branch; 3 recurring one-off badges — 🩸 First Blood (match's first elimination), 🛡️ Untouchable (won without ever losing a life), 🙈 Own Worst Enemy (self-eliminated via your own double); a new `h2hOnly` flag on `GAME_TYPES` (the inverse of the existing `soloOnly` flag) keeps it off the practice/1-player New Game list entirely; badge total 141→144; **deliberately no save/resume support** (`SAVABLE_GAME_TYPES` excludes `killer`) — a scope decision, not a technical limitation, since mid-match state is fully re-derivable from replaying `turns` the same way stats already do; committed tests (`backend/test/scoring.test.js`, `backend/test/db.turn-consistency-guard.test.js`, `backend/test/db.killer-stats.test.js`); verified end-to-end with Playwright (New Game setup flow through the lives-threshold selector, random distinct number assignment, building lives to become a killer including a one-treble instant threshold clear, attacking and eliminating an opponent, First Blood/Untouchable badges, a full multi-leg best-of-N match to completion with the correct winner and GAME OVER summary, stat bubbles/Personal Best/win leaderboard via the API, and the live `/display` `renderers.killer` card showing number/lives-pips/throwing state); this doc (`docs/game-modes-roadmap.md`) is **not** archived since it still covers other not-yet-built variants (Shanghai, Halve-It, Round the Clock, etc. — tracked separately elsewhere on this list) | `docs/game-modes-roadmap.md` |
| End-of-Night Session Recap — a pure read-time aggregation over a single local calendar date (`getSessionRecap(date)`, nothing stored): every completed H2H game that date fully itemized (per-2-player-matchup win/loss records, a flat list for 3+ player games), non-H2H activity folded into a light per-player+game-type `soloActivity` summary rather than itemized; `perPlayer`'s best-visit/best-leg-average scoped to X01 only (same scope `getPersonalBests()`'s own `bestLegAvg` uses); `personalBestsSetTonight` computes 3 well-defined single-number X01 records (leg average, fewest darts to check out, highest checkout) twice per player — once for tonight, once for every day strictly before it — and only flags a record that beats the pre-tonight baseline in the correct direction, never re-firing for a worse leg played later the same night; a chronological `moments` timeline merging 180s/ton-plus-checkouts/match-wins/badges; a Home page teaser (`renderHomeRecapTeaser()`, a genuine network round-trip since "did anyone finish a game tonight" can't be derived client-side) hidden until at least one H2H game has completed that date, wired into both `show('home')` and the boot sequence (the initial load never calls `show('home')`, the same reason the Daily Challenge teaser needed its own explicit boot-time call); a recap screen with a date picker (any past night recomputable for free) and a **📤 Share** button rendering the night through the existing shareable-moment card generator as one new card layout, no new canvas code; committed tests (`backend/test/db.session-recap.test.js`); verified end-to-end with Playwright (the Home teaser appearing after a completed match, the full recap screen render, and Share producing a real downloaded card image); moved to `docs/archive/` | `docs/archive/session-recap-roadmap.md` |
| Marathon Mode — a 45-minute solo endurance SESSION WRAPPER, not a new game type: `marathon_sessions`/`marathon_session_legs` (the `league_fixtures`-style "context table with a `game_id` FK" pattern per CLAUDE.md), every leg a completely unmodified solo practice 501 X01 game (`game.gameType` stays `'x01'` throughout, contributing to lifetime X01 stats/Personal Bests/Nine-Darter exactly like any other practice leg); each leg's `game_id` created server-side by `startMarathonSession()`/`startNextMarathonLeg()` themselves, never accepted from the client; the wall clock checked only at leg boundaries (`finishMarathonLeg()`), never mid-leg — real session length can run past 45 minutes by however long the final leg takes, a deliberate tradeoff; a manual "End Marathon" control ends the session immediately (not "wait for this leg"), leaving the in-progress leg unfinished but its stats kept; two new pure analysis functions in `frontend/scoring.js` (`computeFatigueSplit()` → first-half-vs-second-half average dart count, clamped at zero, landing on an Iron/Tested/Fading/Running-on-Empty tier; `classifyMarathonTrend()` → a 3-segment early/middle/late read landing on The Cliff/The Warm Machine/Flat Line/Inconclusive, floored at 6 legs); a persistent session banner (elapsed/remaining, leg count, `aria-live`) layered above the completely unmodified `renderers.x01` scoreboard, no new live-scoreboard work; 6 stat bubbles, 2 Personal Bests (one ascending, one descending), a lowest-fatigue-split Home leaderboard (ascending, same direction as The Gauntlet's); 11 badges (two lifetime ladders — sessions completed, legs completed — plus 🛡️ Iron/📉 Flat Line/⏱️ Full Distance); **deliberately no save/resume support**, the same scope decision Killer already made (a resumed leg would rebuild through the generic X01 resume path with no marathon linkage at all); badge total 144→155; **a real BUG-18-class bug found and fixed along the way**: the generic X01 `onLegWon()` only cascades a leg win into `finishUnit('game',...)` when `!game.practice`, exactly the same carve-out Ghost Opponent races already needed (`|| game.hasGhost`) — Marathon legs are `practice=1` too, so without extending that same condition to `|| game.marathonSessionId`, every leg would show an endless "Next leg" panel and the session would never auto-chain or end; committed tests (`backend/test/scoring.test.js`, `backend/test/db.marathon-mode.test.js`); verified end-to-end with Playwright (the full setup flow, the persistent banner, multi-leg auto-chaining, a manual End Marathon stop, the analysis screen, badges, and Player Profile/Home wiring); moved to `docs/archive/` | `docs/archive/marathon-mode-roadmap.md` |
| Shanghai game mode — `game_type='shanghai'`, structurally a sibling of Baseball: a fixed round sequence 1..`config.rounds` (default 7, a 20-round long-form option), all players in lockstep on one shared live round (`game.shanghaiRound`); only the round's own number scores (S/D/T = 1×/2×/3× the round number), evaluated by `evaluateVisitShanghai()` in `frontend/scoring.js`; a Shanghai (single, double, AND treble of the round's own number, one visit, any order) wins the WHOLE match instantly, mid-round; absent a Shanghai, the match ends once the final round completes with a single unique points leader, else extra rounds repeat the final round's own number (Baseball's extra-innings precedent); the SEC-25-style consistency guard in `addTurn()`, with a corrected 9× (not the source doc's stated 6×) ceiling — three trebles of the round's number is a real, legal, non-Shanghai visit worth more than a Shanghai itself, a correctness fix over the doc's literal wording, not a deviation from its intent; `turns.leg_won=1` set ONLY for a genuine instant Shanghai (self-referential, Cricket/Killer-style) — a final-round win decided by point totals is never flagged, so `getShanghaiWonLegs()` derives that winner at query time via `SUM(scored)` comparison instead, exactly `getBaseballWonLegs()`'s own derivation (also a refinement over the source doc's literal "or on the final-round winner's closing visit" wording, which isn't always the same turn as the actual points leader); saved-games-compatible by construction (`rebuildShanghaiState()`, pure replay of recorded turns); a `renderers.shanghai` per-round score grid on both the live scoreboard and `/display` (row labels show the round's own TARGET NUMBER, not the round index, so the grid stays meaningful once extra rounds begin); a single-button `renderPadShanghai()` mirroring Baseball's own; the instant-Shanghai moment fires `announce()` plus an icon+text 🀄 banner; 6 stat bubbles (Points/Round, Shanghais Thrown, Win Rate, Games Played, Darts Thrown, Best Round), Personal Bests, a 3-board Home leaderboard set (Points Per Round, Shanghais Thrown, Most Shanghai Wins), and the 🀄 Shanghai! badge (badge total 155→156); no lifetime-points ladder built (the source doc left it optional, "if play shows appetite"); committed tests (`backend/test/scoring.test.js`, `backend/test/db.shanghai-stats.test.js`, `backend/test/db.turn-consistency-guard.test.js`); verified end-to-end with Playwright (practice and H2H setup, an instant mid-round Shanghai win, a final-round decider correctly awarded to the higher-points player rather than whoever's turn ended the round, badges/stat bubbles/personal bests/win leaderboard, and the live `/display` scorecard); moved to `docs/archive/` | `docs/archive/shanghai-roadmap.md` |
| Halve-It game mode (core game only — the custom target editor is split out as its own separate open item above) — `game_type='halve_it'`, structurally another Baseball/Shanghai sibling: a fixed round sequence (the classic 7-round default — 20, 16, double 7, 14, treble 10, 17, Bull — `config.targets`, an ordered `{sector, ring?}` array), all players in lockstep on one shared live round (`game.halveItRound`); only the round's own target scores (`halveItDartValue()` in `frontend/scoring.js` — any ring at face value when unrestricted, only the named ring when restricted; Bull falls out of the same `mult*sector` formula for free since there's no treble-bull ring); a visit that gains exactly 0 halves the running total, rounding UP (`Math.ceil(total/2)` — round-down risks a permanent 1→0 spiral, round-up's floor is 1→1); highest total after the final round wins, with no instant-win condition at all (ties continue into extra rounds repeating the final target, the same Baseball/Shanghai precedent, filling a gap this doc's own design left unaddressed); `turns.scored` stores the gain only (0 on a halved visit) and `turns.bust=1` is repurposed as the halving flag (the same column-repurposing precedent Doubles Practice/guided Around the Clock set), validated by a SEC-25-style consistency guard that checks `bust` for consistency with the derived gain rather than rejecting it outright; a genuine technical wrinkle beyond the source doc's own anticipation — the running total is order-dependent (halving interspersed with additions), so it can't be read back with a single `SUM(scored)` aggregate the way every other fixed-round game type's stats can, requiring a one-off `_replayHalveItLegTotals()` (`backend/db.js`) that replays the raw turns once instead; savable/resumable (`rebuildHalveItState()`, including per-player `everHalved`/`lastVisitHalved` reconstruction so a resumed leg's badge check still sees the whole leg's halving history); a per-round chalkboard grid on both the live scoreboard and `/display` showing the round's own TARGET LABEL as the row header and the RUNNING TOTAL (not a per-round delta) with a ½ marker on a halved round; 6 stat bubbles, 5-field Personal Bests (best single round living in the stat bubbles instead, per Baseball's own Best Inning split), a 2-board Home leaderboard set (Highest Final Total, Most Halve-It Wins), and 2 flavor badges (🪓 Halved at the Death, 🛡️ No Half Measures — badge total 156→158); committed tests (`backend/test/scoring.test.js`, `backend/test/db.halve-it-stats.test.js`, `backend/test/db.turn-consistency-guard.test.js`); verified end-to-end with Playwright (practice setup, a hit, a halving with correct round-up math, a ring-restricted round, a full 7-round game to completion, the No Half Measures badge, stat bubbles/personal bests/leaderboard, live scorecard target labels, and the `/display` scorecard); the custom target editor split out above shipped later (its own ledger row above), so this doc is now fully done and moved to `docs/archive/` | `docs/archive/halve-it-roadmap.md` |
| Dead Man Walking — new solo-only `dead_man_walking` game type, structurally the closest existing precedent is 121 Checkout Ladder (real X01-shaped visits, reusing X01's own bust/win legality from a non-501 deficit), but with a personalized, frozen-at-creation deficit and budget rather than a shared climbing target, and a bust here immediately fatal to the round (no up-to-3-visits retry the Ladder allows); `getWeakestCheckouts(playerName, count)` (`backend/db.js`) reuses Coaching Insight #3's own remaining-score reconstruction (the `SUM() OVER (... ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING)` window-function trick) to rank a player's own double-out X01 remaining-scores (32–170) by `0.5*bustRate + 0.5*nonCompletionRate`, floored at 8 samples, bogey numbers excluded via `checkoutHint()`'s own unfinishable signal, cold-start falling back to `CHALLENGE_CHECKOUTS` (moved to `frontend/scoring.js` so this server-side path can share it with Daily Challenge); `deadManWalkingParForTarget()` computes `par = max(historicalAverage, objectiveOptimal+1)` or `objectiveOptimal+2` with no history — the floor is the one concrete correctness property this doc adds (the round's actual budget, `par-1`, can never drop below the objective-optimal dart count), verified by an exhaustive test across every finishable score 2–170; `games.config.rounds` (15 `{target,par}` pairs) computed once, server-side, inside `createGame()` and frozen for the whole session — never client-supplied or live-recomputed, the same security posture as Killer's number assignment; a new per-dart (not per-visit) `evaluateDeadManDart()`/`resolveDeadManDart()` pair (`frontend/scoring.js`) since the budget won't generally be a multiple of 3 and a bust/finish must end the round the instant it happens; no new columns — `bust`/`checkout` reused in their ordinary X01 sense (Walked Out iff any turn has `checkout=1`, Executed otherwise whether by a real bust or by exhausting the budget without one — the two Executed flavors distinguished live in `announce()`/status text but not stored distinctly, matching this doc's own framing); the write-time guard reuses the X01/Checkout Ladder dart-sum/bust/checkoutPoints arithmetic plus a new per-round **variable** dart-budget guard generalizing the Ladder's flat 9-dart cap to `config.rounds[leg-1].par-1`; round progression is its own dedicated `resolveDeadManWalkingRound()` (`frontend/index.html`), deliberately NOT an X01 `onLegWon()` carve-out the way Marathon Mode's practice-leg exception was, since two of the three ways a round ends (a bust, running out of darts) are never an X01 leg-win EVENT at all — there's no hook to carve into for them; derived result tiers (Pardoned down to Executed, never stored); stats replayed via `_replayDeadManWalkingLegs()` (Halve-It's own "nothing pre-aggregated" complication, since a round's termination reason isn't `SUM()`-able) — 6 stat bubbles, one higher-is-better Personal Best (Most Walked Out), a Home leaderboard, and a lifetime cross-run Longest Walked-Out Streak figure; 14 badges (a 4-tier lifetime-runs ladder, a 4-tier lifetime-Walked-Out ladder, a 3-tier lifetime cross-run streak ladder, plus 🕊️ Full Reprieve, ⚰️ Pardoned, 💀 Last Request — badge total 158→172); no `/display` renderer needed (single-device, same conclusion Checkout Trainer/Gauntlet reached), but its badge ids are still hand-copied into `display.html`'s `ACH_LABELS`/`ACH_DURATION`/`ACH_DESC` maps per the standing convention; **a real isolation bug found and fixed along the way**: `getPersonalBests()`'s `bestLegAvg`/`bestLeg`/`recentFormAvg`/`lifetimeAvg`/`fewestDartsCheckout` and `getPlayerStatBubbles()`'s `avgDartsPerLeg` all relied on `t.checkout=1` with no explicit `game_type='x01'` filter — true only as long as X01 (and Checkout Trainer, separately excluded) were the only game types ever setting `checkout=1`; Checkout Ladder broke that assumption when it shipped, and this drill's own real Walked Out checkouts made it concrete (a committed isolation-regression test caught X01's Personal Bests changing from `null` to real, wrong values); fixed by adding `X01_ONLY` to all three queries; the "Drill this number" cross-link into Checkout Trainer's existing pinned-target flow was explicitly left unbuilt — the source doc's own "Suggested build order" step 9 and its own "Open questions" both frame it as optional, v2-only work once the core loop is proven, so it does not get its own tracked row here; committed tests (`backend/test/scoring.test.js`, `backend/test/db.dead-man-walking-stats.test.js`, `backend/test/db.turn-consistency-guard.test.js`, `backend/test/display.ach-labels-parity.test.js`); verified end-to-end with Playwright against both the real-history-driven path and the cold-start `CHALLENGE_CHECKOUTS` fallback path (a Walked Out round, an Executed-by-bust round, an Executed-by-out-of-darts round, a full 15-round completion landing on the correct result tier and badges, save/resume mid-run); moved to `docs/archive/` | `docs/archive/dead-man-walking-roadmap.md` |
| The Pressure Chamber (core game — item 28; the self-declare honesty mechanic shipped separately as item 32, its own ledger row above) — new `game_type='pressure_chamber'` (`config.rounds: 15`, fixed and server-overridden regardless of client input, the same never-trust-the-client precedent Killer's number assignment already set), 1-4 players (`contexts: ['practice', 'h2h']`); the single load-bearing design decision, a round's Pressure Card (target + one of 8 situational modifiers) as a PURE function of `(gameId, roundIndex)` — `generatePressureCard()`, `frontend/scoring.js` — never stored, so H2H's "identical card sequence" falls out for free and no `target_sector`/`modifier_id` column exists anywhere; two grading paths, sector/ring targets via a new `gradePressureSectorRound()` ("best of the round's darts": full/partial/miss) and finish targets reusing `evaluateVisit()` completely unmodified (always double-out, a judgment call this build settled since the roadmap doc itself didn't pin it down); all 3 modifiers requiring real engine work built in full — Match Dart (only the round's 3rd dart counts, and a finish checkout must land specifically on dart 3), Sudden Death (a genuine per-dart early stop via `evaluateDartPressureSector()`, reusing the Doubles Practice per-dart-live-feedback pattern), and No Warmup (a real 5-second `Date.now()`-based wall-clock deadline, the Checkout Blitz precedent, enforced client-side only — the same limitation Checkout Blitz's own deadline already has — with a genuine miss dart auto-committed on timeout so the server's guard never needs to trust an unverifiable "this was late" claim); the Composure Points formula (base-by-difficulty × modifier-multiplier, half-value partials, a separate smaller miss-penalty doubled again under Double Down/Comeback, Comeback also adding a flat bonus on a hit) and the Composure Rating table (Ice 120+/Steel 90-119/Copper 60-89/Tin 30-59/Rattled below 30) both derived at read time, never stored; reuses Checkout Trainer's exact `bust`/`checkout`/`leg_won` three-way outcome verbatim (`turns.scored` = CP gained only, never negative; a run's total CP = `SUM(scored)` minus a derived total miss penalty, re-rolling `generatePressureCard()` for every missed round, the same "derive the rest, don't store it" philosophy Halve-It's halving rule already established — simpler here since the total isn't order-dependent); the SEC-25-style consistency guard in `addTurn()`, rejecting a round past 15 (no extra-rounds extension exists for this game type); a deterministic solo-vs-H2H tie-break chosen and documented as this build's own judgment call on the roadmap doc's last explicit open question (`pressureChamberDecideWinnerIndex()`: highest CP, then fewest misses, then fewest darts, then turn order — always a definite winner, never a new "draw" UI class); savable/resumable (`rebuildPressureChamberState()`, pure replay); 6 stat bubbles, 3-field Personal Bests (Best Run CP — a peak, no minimum floor, the Checkout Blitz precedent; Best Composure Rating, derived from the peak since the thresholds are monotonic; Longest Full-Hit Streak), a 2-board Home leaderboard set (Best Run CP with its Composure Rating, Most Pressure Chamber Wins); 15 badges — 3 data-driven ladders (lifetime runs completed, lifetime CP earned, and a per-run longest-full-hit-streak ladder reusing `CHUCKIN_MILESTONE_LADDERS`'s engine wholesale) plus 4 one-off flavor badges (🥶 Ice, 🎯 Nerves of Steel, ⏱️ No Warmup Needed, 🃏 Dead Calm — Steady Hands), the last three checked for the ROUND's own graded outcome and (a deliberate judgment call, since Composure Rating is a personal achievement even in H2H per the roadmap doc's own Goal section) 🥶 Ice and every lifetime ladder checked for EVERY player once their run settles, not just the leg winner — badge total 158→173; a `renderPadPressureChamber()` deliberately Pad-mode-only (never the Dartboard SVG, since Sudden Death/No Warmup both need `throwDart()` to stay the single live-input path), extending the BUG-24 `noZoneTracking` fix to this game type too; a `renderers.pressure_chamber` live-scoreboard card in `display.html` (a per-round ✅/➗/❌ chalkboard grid plus a large Pressure Card banner and a live No Warmup countdown, `display.html`'s own copy of the full 15-round card sequence sent since it has no shared `scoring.js` module); committed tests (`backend/test/scoring.test.js`, `backend/test/db.turn-consistency-guard.test.js`, `backend/test/db.pressure-chamber-stats.test.js`, `backend/test/display.ach-labels-parity.test.js` extended for the 4 new one-offs + 3 new ladders); verified end-to-end with Playwright (practice and H2H setup, a full 15-round solo run to a Composure Rating, Sudden Death's early stop, Match Dart grading, badges/stat bubbles/personal bests/leaderboards via the API, and the live `/display` scorecard); the self-declare honesty mechanic split out above shipped later (its own ledger row above), so this doc is now fully done and moved to `docs/archive/` | `docs/archive/pressure-chamber-roadmap.md` |
