# Open Roadmap Items — Central Tracker

> This is the **single centralized place to track every roadmap request across the
> project** — replacing `docs/archive/existing-app-prep-roadmap.md`'s old "Roadmap
> sequencing" table, which is now frozen/historical. See `CLAUDE.md`'s "Roadmap
> docs" section for the standing convention that keeps this file current.
>
> **No item on this tracker is ever "Partially Completed."** A roadmap doc that
> ships part of its design and defers the rest gets split into separate,
> independently-tracked items here — one line per shippable phase (a "v1"/"v2",
> a numbered build-order step, or any other genuinely separable piece of work) —
> each cleanly **Done** or **Not started**. When a roadmap doc's own status
> header would otherwise say "Partial," that's the signal to split it here
> instead of writing "Partial" as this tracker's status.
>
> A roadmap doc moves to `docs/archive/` only once **every** item split out from
> it below is Done — matching the existing archiving convention. A doc can have
> some items Done and others still open here without being archived; it archives
> once nothing about it remains on the open list.

---

## Open items (not yet started, ordered by complexity — lowest first)

> The rows for items **34–59** (the code-quality cluster from the 2026-07-20
> review/`/simplify` passes) are the one exception to complexity-ordering:
> they're sequenced by **recommended completion order** instead, since real
> dependencies exist between them. See "Code-quality cluster: recommended
> completion order" below the table for the phase-by-phase rationale.

| # | Item | Source doc | Complexity |
|---|---|---|---|
| 1 | Mobile: multiple saved server profiles (stretch goal, step 7) | `docs/mobile-app-roadmap.md` | Low |
| 2 | Mobile: "Scoreboard Mode" toggle (step 5) | `docs/mobile-app-roadmap.md` | Low |
| 3 | Mobile: native chrome — change-server access, haptics, biometric unlock (step 4) | `docs/mobile-app-roadmap.md` | Low-Medium |
| 4 | Mobile: distribution decision — App Store/Play Store listing vs. simpler sideload distribution (step 6) | `docs/mobile-app-roadmap.md` | Low-Medium |
| 5 | Localize voice announcements beyond hardcoded English phrases | `docs/voice-announcements-i18n-roadmap.md` | Low-Medium |
| 6 | **Frontend file split** — `frontend/index.html` is 18,623 lines of JS in one `<script>` block, 909 top-level declarations in one scope. Prep done and it changed the plan: **ES modules are the wrong target** (all 335 inline handlers break silently, and 87 mutable bindings — `game` alone referenced by 356 declarations — cannot be assigned across a module boundary). The recommendation is multiple classic `<script src>` files, which share one global scope and are semantically identical to today. Staged, leaf areas first | `docs/frontend-module-split-roadmap.md` | Medium |
| 7 | Mobile: Capacitor scaffold (iOS + Android) with the native Server Setup screen (step 2) | `docs/mobile-app-roadmap.md` | Medium |
| 8 | Mobile: ATS/cleartext config + self-signed cert trust-prompt (step 3) | `docs/mobile-app-roadmap.md` | Medium |
| 9 | Environmental logging (new inbound HA auth model; explicitly scoped as a niche, manually-enabled feature) | `docs/environmental-logging-roadmap.md` | Medium |
| 10 | **Maths Trainer** — a Minigames-category trainer for darts arithmetic (owner request). **For players who haven't learned the doubles/trebles of the higher numbers** — acquiring them, not polishing them — so the two question types are a progression of two skills, not two difficulties: know a segment's value cold ("what is treble 19?"), then total three darts *read off a board diagram* at a glance. Four-option multiple choice, a 60-second Sprint, own achievements. Two consequences of that purpose drive the design: **speed is the metric** (a correct answer that took four seconds means the player computed it rather than knew it, so an instant threshold splits *known* from *worked out* and the headline stat is "segments known cold"), and the counting mode needs a **board prompt** rather than only text, since reading the board is half of the second skill. Writes **no** `turns`/`darts` rows — its own `maths_trainer_rounds` table is what makes "counts toward nothing else" structural instead of fifteen more exclusion-constant call sites | `docs/minigames-roadmap.md` Part A | Medium |
| 11 | **Minigames: pick which further trainers to build** (decision, not a build). Four are proposed and specified — Bust or Safe, Countdown, Cricket Marks, The Leave — none approved. Deliberately one tracker row rather than four, so "on the roadmap" is not read as "agreed"; each becomes its own row once chosen. Part B also argues for extracting the shared four-option quiz engine during item 10 rather than after, since all five games are one interaction | `docs/minigames-roadmap.md` Part B | Low (a decision) |
| 12 | **Multiple live scoreboards at once** — one per active match, plus an optional spectator "master" board showing every match (owner request; the four-simultaneous-tournament-matches case). Concurrent *play* already works — `tournament_matches.game_id` is per match and `createGame()` has no single-active-game guard — so the whole limitation is that `liveState` is one object broadcast to every screen, i.e. two controllers produce one scoreboard flickering between two matches. Becomes a channel map keyed by `games.id`, plus a per-connection **claim** registry implementing the owner's selection rule: a screen auto-follows while no screen holds a claim, and once one does, every later screen is pinned manually — so the single-game household is never asked to choose a game it only has one of, on the first game of the night or the fifth. That rule is cross-screen state, so claims live on the server (per SSE connection, released by the existing close handler); a claim outliving its screen would reintroduce the exact friction the rule removes, and is the thing to test hardest. The scoreboard also carries an **always-available match switcher** — idle-hidden and revealed on any input, two deliberate taps to change, operable by arrow keys and Enter alone since these screens are often a TV with only a remote — so an auto-follow that guessed wrong is fixed on the screen rather than by editing a URL on something wall-mounted. Switching by hand upgrades the claim from implicit to explicit and writes `?game=` via `replaceState`; an auto-follow deliberately writes nothing, or the garage screen would pin itself to tonight's first game | `docs/multi-scoreboard-roadmap.md` | Medium |
| 14 | Online multiplayer (needs someone else running their own Oche instance too — a real adoption chicken-and-egg problem) | `docs/online-multiplayer-roadmap.md` | Very high |
| 15 | Camera/ML scoring (genuinely novel CV engineering; only useful to whoever mounts the hardware) | `docs/camera-scoring-roadmap.md` | Extremely high |
| 16 | Companion website (persistent hosted infrastructure — accounts, matchmaking, a cloud database — the one item requiring the project to operate long-term infra, not just be self-hostable) | `docs/companion-website-roadmap.md` | Extremely high |

### Build-order notes that still apply

- **Tournament mode (single-elimination), core league mode, league
  fixtures / pending matches, AND the New Game page revamp are all done**, and
  the first two are the real, shipped precedent for the "games link into a
  context table" pattern (see `CLAUDE.md`) any future context (online
  multiplayer, or anything not yet designed) can now follow directly — a
  separate `context_matches.game_id` junction table for a context with its
  own match-level lifecycle state (tournament's shape, and now league
  fixtures' too), or a direct nullable `games.<context>_id` column for a
  context with none (league's original shape). League fixtures added the
  `tournament_matches`-style junction-table shape *on top of* league mode's
  existing direct-column shape, for the "pending match" lifecycle state a
  plain nullable column can't represent — both shapes now coexist within one
  feature, which is fine: they answer different questions (which league did
  this game count toward, vs. is this specific pairing's match still owed).
- **Mobile app's steps are sequential as listed** (step 2 → 3 → 4 → 5 → 6 → 7) per `docs/mobile-app-roadmap.md`'s own suggested build order; its one prerequisite (the responsive CSS pass) is already done.
- **Row 5** (voice announcement i18n) is the one remaining order-independent Low-Medium item — it can be interleaved anywhere, including ahead of the bigger lifts.
- **Rows 10 and 11 (Minigames) are order-dependent in one specific way**: item 11 is a
  decision, and it wants making *before* item 10 is built, not after. All five proposed
  minigames are the same interaction (generate a question, offer four options, grade the
  tap, keep a streak, optionally race a clock), so if any of Part B is going to be built,
  item 10 should extract that loop as a shared engine as it goes rather than shipping a
  bespoke mode that the second one then has to either duplicate or refactor. If the answer
  to item 11 is "none of them," item 10 is cheaper built bespoke — which is exactly why
  the decision is worth having first. Item 11 can otherwise be interleaved anywhere; it is
  a conversation, not a build.

### Code-quality cluster (items 34–59): recommended completion order

Reordered from the original by-complexity listing because several items have
real dependencies on each other — doing them out of order means either
blocking or reworking something already done. Seven phases, each best
finished before starting the next; nothing within a phase depends on order:

1. **Quick, independent wins** (59, 58, 56, 52, 38, 39) — **done**, see the
   completion ledger below. No shared dependencies, touched isolated code,
   and shrank the backlog count fast. 59 (badge predicates → scoring.js) was
   placed first because it makes the predicates testable before phase 3
   touches badge-award flow (50) and the leg/set/game cascade (54) that
   triggers them.
2. **Id-keyed killer configs** (43) — **done**, see the completion ledger
   below. A self-contained backend migration converting `config.numbers`
   from name-keyed to id-keyed; two of the three original name-rewrite
   compensators (the rename rewrite, the boot reconciler) are fully gone,
   the third (merge's rewrite) and the import re-key survive in simplified
   form (see the item's own note for why). Doing it early means every later
   phase that touches Killer works against the simpler, post-migration code.
3. **`GAME_TYPES` registry consolidation, batched** (41, 46, 53, 54, 35, 47,
   55, 48, 36, 49, 50) — **done**, see the completion ledger below. All
   repeatedly touched the same registry object and the same per-mode
   dispatch functions, so batching avoided re-opening them phase after
   phase. Internally ordered by the game's own lifecycle: setup (41
   category, 46 option sections) → construction (53) → play (54 leg/set/
   game cascade, 35 undo — same turn-lifecycle functions as 54) → end-of-unit
   stats (47, tightly coupled to 54's `finishUnit` call) → rendering (55) →
   profile/Home (48, then 36+49 as one unit since 49 supersedes 36, then 50).
4. **Live-state keys** (42) — **done**, see the completion ledger below. A
   full-stack producer/allowlist/consumer change; sequenced after phase 3 so
   it could reuse the now-mature registry pattern instead of inventing its
   own.
5. **Savable/resume infrastructure** (40, 37) — **done**, see the completion
   ledger below. 40 folded into 37 as planned; the biggest, riskiest item in
   the cluster, verified live end-to-end for all 12 savable game types.
6. **Efficiency passes** (57, 51, 45) — **done**, see the completion ledger
   below. Done after the structural work they build on: 57's "pad
   build-once" targeted the pad rendering phase 3 restructured, 51's
   badge-progress work followed 50's award-flow consolidation, and 45's Home
   lazy-fetch refactor targeted the consolidated renderers from 48/49.
7. **Architecture decision, resolve together** (34, 44) — 44 explicitly
   overlaps 34's pre-existing, still-undecided question (generalize the
   per-game-type backend stat functions?). Placed last because it's a real
   fork requiring a decision, not just an implementation — resolve 34 first,
   then 44 follows from whichever direction that decision takes.

---


## Abandoned (owner's decision — kept so the reasoning isn't relitigated)

| **Item 34** — generalize the per-game-type backend stat functions | **Decided: keep the per-type shape** (2026-07). The item was never a missing feature — every game type's stats are fully viewable — only an open question about whether a single parameterized query keyed by per-type formula definitions would beat hand-writing a bespoke SQL function set per type. It stayed open for ~10 consecutive game types, and each of those was a chance to choose the general version. The repetition IS the answer: the per-type functions are individually simple and independently debuggable, the formulas genuinely differ (Cricket marks, Baseball innings, Shanghai rounds, Killer lives and the drills' own metrics have almost nothing in common beyond the word "stat"), and a parameterized layer would have to encode every one of those differences as configuration — trading ~10 readable functions for one hard one. Closed as a decision rather than left open indefinitely, so it stops competing for attention. `docs/game-modes-roadmap.md` can now be archived once its own remaining items are done | `docs/game-modes-roadmap.md` |

These were considered and deliberately dropped. The design docs stay in
`docs/archive/` for their rationale; nothing about them is outstanding.

| Item | Why | Source doc |
|---|---|---|
| **Item 6** — the player-page reorganization plan | The *audit* it was blocking on was completed (2026-07) and its findings are recorded in the doc: the page is a fixed ~740px column so a tablet renders the phone layout with 37% dead space; four stacked scoping controls in four widget idioms; the same statistics shown twice at different scopes; nine identical collapsed accordions with no priority order. The three findings that were **accessibility** rather than layout were split out and shipped. Writing the redesign plan itself was dropped: a plan with no intent to build it is inventory, and the audit already captured the value | `docs/archive/ui-overhaul-roadmap.md` |
| **Item 63** — split `games.config` into per-game-type tables | Its own doc recommended against it. `config` has exactly one writer, is never independently edited and has never caused a data-integrity bug; the change is a ~40-call-site rewrite of every `json_extract(g.config, …)` read for purely theoretical 1NF purity | `docs/archive/database-normalization-roadmap.md` §3.2 |
| **Item 76** — dedupe `games.category` against `config.startingScore` | Evidence from shipping its sibling half (item 62): `checkout_points` was read as a VALUE and its replacement is a plain column reference — same cost, clearer. `category` is read as a FILTER and a GROUP BY key in ~27 places, and its replacement is a `json_extract` expression SQLite cannot index, *in exactly the queries that scan on it*. It would have made item 44 measurably worse for a purity gain the code never feels | `docs/archive/database-normalization-roadmap.md` §3.1 |

## Done (completion ledger — kept for history, not re-verified here)

Every item below is fully shipped. This section exists so nothing's completion
history is lost when a doc still has open items elsewhere on this tracker (and
therefore hasn't been archived yet) — see each source doc for full detail.

| Item | Source doc |
|---|---|
| New Game Step 1: no default game, and settings only where the game is (2026-07, live bug report with a screenshot — `docs/bug-roadmap.md` BUG-59). Two causes, one screen. **The default selection**: `currentSetupOptionKey()` derived the ledger's selection from `setup.mode`/`setup.gameType`, neither of which can ever be empty (`startGame()` reads them directly), so there was no value meaning "not chosen yet" and X01 was always selected — with `setupOpenCategoryKey()` hard-coding `'traditional'` as its fallback, so that category was always expanded too. A new `setup.gameChosen` supplies the missing third state, raised in `setMode()` (so every selection path and preset entry point gets it without a new flag to forget) and lowered by `show('setup')`. **The stranded panel**: a selected row renders its own options and Continue *inside itself*, so collapsing that row's category took the only way forward off screen — and the earlier fix for that was a second copy of the whole panel rendered below the entire ledger, which is what the reporter photographed: a bare STARTING SCORE / FORMAT / Continue block floating under five collapsed headers, naming no game. Deleted; collapsing now hides the panel and re-expanding restores it with the selection intact. `setupLedgerRowKey()` resolves `'league_game'` (a Step 2 opt-in with no row of its own) to the underlying x01/cricket row, which is the dead end the fallback had been quietly covering. Worth recording: the browser suite's `new-game` check was **asserting the reported behaviour** ("Continue survives the collapse"), so the fix meant rewriting that check rather than extending it — 9 assertions to 43, now including a sweep of every category. Also hoisted the accordion reset above `show('setup')`'s preset-entry branch, without which a stale `'none'` would leave `drillCheckout()` with no row rendered and nothing to focus. `backend/test/frontend.new-game-no-default-selection.test.js` (11 cases), `REFERENCE.md` §20, README | `docs/bug-roadmap.md` |
| **`practiceUnit` — the practice legs/sets rule, as a registry member** (2026-07). Five modes each stated it separately inside `startGame()`: Baseball, Shanghai, Halve-It and The Pressure Chamber forced 1 leg / 1 set when `setup.mode !== 'h2h'`, and Dead Man Walking forced 15/1 for its own reason. The four practice flags were near-identical and said so out loud — *"Same BUG-22 reasoning as isPracticeBaseball above"*, then *"Same BUG-22 reasoning again"*, twice more. A sixth mode of that shape needed a sixth copy, and forgetting it is **silent**: a solo drill quietly structured as a best-of-3, recorded that way, nothing visibly wrong on screen. Now one `practiceUnit` member per type, plus a short `SINGLE_UNIT_MODES` list for Daily Challenge and Ghost — which deliberately stay mode-level, because both are plain X01 underneath and an ordinary practice X01 must keep the legs the player chose. **The two axes were fused before and are now separate.** Verified by EQUIVALENCE rather than hand-written expectations, per the item's own precondition that match structure feeds the statistics and so is not a place to assert what you believe the old code did: `backend/test/frontend.match-units.test.js` keeps the original five-flag expression verbatim as a reference implementation and compares it against the registry across every reachable combination of type, mode and chosen legs/sets. That sweep immediately earned its keep — it found a genuine divergence on `(mode:'practice', gameType:<a drill>)`, which turned out to be an **unreachable** pair (`selectSetupGame()` resets a drill gameType the moment a non-drill mode is chosen), so the test now models reachability and separately asserts the guard that makes it true, rather than silently covering less than it claims. Confirmed to catch three realistic mistakes: a dropped `practiceUnit` (the 'forgot the fifth copy' bug), a lost h2h carve-out, and Dead Man Walking's 15 typo'd to 1 | *(no separate roadmap doc — from the 2026-07 band-aid review)* |
| **Band-aid review, and `restoreSetup` as a registry member** (2026-07, owner request — "look for band aids applied to minor issues… so small things don't compound"). Four found and pulled, each covering something real. (1) **32 schema migrations shared one blanket `catch(e) {}`.** SQLite has no `ADD COLUMN IF NOT EXISTS`, so a re-run throws `duplicate column name` — and the catch handling that was given authority over every other error too. A migration naming a table that does not exist throws `no such table` and was swallowed identically: the column silently never added, nothing at startup, nothing in the log, first symptom a query reading `undefined` months later. Now `addColumn()`/`dropColumn()`, swallowing exactly the one "already applied" error and rethrowing the rest with the statement attached. **Narrowing it immediately exposed a defect the blanket catch had hidden since it was written** — the block contains one SUBTRACTIVE migration whose already-applied error is a different string, and it failed on a fresh database the moment the catch stopped covering for it; hence two named helpers rather than one. (2) **`display.html` ignored the project's own error convention.** `index.html` states it — "use `.catch(logErr)`, never a bare `.catch(()=>{})`, which is the same decision with the evidence thrown away" — and obeys it 54/54; `display.html` had five bare ones because it predates the rule. That matters more on that screen, not less: it is on a wall with nobody watching, so a settings prefetch failing to a default leaves no trace anywhere else. The one genuinely-expected rejection (audio autoplay) stays bare and now says why. (3) **`drillGameTypes` duplicated the registry** — a hand-written list of eight types identical to the registry's own `soloOnly` set, whose drift had already cost a real bug (Around the Clock gained its race variant, the copy kept calling it solo, and `setMode('practice')` silently started an X01 game from its ledger row). Now derived. (4) **`playAgain()`'s six-branch chain → `GAME_TYPES.<type>.restoreSetup(config, finished)`**, the inverse of `buildConfig`. `buildConfig` is required of all 16 types and throws when absent; its opposite direction was a hand-chain nothing enforced, so **Doubles Practice and Checkout Trainer had both been missed** — the first losing its chosen double targets on Play Again, the second all five of its choices, neither producing any symptom beyond the wrong game starting. `backend/test/frontend.play-again-roundtrip.test.js` (12 cases) drives setup → config → setup for every type with deliberately non-default fixtures, and was confirmed to fail on exactly those two modes when they were reverted. Worth recording: fixing (3) shipped a `ReferenceError` from a missed second reference that neither `check.js` (it does not parse JavaScript) nor the type checker (index.html's inline script is uncovered) could see — the browser suite caught it, and the assertion-count guard reported "ran 8, expected 9". `REFERENCE.md` §1 registry contract, §"Database schema" migrations | *(no separate roadmap doc — owner request)* |
| **CI widened from one verification layer to four** (2026-07). CI ran `npm test` alone, which is one of three layers and **not the one that catches the worst failures**: the frontend split earlier this month broke every screen in the app and all 1,683 backend tests stayed green throughout, because the failure was inline handlers silently resolving to `undefined` — nothing running in Node can see that. `.github/workflows/test.yml` is now four **independent** jobs (`static` = `node --check` over `backend/`, `frontend/` and `frontend/js/` plus `backend/check.js`; `tests`; `browser` = the 457-assertion `verify-ui` suite against a real Chromium; `secrets`). Independent rather than sequential steps so one push reports everything that is wrong instead of only the first thing, and so a seconds-long failure reports in seconds. Playwright is installed **globally on the runner and pinned**, never into the repo — the zero-dependency promise is about what *running* Oche needs, and this is tooling that never ships; `lib.js` now uses the image's bundled Chromium when that path exists and falls back to Playwright's own otherwise, which is the one change that lets the identical script run locally and in CI. Failure screenshots upload as build artifacts. Coverage is printed under `if: always()` and **never enforced** — a threshold turns "add a test" into "the build is red", which trains people to delete assertions. `REFERENCE.md` §38 | `docs/testing-and-observability-roadmap.md` |
| **Secret scanning** — `backend/scan-secrets.js` (`npm run scan`), a CI job, and a pre-commit hook (2026-07). Separate from `backend/check.js` on purpose: a static-check finding is fixed in the next commit, but git history is permanent, so a leaked credential cannot be — deleting the file changes nothing. Tuned to what this repo actually risks, which is **not** a pasted token (Oche has no API keys of its own) but **the database or a backup of it being committed**, since that holds admin password hashes, player PIN hashes and the Home Assistant webhook config; `.gitignore` covers `data/` and `*.db`, so the scanner is the backstop for `git add -f` and for a debugging copy saved as `darts.db.bak` — detected by the SQLite file **magic bytes**, so an unusual extension changes nothing. **Two obvious designs for the generic-credential rule are wrong and both are recorded, because the first one shipped.** Entropy alone cannot work (`game-type-around-the-clock-1` scores 3.94 bits/char; an MD5-shaped key scores 3.39 — the ranges overlap completely), and the "entropy ≥ 4.0" version written first **scanned clean while being incapable of catching a hex key at all**, since 4.0 sits on hex's theoretical ceiling. Its own test suite caught that, which is the argument for the suite's shape: every rule is fed a real-shaped example rather than only confirming the repo is clean. The rule now gates on **structure** — a ≥20-character unbroken alphanumeric run — with entropy and character-class diversity kept only to reject degenerate long runs. The pre-commit hook runs this and nothing else, deliberately: one that also ran the tests would be slow enough to get switched off, and a hook people switch off protects nothing. Not a substitute for GitHub's own push protection, which is free, wider and self-maintaining; the two are complementary. `backend/test/scan-secrets.test.js` (13 cases), `REFERENCE.md` §37 | *(no separate roadmap doc — from the 2026-07 tooling audit)* |
| **Item 75** — the four solo-run modes replay their dart counters on resume (2026-07). Bob's 27, 121 Checkout Ladder, the Gauntlet and Dead Man Walking came back from a save with `legDarts`/`setDarts`/`gameDarts` at 0 (and Bob's 27 with a blank round card), because their rebuilds replayed the position and nothing else. Persisted stats were never affected; the in-session display and the completion panel's dart figure were. **`legDarts` means something different in each of the four, and where the reset lives is not where you would look**: Checkout Ladder resets in the ordinary `resetPlayerForNextLeg` hook (so it is the current attempt's darts), the Gauntlet genuinely never resets, Bob's 27 is one continuous run — and Dead Man Walking never reaches `startNextLeg()` at all, zeroing it inside `resolveDeadManWalkingRound()`, so its figure is the CURRENT ROUND's darts. The first cut of the fix got that last one wrong by reading "no-op `resetForNextLeg`" as "no per-leg counter", and **the `resume-fidelity` check rejected it** — which is precisely what that check was built to do: it asserted the gap rather than excluding it, then told whoever fixed each mode to move it into the main sweep. All four are now in it, so all 12 savable types go through one assertion and the known-gap branch is gone. `backend/test/scoring.solo-run-dart-counters.test.js` (10 cases) | `docs/code-quality-roadmap.md` |
| **Item 44** — the repeated whole-darts-table scan, done once per request (2026-07). Twelve query sites each inlined `(SELECT turn_id, COUNT(*), SUM(is_treble) FROM darts GROUP BY turn_id)`, a full scan of the darts table apiece that SQLite cannot share between statements — five inside `computeStats()` alone, more in `getSessionRecap()`. **Measured before deciding anything**, since the item was filed as a suspicion: at 2,000 games the roster page cost **1,215ms** and Tonight's recap **1,028ms**, growing linearly at ~0.6ms per game, on a single-threaded server, on the two surfaces most likely to be open. A covering index was tried first and **rejected on evidence** — `darts(turn_id, is_treble)` moved the aggregate only 127ms → 99ms, because the cost is the scan itself. What shipped is one TEMP table (`_dart_agg`) built once per call by a reentrant `withDartAgg()` wrapper around the seven entry points that need it; profiling then showed the biggest remaining cost was the three nine-darter queries joining raw darts at ~140ms each, and moving those onto the aggregate simplified them too (the 1:1 join means `COUNT(*)` counts turns, which is why the original needed `COUNT(DISTINCT t.id)`). Result: roster **1,215 → 727ms**, recap **1,028 → 608ms**, and at a realistic 250 games 134 → 82ms. Deliberately NOT a stored column — that would need maintaining on every insert and could drift, the exact trade item 62 spent a wave undoing; this is a per-request cache of a pure aggregate, not a second source of truth. **Verified output-identical**: a harness capturing every figure from `computeStats`/`getSummary`/`getSessionRecap`/per-player bubbles/personal bests/finishes/coaching insights across a 2,000-game database compared byte-for-byte before and after, at each step. `backend/test/db.dart-agg.test.js` (5 cases) pins the cache's LIFETIME — staleness, missing table, and reentrancy — since none of those fail loudly, they just return quietly wrong statistics | `docs/code-quality-roadmap.md` |
| **Item 72** — Checkout Trainer's "Route Recall" sub-mode, built (2026-07). One target held across many submissions while the player names as many DIFFERENT ways to check it out as they can — a breadth drill rather than Freeform's best-answer one. **Two things the design had wrong, both found by building it.** (1) Its proposed enumeration alphabet was `CO_FIRSTS`, which has 42 segments and no doubles — right for a hint, and it would have silently lost real routes like 110 = D20 T20 D15. (2) It assumed straight-out was the explosion risk and double-out sidestepped it: at three darts double-out, target 58 has **730** routes and **81 of 162** finishable targets have 200+, so "find them all" is not a task there. The owner's resolution turned that into the mode's shape rather than a limitation — the 1- and 2-dart ceilings ship the drill as designed (revealed total, completion moment, coverage against a real denominator, worst case 36 routes), and the 3-dart ceiling ships as an open-ended "how many can you name" with no total and no finish line. Three outcomes per submission, and the middle one is the point: a **duplicate records nothing at all** — no turn, no counter, no undo entry — because re-entering a route by accident must cost nothing, which also makes "routes found" simply the count of `checkout=1` turns rather than something de-duplicated at read time. Grouping resolved with **no new column** (`turns.set_no` is the hunt), exactly the "weigh it against an existing column first" the doc asked for. The load-bearing invisible part is **isolation**: `checkout=1` means different things in this sub-mode and in Freeform/Blitz, so every Freeform statistic is scoped `NOT_ROUTE_RECALL` — written `IS NOT` rather than `!=`, since `config.mode` is absent on older rows and `!= NULL` is NULL, which would have erased every pre-existing Checkout Trainer stat instead. One real defect found by its own tests: the grader reported "goes past zero" for a route landing exactly on zero off a single, which is both false and the wrong lesson; the rejection reasons now separate `overshoots` / `short` / `bad-finish`. `backend/test/scoring.route-recall-grading.test.js` (14, including a sweep proving every route the enumerator lists is accepted — otherwise a hunt could show a total it is impossible to reach), `backend/test/db.route-recall-stats.test.js` (8), verify-ui `route-recall` (16). Doc archived; every item in it is Done | `docs/archive/checkout-trainer-route-recall-roadmap.md` |
| **Player Profile accessibility pass** — a heading outline, scope announcements and distinct button names (2026-07). Split out of `docs/archive/ui-overhaul-roadmap.md`'s design-phase audit and shipped without waiting for the redesign, because a page having no headings is broken regardless of how it is eventually laid out. Three defects on the app's densest screen (~3 viewports of scroll on a phone *and* a tablet, ~1,100 DOM nodes, 34 buttons): **all 21 section titles were styled `<div>`s**, so jump-by-heading — the navigation that makes a page that long usable — did not exist at all; **the three scope controls announced nothing** when they replaced the entire contents of the page, so with a screen reader three screens of statistics silently became different statistics; and **ten 🎯 Drill buttons shared one accessible name**. Now an `<h2>` name plus `<h3>` sections, with the collapsible ones keeping their `<summary>` (that element is the disclosure *button* — losing it would trade one affordance for another) and carrying a nested `<h3>` so the section is reachable both ways; the scope controls call the app's existing `#sr-announcer`, which needed no new region, it simply was not being used here; and each drill button names its own score. **Verified semantics-only**: the page's `scrollHeight` is identical to the pixel before and after, on both viewports. New verify-ui check `profile-a11y` (10 assertions) asserts the outline *structurally* — an H2, no skipped levels, every summary carrying a heading — rather than as a list of expected titles, so adding a section to the profile does not mean editing the check; all three fixes confirmed to fail it when reverted | `docs/accessibility-roadmap.md` |
| **`allCheckoutRoutes()` + `routeKey()`** — the complete-route enumerator Route Recall is built on (2026-07, item 72 build-order step 1). Every checkout function in the app finds ONE route and stops; this is the first thing that needs the whole set, to answer "have you already found this one?" and "are there any left?". **The roadmap's proposed alphabet was wrong and would have shipped a silently incomplete answer**: it said to reuse `CO_FIRSTS`, which holds 42 segments — every treble, both bulls, every single — and **no doubles**. That is right for a hint (nobody aims a double as a setup dart) and loses real routes here; 110 = D20 T20 D15 is a finish a player can absolutely name, and a `CO_FIRSTS` search never sees it. The alphabet is the full 62-segment board. Routes canonicalize as an unordered multiset of setup darts plus a designated final dart, which is the only ordering that carries meaning — T20 then T19 is not a different route from T19 then T20, but for 60 "D20 then D10" and "D10 then D20" genuinely are, and both fall out of the same rule. Proven against an **independent brute-force oracle** (a triple loop over every ordered sequence, canonicalized separately) across all 180 targets × both out-modes × all three ceilings — 1,080 set comparisons — plus legality, no-duplicates, ceiling-monotonicity, agreement with `checkoutHint()`'s dart count on every finishable target, and the bogey numbers. One thing the enumerator deliberately does NOT inherit is `checkoutHint()`'s `rem > 170` cutoff: that is an X01 double-out convention, and 171 = T20 T20 T17 is a real straight-out finish. `backend/test/scoring.all-checkout-routes.test.js` (12 cases) | `docs/archive/checkout-trainer-route-recall-roadmap.md` |
| **Item 62 (checkout_points half)** — `turns.checkout_points` dropped and derived (2026-07). The column duplicated `turns.scored` on every turn that had one and is now computed at read time across ~30 query sites. **The roadmap's own stated derivation was wrong on two counts, and implementing it is what found that.** (1) It said the column equalled "the checkout dart's `scored` value" and proposed an `ORDER BY dart_no DESC LIMIT 1` subquery — that is the FINISHING DART; a 170 checkout is T20+T20+Bull and the subquery would have returned 50. The column duplicates the visit total, `t.scored`, which makes the replacement *cheaper* than the plan feared rather than more expensive. (2) `checkout` is an **overloaded flag**: for X01, 121 Checkout Ladder and Dead Man Walking it means "checked out, for this many points", but The Pressure Chamber and Checkout Trainer reuse it for "this visit was a legal attempt, not a miss", where `scored` is a CP gain or a flat 0. Those two wrote nothing into the column, which is how that distinction was encoded — so a naive `CASE WHEN checkout=1 THEN scored END` turns a 150-CP Pressure Chamber round into a 150 checkout. The shipped expression takes its game-type list from `GAME_TYPE_REGISTRY` (`checkoutIsAttempt: true` on the two entries that overload the flag), so a future mode marks itself. Stated honestly: every current read site is *also* `X01_ONLY` (BUG-27 put it there), so the marking changes no shipped number today — it is asserted anyway, and asserted against the expression **directly** rather than through a stats function, because every stats function's own X01 filter would mask a wrong derivation. `backend/test/db.checkout-points-derivation.test.js` (9 assertions), confirmed to fail when the marking is dropped. The migration is a `DROP COLUMN` with **no backfill and nothing to roll back** — every value it held is still in the row it was copied from — wrapped in the same idempotent `try/catch` as db.js's additive migrations. Exports: the turns CSV is byte-identical (the column is still emitted, just computed); old JSON exports stay importable; the games CSV's `highest_checkout` is now blank rather than `0` for the two attempt-modes | `docs/archive/database-normalization-roadmap.md` §3.1 |
| **Item 67** — build-once DOM for the Cricket/single-target/Pressure-Chamber pads (2026-07). All three opened with `pad.innerHTML = ''` and rebuilt every button, with a fresh `onclick` closure each, on every dart — The Pressure Chamber discarding and recreating twenty-two of them three times a visit. All three now carry `pad.dataset.padKey` (the default pad's item-57b guard, renamed from `padKind`/`hasMiss` so there is one convention rather than two). The deferral was right that these aren't a copy of 57b, and the answer is a split rather than a cleverer guard: the single-target and Pressure Chamber pads have **no** per-dart content, so their keys (the round's target; which of the two shapes is built) mean a rebuild happens on a round change and not otherwise; Cricket's marks glyph, `closed` class and per-button `aria-label` genuinely do move per visit, so those are written onto the existing buttons instead of being a reason to tear the pad down. Each renderer now ends with one `disabled` sweep over `pad.querySelectorAll('button')`, which matters once the creation sites stop running every frame. New verify-ui check `pad-reuse` (19 assertions) asserts **both halves, because they pull against each other**: the buttons must be the same NODES after a dart (stamped before, looked for after — a rebuild is invisible to any check that only reads the rendered result, which is why this survived so long), and Cricket's marks must still MOVE (the naive way to stop rebuilding is to stop rendering, which freezes the pad while the real marks move underneath). Confirmed on both: restoring the unconditional rebuild fails 4 assertions, freezing the glyph fails 1 | `docs/code-quality-roadmap.md` |
| **Item 65** — the `rebuild*State()` replay loop, unified (2026-07). Baseball, Shanghai, Halve-It and Pressure Chamber's rebuilds were character-for-character identical apart from five hooks, and Cricket's differed only in how it names a leg win and its practice set-gate: ~15 lines of leg/set bookkeeping written out five times over save/resume's core correctness engine, which is exactly where item 37's starter-rotation desync once hid. All five now run on one `_replayVisits()` (−182/+146 lines). X01 deliberately stays out — bust/checkout handling, per-player starting scores and handicaps have nothing in common with "a fixed sequence of rounds," and folding it in would turn the hooks into a configuration language. **The deferral's "full-matrix live save/resume verification" was done two ways**, because the pure functions and the path around them fail differently: (1) direct equivalence against the pre-refactor implementations over 250 randomised turn streams per mode — **1750/1750 comparisons identical**, with the harness itself validated by perturbing one starter rotation in the reference and watching it fail 111 of 250 seeds; (2) a new verify-ui `resume-fidelity` check driving all 12 savable types through the real save endpoint and the real `resumeGame()`. **One real bug found and fixed**: Halve-It resumed with its per-round halving marks blank while the totals beside them were correct — the card said a round scored 30 without saying it had been halved to get there. It needed both halves (the rebuild never replayed `roundHalved`; the resume member's hand-listed copy omitted it) and had been recorded in the code as a known cosmetic gap. That is the ONE intended divergence from the reference, and it was confirmed to be exactly that: across all 250 Halve-It seeds `roundHalved` is the only differing field, and no top-level field differs at all. A separate pre-existing gap in four other rebuilds was split out as item 75 rather than half-fixed | `docs/code-quality-roadmap.md` |
| **Item 66** — the five remaining `undoLastTurn*` field restores, generalized (2026-07). The item proposed a generic `Object.assign(p, snap)`; that would have been wrong, because a snapshot was one FLAT object mixing the player's fields, the game's and the undo bookkeeping — so `Object.assign(p, snap)` copies `baseballInning` onto a *player*. Namespacing them (`snap.player` / `snap.game`) is what makes a generic restore possible at all, and the field list is now stated once at capture, beside the mutations it protects, with `restoreVisitSnapshot()` reading the snapshot's own shape. The drift this removes had already produced a real defect — Halve-It's `roundHalved` was captured by neither side, so an undone visit painted a halving that no longer happened. Verified by the verify-ui `turn-loop` check, deepened from one visit to three so the snapshot STACK is exercised rather than just its top entry, and confirmed to fail when a single key is dropped from the one remaining list. Checked and found NOT true: the shallow copy on capture is defensive rather than load-bearing — all five modes replace their object fields rather than mutating in place, so an identity copy still passes. It stays because "the snapshot is a copy" is a much easier property to keep true than "no commit path anywhere ever mutates a snapshotted object in place" | `docs/code-quality-roadmap.md` |
| **Item 69** — `startNextLeg()`'s parallel reset chain → a `resetLegState` registry member (2026-07). A second hardcoded per-mode if-chain sat one line after the registry call that already owned leg resets, and had grown to **six** modes — the sixth arriving days earlier with Around the Clock's race variant, which is the clearest evidence available that a mechanism sitting beside the registry gets extended instead of the registry. **Not** folded into `resetForNextLeg` as the deferral suggested: that member is per-player and runs once per seat, while this state is per-*game* (`baseballInning`, `pressureChamberDeadline`, a timer teardown) — folding it in would have run those N times a leg, including `stopPressureChamberNoWarmupTimer()`. It is a sibling member called exactly once. All sixteen types declare one; the ten with nothing to reset point at `resetLegStateNone`, the named-no-op pattern `enterTurnPerDart` established. The deferral's "full per-type multi-leg live check" is the new verify-ui `leg-reset` check (12 assertions): fingerprint `game`'s own state at leg 1, dirty it with a real visit, cross a leg boundary, require the fingerprint back — generic rather than a per-mode field list, so a mode that adds a counter and forgets to reset it is caught without anyone extending the check. Confirmed failing on a deliberately dropped `halveItRound` reset. It sweeps the nine types that actually reach `startNextLeg()`, and that hand-written list cannot rot: the check requires every type declaring a real `resetLegState` to appear in it | `docs/code-quality-roadmap.md` |
| **Item 70** — `startGame()`'s 14-branch config ternary → a `buildConfig` registry member (2026-07). Now one line, `GAME_TYPES[gameType].buildConfig(setup, startScore)`. Declared as inline arrows per entry the way `category` already reads, so each type's reasoning — why Baseball's innings are fixed at 9, why the Pressure Chamber sends a value the server overrides anyway, why a Checkout Trainer pin forces Freeform — stays attached to the branch it explains instead of being orphaned in `startGame()`. X01 declares its own `{startingScore}` and there is no fallthrough: the old ternary's final `:` silently handed any unlisted type X01's shape, which is nonsense config for a mode with no score. Cricket re-reads `resolveCricketNumbers()` rather than having it threaded in, because the "exactly 7 targets" **validation** has to stay in `startGame()` where it can abort the start. The deferral asked for a per-type live comparison of the persisted config before and after, since a wrong transcription has no compile-time signal — done exactly that: a driver started **all 16 types** against the pre- and post-refactor sources and captured `config`/`category`/`start`, and the two are byte-identical (Dead Man Walking's server-generated `rounds` excluded, as those differ per fresh player by design; its one client field matches). Both members joined item 64's four in `backend/test/frontend.turn-loop-dispatch.test.js` | `docs/code-quality-roadmap.md` |
| **Item 73** — `getMetricHistory()` ↔ stat-bubble parity, swept and pinned (2026-07). The Player Profile shows a number and, directly beneath it, a chart of the same number over time, computed by two entirely separate SQL queries. Nothing enforced that they agree, and the pairing had already produced two real bugs: `avgdartsperleg` missing `X01_ONLY` (Checkout Ladder and Dead Man Walking legs, which also set `checkout=1`, leaked into the chart only), and BUG-31's `pace` using `NOT_HYPOTHETICAL_DARTS` against the bubble's `NOT_CONTINUOUS_STREAM` (41 darts/min against 3, on one screen). Both were found by a person noticing two numbers disagree; `backend/test/db.metric-history-parity.test.js` replaces that. **The pair list is derived from the app's own key maps** (`STAT_DEFS` + `BUBBLE_KEY_MAP` and the six per-mode equivalents, read out of `index.html`), not restated in the test — a metric added there arrives with a parity assertion already pointing at it, and a third copy of the list would be the one that goes stale. Covers all seven charted families (X01, Cricket, Baseball, Doubles Practice, Chuckin, Around the Clock, Around the World) on both the Practice and H2H tabs. Two things the fixture does deliberately: it carries **timestamped guided Around the World darts** and a **Checkout Ladder leg that checks out**, without which neither historical bug is observable at all — verified by reverting each fix in turn and watching the suite go red (chart 14.4 vs bubble 3.0; chart 7.67 vs bubble 8.5). `dartsThrown`/`avgDartsPerDay` are the one documented non-equality (lifetime bubble, mode-scoped chart) and are pinned as a sum instead. Sweep result: no metric currently disagrees, and no chart arm exists without a `STAT_DEFS` key | `docs/bug-roadmap.md` (BUG-31 fix step 4) |
| **Item 60** — `DB.saveGame()` now awaits the write queue (2026-07). Resume is replay, not snapshot: the server stores only "this game is paused" and rebuilds the whole position from recorded turns. `recordTurn()` serializes through `DB._chain`; `saveGame()` bypassed it entirely, so a save could resolve while turn writes were still in flight and a player who pressed Pause then Resume got back a game the server had only partly heard about. **Reproduced deterministically** — three visits thrown, one recorded, a 501 leg returning on 321 instead of 141 — not just at zero delay in a script but reachable by a double-tap or a connection reordering two requests. `saveGame()` now chains onto `_chain` rather than joining it: the caller needs this promise to BE the answer (a save must be known to have succeeded before navigating away), which `_queue`'s fire-and-forget shape can't give it, and `_chain` never rejects (every queued task carries its own `.catch`) so one failed turn write can never leave a player unable to save. New verify-ui check `save-resume` (6 assertions) drives the real buttons' code path with zero delay; confirmed it fails 4/6 with the fix reverted. It lives in verify-ui rather than `backend/test` because the defect was entirely in the browser's request ordering — the backend was correct throughout, and no coverage over `db.js` could have seen it | `docs/code-quality-roadmap.md` |
| **Item 74** — pairwise head-to-head records scoped to duels (2026-07, owner's decision). `getH2HRecord()`/`getH2HSummary()` counted **any** non-practice game containing both named players, so a 3-4 player game the winner took also landed in their record against each loser. Now `g.player_count = 2` (the shared `H2H_PAIR_ONLY` clause): a pairwise record answers "how have these two done against each other", and a game a third person also played is not a result between them. This is the rule `getSessionRecap()` had always applied to Tonight's Recap's own pairwise grid ("A single 3+ player game has no single 'matchup' pair"), so the change makes the two pairwise surfaces agree rather than introducing a new opinion — and the aggregate win-rate leaderboards stay deliberately count-agnostic, since there a 4-player winner really did beat three people. The reachable impact was narrower than the item assumed: `getH2HRecord()` has no HTTP route and no frontend caller, so the two badges fed by `/api/players/h2h-summary` were the whole user-visible surface — **The Rematch** could fire as "revenge" for a four-player game the opponent happened to win, and **Grudge Match** reached its 10-game milestone on games that were never duels. The filter also removes a latent wrong answer flagged in `REFERENCE.md` §22: `previousWinner` resolves the winner as "p1, else p2", which is only sound when the game had exactly those two participants — a 3+ player game won by somebody else entirely reported p2. Six new cases in `backend/test/db.h2h.test.js`; the existing test that documented the old behaviour as deliberate was rewritten rather than deleted. `REFERENCE.md` §3 rewritten, §22's known-limitation entry removed | `docs/archive/multiplayer-x01-roadmap.md` |
| **Item 64** — the four turn-loop dispatch chains → GAME_TYPES members (2026-07). `render()`, `throwDart()`, `undoLastTurn()` and `enterTurn()` each opened with their own hand-maintained fifteen-branch `if(game.gameType === …)` chain — the same list written out four times, which is four chances for a new type to be added to three of them. Now `render`, `throwDart`, `undoLastTurn`, `enterTurn`, plus an `afterDart` the original sketch didn't anticipate: `throwDart()` wasn't a plain chain but seven wholesale overrides, then a shared guard-and-push, then a *second* chain of post-push behaviours, so the shared body stays as `throwDartVisit()` and only the variance moves to the registry — giving all sixteen a full `throwDart` would have duplicated the guard and the push sixteen times. X01 is explicit and there is no fallthrough: a type missing a member throws on its first frame instead of silently running X01's scoreboard or committer. That mattered for `enterTurn`, where six types had no branch and reached X01's committer, working only because `noDartsThrown()` happened to return early for all of them; they now declare `enterTurnPerDart`, a named no-op. **The item's own precondition was honoured** — its update said to add the scoring assertions before the refactor, because a mis-transcribed turn-loop branch produces a wrong score, which looks entirely normal on screen. The verify-ui `turn-loop` check was written first (throw → commit → undo, state must come back byte-identical; no per-mode expected numbers, since what isn't covered elsewhere is whether the right *function* ran at all) and immediately found **five pre-existing undo defects** invisible to every existing test: `atwHitSet` never restored in any mode while its sibling `singlesHit` was, Checkout Ladder and Dead Man Walking restoring none of the six achievement-tracking fields despite calling `awardVisitAchievements()`, and Halve-It leaving a `roundHalved` mark that painted a halving which no longer happened. Fixed at altitude — one shared `snapshotVisitAchievementState()`/`restoreVisitAchievementState()` pair instead of six fields re-listed per call site, which is how three modes ended up restoring different subsets. `backend/test/frontend.turn-loop-dispatch.test.js` (8 cases) pins the contract at source level; verify-ui `turn-loop` (47 assertions) pins the behaviour | `docs/code-quality-roadmap.md` |
| **Item 61** — Around the Clock head-to-head race (2026-07). The drill became a **dual** type: solo is untouched (one clock = one game), and 2+ players now race the same twenty numbers — three darts each, turn about, first to clear takes the leg, with real best-of-N legs and sets through the shared `advanceLegSetGame()`. That meant taking it out of `drillGameTypes`/`drillModes`, since it is now reached as `setMode('practice'|'h2h')` + `setGameType()`; left in either list, `setup.gameType` would have been reset to `x01` the instant `setMode('practice')` ran, silently starting an X01 game from the Around the Clock ledger row. The turn boundary (`ATC_DARTS_PER_VISIT = 3`) runs the same line in both modes — with one seat the walk returns to it, so there is no solo-only branch to keep in step. The scoring screen renders one row per player, each with its own outstanding-numbers grid (the grid host moved from an id to a class — a race renders one per player, and duplicate ids would have broken `getElementById()` and the landscape rail's `:has()` rule). On `/display` the Ring stage stays for solo and steps aside for a race: `stage()` returns `''` above one player and `renderScoreboard()` falls through to a new `lane()` of twenty cleared/owed cells — two dials side by side would each be under half the size that made the dial worth choosing. Expressing "not this state" required calling `stage()` once and testing its result rather than testing for the method, which is now the registry's documented contract. **Stats**: the two drill boards are now explicitly practice-scoped server-side (they were written when this mode was always practice by construction, which the race made false), and the race gets its own Most Race Wins board via `_winLeaderboard('around_the_clock')` on the H2H tab. `rebuildAroundTheClockRaceState()` is the one new calculation — the leg boundary resets *every* clock, so who won which leg has to be known while walking the turns, which is why the solo rebuild could not just be run per player. Found on the way: `playerSnapshotAroundTheClock()` never sent `roundTrebles`/`roundDoubles`/`roundMisses`, which the Ring stage has read since the redesign — every board showed "0 trebles, 0 misses" for a run that had plenty of both. `backend/test/around-the-clock-race.test.js` (12 cases), `REFERENCE.md`, README | `docs/game-modes-roadmap.md` |
| **Item 71** — 2-4 player head-to-head X01 (2026-07). `maxPlayersForSetup()` now returns `4` for the `x01` key: an X01-specific ceiling, deliberately narrower than the app's global 6, because a 501 leg is long enough that at six players the wait between your own visits stops being a game you are playing and becomes one you are watching. The research the doc did up front held — the turn engine (`nextActiveIndexFrom()`, a modulo walk that skips `dnf`), the leg/set/match tree, the completion panel (`h2hPanelColumns()` maps over `game.players`, and `panelHeroesByPlayer()` already shrank its numerals past two) and the whole `/display` lower third were already generic, so the build was one cap, one CSS fix and one accessibility addition rather than a layout project. Verified by playing real four-player legs in both orientations and on a 1920x1080 second screen: four `.pscore` rows fit without scrolling in landscape (96px each) and portrait (59px) with the board unaffected, and the post-leg result view renders four lanes, four standing chips and the verdict line. The one real defect found was in the row's name line — a long name left just enough width for "DOUBLE" and pushed "OUT" onto a second line, so one row in a stack of four rendered a line taller than its neighbours; `.pscore .nm-out` is now `white-space: nowrap`. Every opponent-framed badge (Comeback Kid, Giant Slayer, Nerves of Steel, The Rematch, Grudge Match) was already gated on `active.length === 2` for the bow-out case and therefore correctly never fires at three or four. **Accessibility**, per the doc's own open question: `announceTurn()`/`nextThrowerPhrase()` append "*Name* to throw." to each committed visit at three or more active players — silent at one or two, and silent again once bow-outs shrink a match back to two — appended to the result rather than spoken as a second `announce()`, since two calls in the same tick collapse into one in a single live region. Applied to every multi-visit mode (Cricket/Baseball/Shanghai/Halve-It/Pressure Chamber all had the identical gap), not just X01. `backend/test/frontend.multiplayer-x01.test.js` (18 cases), `REFERENCE.md` §11 + §20 | `docs/archive/multiplayer-x01-roadmap.md` |
| **Item 68** — `finishUnit()`'s per-type match-summary special cases → a registry member (2026-07). Shipped as `GAME_TYPES.<type>.completionPanel(game, winner, kind)` during the completion-panel work rather than as a standalone refactor, and as a **superset** of what the item asked for: a spec rather than HTML (one renderer builds the markup, so layout and accessibility are written once), covering the **leg**-complete screen as well as GAME OVER, and requiring every mode to declare either a panel or `noCompletionStats` — an entry declaring neither now fails both suites, which is what replaced the deferral's worry about silent breakage. All five ad hoc summary IIFEs, the `h2hStatsHtml()` exclusion list and all ten per-type flags are gone; the last flag (an `isCricket` Share-button suppression) became its own registry member `noMatchShare`, since "this mode builds no matchwin moment card" is a fact about the mode rather than about Cricket. The full per-type live check the deferral required was done via the verify-ui `all-game-types` check. | `docs/code-quality-roadmap.md` |
| Live scoreboard redesign — "Lower Third" + "The Ring" + a post-leg result view (2026-07, owner request, chosen from four `/frontend-design` directions). Measured on a real 1920x1080 screen first: ~45% of every player card was a hole, the biggest object was the remaining score (which changes once every three darts) while the average, the checkout and the darts just thrown sat at 1.7vmin along the bottom, **Cricket's MPR was absent entirely**, and every non-X01 mode was shoehorned into an X01-shaped card. Now: a permanent **throw strip** carrying the live visit at 8vmin, full-width **player lanes** whose statistics run across the width, and the match score absorbed into the top bar (the old `.matchbar` repeated every name directly above the cards that already printed them; the old `.co-strip` folded into the strip's third pane). Every game type declares exactly one of `lane()` or `stage()` — the fourteen `card()`/`scorecard()` renderers, eleven `summary()` methods and seven helpers they replaced were deleted once unreachable (display.html 3209 -> ~2560 lines). **Around the Clock and Around the World take direction C, "The Ring"** (the owner's separate pick for the two guided drills): a board-shaped dial centre stage, with a new ring that splits each wedge into the three outcomes a number owes so a part-finished number reads at a glance — which a 63-cell grid never did across a room. Just Chuckin' It joins them, its heatmap being the only thing a mode with no score, no target and no round can show. Per-dart modes get their own strip (three pending slots and a visit total are both lies there). **The post-leg/match result view** fixes a conflict that predated the redesign: a full-screen black banner announcing "LEG COMPLETE" sat directly on top of the summary cards for exactly the 3.5 seconds those numbers were worth reading; the result is now composed in place from the same lanes, in the same positions, and the banner is suppressed whenever there is a result view. Lane figures come from `scoring.js`'s new `liveLaneStats()`, built on the same `pracAggregate()` the leg-complete panel uses, so the TV and the results screen cannot disagree; `turnsForPlayer()`'s unattributed-list rule is load-bearing (filtering an unattributed list by name would silently blank every solo drill's history line). Fixes found by looking at the rendered screens rather than trusting them: `visitScored` needed an `ALLOWED_LIVE_KEYS` entry (BUG-28's exact shape) or the strip showed 0 for every visit; Cricket's mark glyphs were being escaped and printed as tag source; and the chalk-dust burst was silently lost because `triggerChalkEffects()` queried a `.score` element that no longer existed. Three hardening suites (BUG-12, SEC-26, BUG-30) were re-pointed at the renderers that replaced the ones they covered rather than deleted with them. `backend/test/live-lane-stats.test.js` (12 cases), `REFERENCE.md` §7, README | *(no separate roadmap doc — owner request implemented directly)* |
| Completion panels for every game mode (2026-07, owner request — "use this as a template and customize the same page for all other game modes"). The solo-practice "Trophy Cabinet" panel became the shape every mode's completion screen is built from: one renderer, `completionPanelHtml(spec)`, driven by a `GAME_TYPES.<type>.completionPanel(game, winner, kind)` spec — hero band, optional shelf, emoji tallies, quiet columns — dispatched from `finishUnit()`'s leg AND game-over branches. **Twelve modes gained one**: X01 H2H, Cricket, Baseball, Shanghai, Halve-It, The Pressure Chamber, Killer, Bob's 27, The Gauntlet, Dead Man Walking, Around the Clock, Around the World. The five bespoke `${xSummary}` blocks concatenated into `finishUnit()`'s template (and the nine per-type flags gating them) are gone — that hand-maintained shape is exactly what let **Cricket ship with no completion panel at all** and let the rest drift into five different layouts. The four modes that legitimately show nothing now declare `noCompletionStats: true` rather than rendering blank: Just Chuckin' It, Doubles Practice and Checkout Ladder never reach `finishUnit()` (no unit boundary ever resolves — only "End Game", which abandons), and Checkout Trainer has its own paper-themed results screen. A registry entry declaring **neither** is now a test failure in both suites. Two per-visit logs were added because a panel needs them and nothing else records them: `player.legRoundMarks` (Cricket — MPR is its mean; not derivable from `player.marks`, which caps at the closing 3 and hides overkill) and `game.dmwRoundResults` (Dead Man Walking — which rounds were survived, at what target, in how many darts; `dmwWalkedOutCount` only says how many), both undo-safe and both re-derived on resume. `h2hStatsHtml()` was refactored onto the shared `h2hPanelColumns()` so a mode's per-player rows are stated once, in its registry entry. Solo game-over wording now derives "SESSION COMPLETE / Stats saved." the same way the solo banner already did. `backend/test/completion-panels.test.js` (31 cases, spec functions brace-matched out of `index.html` and evaluated against built game states), new `rebuildDeadManWalkingState()` cases in `scoring.test.js`, 16 new live assertions in the verify-ui `all-game-types` check (every registered mode's panel rendered against real player objects after a real dart), `REFERENCE.md` §2 "Completion panels" | *(no separate roadmap doc — owner request implemented directly)* |
| Solo-practice leg-complete panel redesign (2026-07, owner request, chosen from four `/frontend-design` directions — "Trophy Cabinet"). The old panel was two flat label/value cards where every row carried identical weight, so nothing was scannable, and the **three-dart average was absent entirely** (it lived in 11px text up in the live scoreboard). Now: the two averages as a hero band (leg first, gold-tinted), the session's checkouts as a chip shelf highest-first with the best enlarged, emoji tallies in the app's own vocabulary (💯 ton+, 🎯 180s, 🐟 Big Fish, 🔥 best visit) where a zero stays visible but dimmed, then two quiet columns for the secondary stats. Fixes reported alongside it: session checkouts rendered chronologically while Best Visits beside them was sorted (two lists of numbers under two different rules); solo practice announced "Ben wins the leg" when there is nobody to beat — `finishUnit()` now derives "Leg 17 complete" for `practice && players.length === 1`, derived rather than passed via `opts` because every solo leg comes through the generic `onLegWon()`; and the session's leg count now appears in both the subtext and the panel. New stats: first-9 average, ton+ visits, 180s, Big Fish, darts per leg, legs played. The aggregates went into `frontend/scoring.js` as `pracAggregate()`/`pracFirst9Average()` so both scopes share ONE implementation (a leg figure and its session counterpart can never be computed two different ways) and so they carry committed tests. Averages read the player's canonical `legPoints`/`legAvgDarts` and `gamePoints`/`gameAvgDarts` rather than being recomputed, so the panel can never disagree with the live scoreboard. H2H is unaffected. `backend/test/prac-leg-panel.test.js` (18 cases), `REFERENCE.md` §2, README | *(no separate roadmap doc — owner request implemented directly)* |
| Checkout Ladder achievements + "one world = one game" for guided Around the World (2026-07, two live bug reports — `docs/bug-roadmap.md` BUG-37/38). **BUG-37** is BUG-34's root cause showing up in a second mode: the per-visit achievement block lived inline in `enterTurn()`, so every mode with its own commit path missed it. Checkout Ladder evaluates visits with `evaluateVisit()` — X01's own evaluator — so it now calls the shared `awardVisitAchievements()`, and its player factory gained the tracking fields (same gap Dead Man Walking's had). An audit of all enter-turn paths found seven modes awarding only the time-of-day pair; the three that use `evaluateVisit()` are wired up, and the other four are deliberately left alone (no remaining score to check out from), a decision now recorded in the test rather than re-litigated. **BUG-38**: guided Around the World tracked LIFETIME progress, so a run began at whatever your lifetime set held — the badge fired for hitting only the few outcomes you'd never hit, and once the set was complete every new game opened at 63/63 with nothing to do (`p.sessionHitSet` was only added to when an outcome was new to lifetime, so a complete player could never make progress at all). Redesigned to the session's own 63, matching Around the Clock's "one clock = one game" — completion ends the game with a WORLD COMPLETE screen and the full completion sequence, without which the game never becomes `done` (the exact bug the Clock's redesign fixed). Lifetime stays visible as context and still drives the profile grid, Home leaderboard and passive badge. `rebuildAroundTheWorldState()` now replays the session's hit set so a resumed save doesn't restart at 0/63, and undo restores the completion guard. `REFERENCE.md` documented the old behaviour as intended, so it was updated as a deliberate design change. `backend/test/around-the-world-session.test.js`, `backend/test/frontend.visit-achievements-shared.test.js`, README | `docs/game-modes-roadmap.md` |
| Dead Man Walking: difficulty levels, an integer dart budget, and achievements that actually fire (2026-07, three live bug reports — `docs/bug-roadmap.md` BUG-34/35/36). **Achievements**: every per-visit check — 180s, Big Fish, the whole CHAIN_CHECKS list, Metronome/Cruise Control/Ice in the Veins, Around the Clock/World progress, first-100-checkout — had grown up INLINE inside `enterTurn()`, X01's own commit path, so a mode with its own commit function reached none of it; extracted to `awardVisitAchievements(p, ev, snap)` and called from both. `newMatchPlayerDeadManWalking()` also needed the per-visit tracking fields, a gap only found by driving a real round in a browser. **Budget**: par came from an average of darts per whole LEG (~25), so a band with history handed out ~24 darts for one checkout while a band without fell back to a 4-dart budget on a 170 — and being an average it was fractional, which is what put "24.7 darts left" on screen and then "7.699999999999999" after two misses. **Difficulty** replaces it: budget = perfect route + margin (Easy +6, Medium +4, Hard +2, Extreme +0), so a setting means the same thing on a 32 as on a 170, and Extreme sits exactly on the objective floor rather than below it. Integer by construction, verified exhaustively across every finishable score on every difficulty. Difficulty is the one client-supplied field this mode has, validated against a fixed four-id list; the 15 targets stay server-authoritative. `DEAD_MAN_WALKING_BANDS`/`deadManWalkingBandFor()`/`_dmwHistoricalAverageDarts()` deleted. `backend/test/frontend.visit-achievements-shared.test.js`, rewritten suites in `scoring.test.js` and `db.dead-man-walking-stats.test.js`, `REFERENCE.md` §33, README | `docs/archive/dead-man-walking-roadmap.md` |
| Board colour scheme for a rotated board (2026-07, owner request). A physical board is rotated periodically to spread the wear and the number ring moves with it, so an odd-numbered rotation lands sector 20 on the other zone scheme. Settings → Board colours is that one choice — which scheme 20 sits on — so the on-screen board matches the one on the wall. A dartboard has exactly two schemes and each is a **pair** (black bed + red rings, white bed + green rings); the colours are fixed constants, deliberately not configurable. This **fixed a rendering bug**: `buildDartboard()` picked the bed and the ring from two independent alternating lists, pairing the TAN bed with the RED ring — a combination no real board has, i.e. the beds were offset one sector from the rings. Treating a scheme as an indivisible pair makes that unrepresentable, and a committed test sweeps valid, invalid and hostile inputs asserting a bed is never split from its own ring. Two deliberate default-board changes follow: sector 20 now has a **black bed with red rings** (an unrotated real board), and the green darkened `#1b8a3a` → `#17752f` because the inner bull takes sector 20's ring colour, so a rotated board put the 12px "Bull" label on green where nothing cleared 4.5:1 (best was 4.10:1); the darker green reaches 4.70:1 and also improves the ring against its own white bed. `boardLabelColor()` computes that label, generalising the hardcoded colorblind special case it replaced; colorblind mode keeps precedence over ring colours. Because the colours aren't configurable there is no path from stored data to an SVG `fill` attribute — the only stored value is one of two known ids, rejected with a 400 if unknown. Scoped to the scoring board. `backend/test/board-colors.test.js`, `backend/test/server.board-colors.test.js`, 11 live checks in the verify-ui suite, `REFERENCE.md` §17 "Board colours", README Settings + API sections | *(no separate roadmap doc — owner request implemented directly)* |
| Landscape/tablet scoring layout, board-first pass (2026-07). On an iPad Air 4 the board drew at **400px** of an 1180px screen. Two causes, neither a cap on the board itself: `body.game-active .wrap` capped the whole game screen at 760px, and the board SVG is `preserveAspectRatio="meet"`, so with column 2 at 422x753 the *width* was binding and it drew 400x400 inside a 731px-tall well. Overriding the cap **inside the landscape media query only** (portrait keeps it, and portrait geometry was diffed live at 390x844 against the pre-change build and came back byte-identical) plus a narrower rail took the board to **739px**, a 1.8x linear increase. **No board geometry changed** — `BOARD_GEOM`'s radii and every sector path are untouched, so the doubles and trebles keep their true visual size and no hit area exceeds the shape drawn under it; the accuracy gain is the scale, not the target, and the radius table is now pinned by a test for exactly that reason. Also: a compact score strip (selected structurally via `:not(:has(#atc-live-progress, #atw-live-progress))`, not by naming game types in CSS), a one-line current-turn dart row whose filled slots are real `<button>`s calling `undoToDart(i)`, a board-tap score flash (`#dart-flash`, 1.5s hold then fade, `aria-hidden` so it doesn't double-announce), and a large Undo Dart target. Undo depth was investigated and needed no change — `undoDart()` pops one uncommitted dart per tap and `undoLastTurn()` walks a real snapshot stack back through the whole leg; the gap was discoverability, which the tappable row closes. `backend/test/frontend.landscape-scoring.test.js`, `REFERENCE.md` scoring-screen-layout section | *(no separate roadmap doc — owner request implemented directly)* |
| Checkout Trainer presentation — chosen from four `/frontend-design` directions (2026-07, direction A). The mode was filed among 16 board games in a collapsed "Practice & Drills" group, third of seven and the only one of them needing no dartboard, with "no dartboard needed" buried in a blurb behind a disclosure. It now gets **the Pocket Card on Home** (`renderPocketCard()`) — a live target answerable in place, below Today's Challenge and above Start new game — and **Paper Mode** (`GAME_TYPES.<type>.paperTheme`, `body.paper-mode`) restyling its play area to match. The one design move behind both: this is the only surface in Oche made of paper rather than the black board or the slate scoreboard, using the dartboard's own wedge cream, so "you can play this with no board in the room" reads at arm's length. The card's answers are deliberately unrecorded and say so — Home is shared, and requiring a player pick would destroy the two-tap immediacy that is the point; it delegates target selection and grading to `pickCheckoutTarget()`/`gradeCheckoutAttempt()` so it can never disagree with the real mode. Paper is painted on `#screen-game`, NOT `.game-play-area` (which is `display:contents` and generates no box — a background there silently does nothing). Both palettes held to `docs/accessibility-roadmap.md`'s 4.5:1 standard, with two colours caught failing during the build; committed tests capture each colour out of the stylesheet rather than a copied list (`backend/test/frontend.pocket-card.test.js`). `REFERENCE.md` §19 rewritten | *(no separate roadmap doc — `/frontend-design` direction implemented directly)* |
| Test data seeder — `backend/seed-dev-db.js` (`npm run seed`), the third testing target: a populated, realistic, **deterministic** database built by simulating real X01 (H2H and practice) and Cricket matches, closing the gap that both existing verification surfaces are blind to an empty database (`node:test` seeds two or three hand-picked turns per case; the `verify-ui` browser suite runs against a scratch DB, so every Home/Pulse/leaderboard/personal-best panel it renders is in its zero state — a leaderboard sorted backwards, a query that only breaks on ties, a "best ever" that never updates and a rate with the wrong denominator all look correct until there is enough history for the wrong answer to differ visibly from the right one). Two invariants make its output trustworthy: every row goes through `db.js`'s **own** `createGame()`/`recordTurn()`/`completeGame()`/`abandonGame()` — `recordTurn()` specifically, the `enforceConsistency:true` path the HTTP layer uses (SEC-22), never raw SQL — so it cannot manufacture a state the running app couldn't produce (a "bug" found against an impossible state costs an afternoon to disprove); and every visit is scored by `frontend/scoring.js`'s **own** `evaluateVisit()`/`evaluateVisitCricket()`, so there's no second copy of the rules to drift. Throwing model: a per-player `skill` scaled per ring (`ringFactor()`), with misses degrading realistically (right number/wrong ring → board neighbour → off the board), so trebleless rate, ton-plus rate, checkout percentage and the miss heatmap all fall out of one distribution; once a finish is live, aiming defers to `checkoutHint()` — the app's own advice — which besides realism is what makes a 170 Big Fish and a nine-darter structurally *reachable* (a hand-rolled "set up 32, take the double" policy never aims at the bull). Default roster skills are spaced widely on purpose so every derived metric (3-dart average, darts/leg, trebleless rate, Elo, Cricket MPR) comes out in the same monotone order, making "is this leaderboard the right way up?" answerable rather than lost in sampling noise. Seeded mulberry32 PRNG (same `--seed` → same darts, so a bug found once is reproducible forever); timestamps backdated across `--days` after creation — `created_at` plus `completed_at` **or** `dnf_at`, never both — since otherwise every seeded game lands in the same second and collapses Legs Today/This Week, On This Day, form trends, the recap and every date-bucketed query. Refuses to write to `data/darts.db` with or without `--force`. Also added: a `test:coverage` npm script (`node --test --experimental-test-coverage`). Committed tests in `backend/test/seed-dev-db.test.js` (route-label round-tripping across every finishable score 2–170 in both out modes, `aimX01()` never proposing an off-board or treble-bull dart, `aimCricket()`'s stalemate guard, PRNG reproducibility, and an end-to-end run asserting two same-seed runs produce identical darts, one ending per game, one winning turn per leg, and both write guards). `REFERENCE.md` §35 | `docs/testing-and-observability-roadmap.md` |
| Player Profile: scoped "Games & Legs Played"/"H2H Wins" to the selected Game Mode, redundancy audit, Household Rating moved to header. The "Games & Legs Played"/"H2H Wins" sections previously ignored the Game Mode dropdown entirely, showing every category from every game type mixed together (e.g. "170", "301", "Cricket (15-20, Bull)" all at once regardless of which mode was selected) — the one part of the profile that didn't scope the way the stat bubbles above it already did. Fixed via a new `CATEGORY_MATCHERS`/`categoryMatchesMode()` lookup keyed on each H2H-capable game type's own `category()` generator (item 41), also applied to the Practice tab's "Legs Thrown" list for consistency. Merged the two sections into one non-collapsible "{Mode} — Games & H2H" line (the "persistent summary line" direction from a `/frontend-design` 4-idea exploration the owner picked), tailored per mode to avoid restating what's already a stat bubble: X01 (which has no games/win-rate bubble at all) shows the full `N games · M legs played · W-L H2H` plus a per-category chip row; Cricket/Baseball/Shanghai/Halve-It/Pressure Chamber/Killer (which already show primary "Games Played" and "Win Rate" bubbles) show only `M legs played · S sets won · L legs won`, omitting the redundant games/win-loss figures. A `/code-review`-style audit of the whole Player Profile page for other same-page redundant data (excluding the header stats) found two further confirmed duplicates, reported to the owner but not yet fixed pending their decision: Around the World's "Sessions Played" stat bubble duplicates its own Personal Best entry of the same name; X01's "Big Fish" bubble duplicates the 170 checkout row already at the top of "Top Checkouts" (same underlying query/filter, same page view). Separately, moved Household Rating up into the page header (4th tile, alongside X01 Average/Total Darts Thrown/Darts Per Day) via its own `loadPlayerHeaderRating()` fetch (`GET /api/players/elo`, same endpoint the existing "📈 Household Rating" section below already uses) — the headline number now shows immediately regardless of tab/mode, same "header teaser + full section below" precedent X01 Average already set; the fuller section (record/rank/sparkline) is untouched. Verified live via Playwright against the real `renderPlayerPage()` code path with synthetic multi-category X01 and Cricket data: correct per-mode category scoping, correct redundancy-trimmed content for each mode, 4-tile header renders correctly; full 1305-test backend suite passes (frontend-only change). `REFERENCE.md` updated (rewritten header-row subsection, new "Games & H2H section — scoped to the selected Game Mode" subsection) | *(no separate roadmap doc — implemented directly)* |
| Players page visual redesign ("Standings Board") via `/frontend-design`: four distinct mockup directions were built and screenshotted, the owner picked the ranked-standings direction, and it was implemented into the real `frontend/index.html` — no new backend endpoint or data shape, purely a restyle/re-sort of the same `renderRoster()` data. Replaces the flat alphabetical roster list with a lifetime-average-ranked standings board (`rosterAvgOf()`) — players with no avg yet are kept in a separate "Not yet ranked" group rather than sorted in alphabetically at the bottom. Rank #1 gets a gold divider styled after "the oche" (the actual toe line darts are thrown from), shown only when there's a field below it. Each row shows rank, name + achievement badges, a win-rate/games-played/last-played meta line, the lifetime average, and a recent-form arrow — all the same underlying calculations the old list already used, just restructured. Rename/Delete moved from two always-visible icon buttons to a single "⋮" kebab popover per row (`toggleRosterKebab()`), closed by a document-level outside-click listener; the actual write-gating (`Auth.ensureCanWrite()`, `canManagePlayers()`) is unchanged. Fixed a real overflow bug found during verification: a long unbroken player name (test fixtures like `TooltipTest_1784730458460`) pushed the Avg/Form columns outside the card since the grid's name column had no `min-width:0`/word-break — fixed before shipping. Verified live via Playwright: correct ranking order, "The Oche" divider placement, unranked-group separation, kebab open/close, and row-click navigation to the Player Profile page all work correctly at a real phone viewport width; full 1305-test backend suite passes (frontend-only change). `REFERENCE.md` updated (new "Players page visual redesign — Standings Board" subsection) | *(no separate roadmap doc — implemented directly)* |
| Settings page visual redesign ("Modular Dashboard" + search) via `/frontend-design`: four distinct mockup directions were built and screenshotted, the owner picked the modular-dashboard tile direction with the search box from the search-first direction folded in, and it was implemented into the real `frontend/index.html` — no new backend endpoint or data shape, purely a restyle of how the existing 17 settings sections are presented/navigated. Replaces the old 4-tab-plus-accordion layout with a continuous, always-visible tile grid (`.stile-grid`/`.stile`) grouped by zone (Access & Account/Gameplay & Display/Integrations/Admin & Danger Zone, each with a left-border accent color as a redundant, non-exclusive visual cue) — the old `.player-tabs`/`switchSettingsTab()` tab bar and `.settings-group[data-group]` hidden-gating are gone. Every tile shows a live one-line current-state summary (`refreshSettingsTileStates()`/`SETTINGS_TILE_STATES`, read straight from the same `SETTINGS_FIELDS` form elements Save settings already persists, refreshed on every edit via event delegation) before it's expanded; tapping a tile spans it to the full grid row (`.stile.expanded`) instead of squeezing its detail body into a half-width column. Adds a search box (`#settings-search-input`/`filterSettingsTiles()`) that filters tiles by their own rendered text, hiding a zone's label+grid together once every tile under it is filtered out. Verified live via Playwright: all 17 tiles render with correct live state values (including a real save/reload persistence round-trip), search correctly narrows to a single matching tile, and tile-expansion correctly spans the full row without disturbing sibling tiles at a real phone viewport width; full 1305-test backend suite passes (untouched by this change, frontend-only). `REFERENCE.md` updated (new "Settings page visual redesign — Modular Dashboard + Search" subsection) | *(no separate roadmap doc — implemented directly)* |
| Home screen visual redesign ("Live Ticker & Podium") via `/frontend-design`: four distinct mockup directions were built and screenshotted, the owner picked direction 3, and it was implemented into the real `frontend/index.html` — no new backend endpoint or data shape, purely a restyle/rearrange of `getSummary()`/`getHomeExtra()`'s existing payload. Adds an auto-scrolling ticker (`#home-ticker`/`renderHomeTicker()`) surfacing today's challenge, last game's winner, the all-time highest checkout, all-time 180 count, and the Household Ratings leader — hidden when no data is available, `aria-hidden="true"` since every item it shows is also shown non-scrolling elsewhere on the same page, and its animation is disabled under `prefers-reduced-motion: reduce`. Restyles the Today's Challenge card so the challenge name is a large Bebas Neue headline with a gold eyebrow label. Adds a visual 1st/2nd/3rd podium (`.home-pod`) to the H2H/Practice tab's Three-Dart Average leaderboard specifically (`renderAvgLeaderboard()`) — every other Home leaderboard is deliberately left as a plain ranked list; rank 4+ still uses the existing shared row template. Section titles across the Home tab (`.pp-section-title`) and all `.hero-val` stat figures gained a passive Bebas Neue/gold-glow upgrade via shared CSS, without touching individual per-game-type renderer functions. Verified live via Playwright against the dev DB: ticker populates and loops with real data; podium correctly renders a single centered gold seat when only one player currently has qualifying stats (a normal data-driven state, not a bug); full 1305-test backend suite passes (untouched by this change). `REFERENCE.md` updated (new "Home page visual redesign — Live Ticker & Podium" subsection) | *(no separate roadmap doc — implemented directly)* |
| Around the Clock redesigned to "one clock = one game" — fixes a real bug (completing the clock never called `DB.completeGame()`, so `game.done` stayed false and the hamburger menu kept offering "⏸ Save for later" even after a player had fully cleared the clock). Completing the clock now ends the game immediately via the same `DB.completeGame()`/`finishUnit('game', ...)` path Gauntlet/Bob's 27/Dead Man Walking already use, replacing the old "Start Next Clock" same-session continuation. New CLOCK COMPLETE stats screen (darts/trebles/doubles/misses/time) on both the in-app GAME OVER card (`aroundTheClockSummary`) and the live scoreboard (`renderers.around_the_clock.summary()`, `display.html`) — `roundTrebles`/`roundDoubles`/`roundMisses` tallied alongside the existing `roundDarts`, correctly reconstructed on resume too. Audited every other match-style type's `legSummary`↔`display.html` `summary()` field pairing for the same class of bug (Cricket/Baseball/Shanghai/Halve-It/Pressure Chamber/Killer/Dead Man Walking/Bob's 27 all already correct — clean bill of health). Added `finishUnit(kind, winner, opts)`'s optional third argument so a completing game type can override the generic "{winner} wins the game"/"GAME OVER" wording where it doesn't fit a solo drill, plus a new `game.doneHeading` one-shot field (mirrored to `/display` the same way `legSummary` already is) so the live scoreboard's banner heading can be overridden too, not just its subtitle. Every `finishUnit('game', ...)` completion (all match-style types) now shows **🔁 Play Again** (relaunches the identical game type + same players + same leg/set format + relevant per-type secondary config, reusing the New Game wizard's own `NEW_GAME_MODE_OPTIONS.apply()`/`startGame()` rather than duplicating either) and **🏠 Return Home**, with the old generic "New game" link kept as a smaller, demoted third option — tournament matches keep their existing single "Back to bracket" action, unaffected. Committed tests: 2 new cases in `backend/test/scoring.test.js` for the treble/double/miss tally (including the legNo-transition reset). `REFERENCE.md` §2 updated (new "Game Over screen — Play Again / Return Home" subsection, rewritten "Guided Around the Clock / Around the World" subsection); `docs/game-modes-roadmap.md`'s "Guided Around the Clock / Around the World" section marked superseded. Verified live end-to-end via Playwright: a full 20-number clock completion (hamburger's Save button correctly hidden afterward, CLOCK COMPLETE stats correct on both the in-app screen and the live scoreboard, Play Again relaunching a fresh solo clock with the same player), a real 2-player X01 match's Play Again (same players/game type), and a saved-then-resumed mid-clock game correctly restoring all three new tallies. Full 1303-test backend suite passes | *(no separate roadmap doc — implemented directly)* |
| Forfeiting a multiplayer game / abandoned games count as a DNF. Two related, additive facts, neither changing what `completed_at`/`winner_id` mean anywhere: (1) "🚶 Bow out…" in the game-options ☰ menu lets one player in a still-live multiplayer match (X01/Cricket/Baseball/Shanghai/Halve-It/Pressure Chamber/Killer) leave without ending it for everyone else — `forfeitPlayer(gameId, playerName)`/`POST /api/games/:id/forfeit` marks `game_players.dnf=1` for just that participant; if it leaves exactly one other active player the match completes as a walkover win for them (reusing `completeGame()`, same shape as `recordWalkover()`); if it leaves zero, the match ends with no winner via new `games.dnf_at`. (2) "🚪 End game" now actually calls the backend (`abandonGame(gameId)`/`POST /api/games/:id/abandon`, wired into `askEndGame()`'s existing confirm flow) — every previously-invisible abandoned match (including a 501 game never closed on a double) is now marked DNF instead of silently staying an uncounted in-progress row forever. Turn rotation/leader-determination made DNF-aware across every shared engine site (`advanceToNextActivePlayer()`, `isRoundComplete()`, `evaluateVisitCricket()`'s opponent filter, the new `soleActiveLeaderIndex()` helper for Baseball/Shanghai/Halve-It, Pressure Chamber's filtered candidate pool); Killer reuses its existing `eliminated` flag rather than a second mechanism. Known limitation at the time: a save/resume cycle after a bow-out would rebuild the departed player as active again, since `dnf` isn't threaded through the pure replay/rebuild functions — `saveGame()` rejected (409) saving any game with a `dnf=1` participant instead of silently allowing the desync (found in a full-codebase review). **That limitation is now closed and the 409 removed — see the "DNF survives a save/resume" item below.** Committed tests: `backend/test/db.forfeit-and-abandon.test.js`, plus a "DNF-aware winner determination" suite in `backend/test/scoring.test.js`. `REFERENCE.md` §13 updated. Verified live: a 3-player Cricket bow-out (turn skips the departed player correctly, DNF tag shown, other two keep playing) and a 2-player walkover (forfeiting player marked DNF, opponent auto-credited the win).<br><br>**Follow-up fixes from a full-codebase code review** (7 more confirmed bugs, all fixed in the same pass): `isRoundComplete()` could never detect round completion again once the leg's *starter* bowed out mid-leg (Baseball/Shanghai/Halve-It/Pressure Chamber would stall indefinitely) — fixed by re-anchoring `game.starter` via a new shared `nextActiveIndexFrom()` helper whenever the departing player was the starter. `completeGame()` had no "already ended" guard at all (unlike `forfeitPlayer()`/`abandonGame()`) — a second completion could overwrite `winner_id` or violate the "never both `completed_at`/`dnf_at`" invariant — fixed via a shared `_requireLiveGame()` helper all three now call. Opponent-gated H2H badges (Comeback Kid, Giant Slayer, The Rematch, Grudge Match, Nerves of Steel) silently stopped firing once bow-outs shrank a match to a genuine 2-player decider, because `onLegWon()`/`onLegWonCricket()` checked `game.players.length` instead of the active-player count — fixed. `saveGame()`/`getResumeState()` didn't check `dnf_at` at all, letting an abandoned game be saved/resumed via the raw API (not reachable through the shipped UI) — fixed, plus the new "block saving with any `dnf=1` participant" guard above. `BOW_OUT_GAME_TYPES` was a hand-maintained array disconnected from the `GAME_TYPES` registry (unlike registry-derived `SAVABLE_GAME_TYPES`) — fixed via a new `bowOutable:true` registry member. Two identical `/api/players/stat-bubbles` requests fired on every Player Profile render in the default view — fixed by having the dropdown-scoped fetch reuse its own response for the header stats when they're the same shape. The ☰ game-menu used `role="menu"`/`role="menuitem"` with no keyboard contract (no arrow-key navigation, no focus-into-menu on open, no focus-restore-to-trigger on close) — fixed with a new `gameMenuKeydown()` handler matching the existing `tournamentTabKeydown()` pattern. Verified live (synthetic-event scoring.js checks for the round-completion fix, direct-state checks for the registry/badge fixes, and Playwright keyboard-navigation checks for the menu fix); 2 pre-existing tests updated to match the new stricter `completeGame()`/`saveGame()` behavior; 4 new committed tests added | *(no separate roadmap doc — implemented directly)* |
| Daily Challenge: live scoreboard now shows the actual challenge, not a raw X01 countdown from a filler starting score. All six formats are plain X01 underneath (`game.gameType` stays `'x01'`), so `/display` previously showed "1000" (the too-high filler three formats use so the engine never busts/wins mid-attempt) as if it were a real category. `GAME_TYPES.x01.liveModeState` (new — `null` for every ordinary X01 game) sends the challenge format/label/metric/visit progress; `renderers.x01.card()` swaps the hero number for the actual metric on the three filler-start formats (Bullseye Gauntlet/Treble Run/Steady Hand — bulls hit, distinct trebles, capped running total, with a 3-visit progress-pip row and, for Treble Run, which numbers as chips) and keeps the real countdown for the other three (Checkout Sprint/Speed to Zero/The Long Game — genuinely meaningful — adding the metric as a caption instead). Top bar and a new "🎯 CHALLENGE" card tag make it visually distinct from a normal X01 game. `REFERENCE.md` §6 updated. Verified live across all six formats (hand-simulated in-progress state through the real liveModeState()/pushLive() path) | `docs/archive/daily-challenge-roadmap.md` (new addition, doc already fully archived) |
| Full-frontend code review (index.html/display.html/scoring.js, 9 finder angles — 4 correctness + reuse/simplification/efficiency/altitude/conventions) — 11 confirmed correctness bugs fixed, all independently verified before fixing: (1) `lastLegWasChallenge`/`lastChallengeResult` only reset on one exit path of `onLegWon()`, so a stale Daily Challenge could leak its "CHALLENGE COMPLETE" heading/buttons onto an unrelated later H2H match's "SET COMPLETE" screen — fixed by resetting both unconditionally at the top of `onLegWon()`. (2) `/api/challenges/start` was fired without `await`, letting gameplay begin before the round-trip resolved — if it then rejected, the 3 filler-starting-score formats (bullseye_gauntlet/treble_run/steady_hand) could never complete, since their only end condition is gated on `activeChallenge` staying non-null — fixed by awaiting it. (3) Comeback Kid's deficit-sampling gate (X01 and Cricket) checked raw `game.players.length===2` while the awarding side (`onLegWon`/`onLegWonCricket`) correctly checks active/non-dnf count, so a bow-out shrinking a 3+ player match to a real decider froze the tracked deficit stale — both sampling sites now match the awarding side. (4) A Killer bow-out that dropped the alive count to 1 didn't end the match — the win check only ran inside `throwDartKiller()`, never from the bow-out handler itself — fixed by checking alive count right after marking a departing player eliminated. (5) Pressure Chamber's live `totalCp` (`scoring.js`) only ever added `result.gained`, never subtracted `result.missPenalty`, contradicting the backend's own documented read-time formula — could show the wrong live winner and incorrectly fire the Ice badge mid-run — fixed, with a new committed test. (6) `display.html`'s `render()` called `renderer.summary()` with no guard for the 4 renderers that don't define one (doubles_practice/chuckin/around_the_clock/around_the_world) — would throw and silently freeze the scoreboard if ever reached — fixed with an X01-shaped fallback. (7) The checkout strip's change-guard keyed only on the route string, not the thrower — two players left on the identical checkout could show the wrong name — fixed by keying on both. (8) Settings' generic field-load fallback had no dedicated branch for `number-zeroOk`, unlike save — fixed to match. (9) Five Player Profile loaders (`loadPersonalBests`/`loadStatBubbles`/`loadPlayerHeaderStats`/`loadTopFinishes`/`loadDartHeatmap`/`loadDartAnalytics`/`loadCoachingInsights`) were missing the "player changed mid-fetch" guard several sibling loaders already use, letting a fast profile switch's stale response overwrite the next player's already-rendered sections (also fixed, as a consequence, the "Race this leg" button binding the wrong player's leg). (10) The Daily Challenge history cache never invalidated across a date rollover (only cleared on a player switch) — fixed by folding the date into the cache key. One candidate (duplicate player names breaking Cricket/Killer's name-keyed opponent lookups) was investigated and **refuted** — names are enforced unique at both the DB and UI layers. Two CLAUDE.md convention violations also fixed: `rebuildShanghaiState`/`rebuildHalveItState` (badge-affecting replay calculations, including the Halve-It badges' `everHalved`/`lastVisitHalved` flags) had no committed test — added 3 new tests; Player Profile's Stats/Player Settings tabs signaled the active tab by color alone with no ARIA state, unlike the tournament bracket tabs' existing correct pattern — added `role="tab"`/`aria-selected`/`aria-controls`/roving-tabindex arrow-key navigation (`playerTabKeydown()`, mirroring `tournamentTabKeydown()`). Reuse/simplification/efficiency/altitude findings from the same review are tracked separately for the subsequent full `/simplify` pass. Committed tests: 1 new in `scoring.test.js` (Pressure Chamber miss penalty) plus 3 more (Halve-It badge-history reconstruction ×2, Shanghai resume reconstruction). Verified live: a real Daily Challenge completion followed by a real H2H best-of-3-sets match, winning only the first set, confirming the set-complete screen now renders correctly instead of the stale challenge screen. Full backend test suite green (1275/1275) | *(no separate roadmap doc — implemented directly)* |
| Daily Challenge: the post-completion results screen (`finishUnit()`, `frontend/index.html`) replaced its generic "This Leg / This Session" X01 average panel — actively wrong for the three filler-start formats, whose average is computed against a meaningless countdown — with the actual challenge outcome. `getChallengeResultSummary()` (new, `backend/db.js`, shared by `completeChallengeAttempt()`'s fresh-completion and already-completed-retry paths) extends `/api/challenges/complete`'s response with `personalBest`/`previousBest`/`lastResult`/`recentAttempts`/`currentStreak` alongside the existing `isPersonalBest`. The screen now shows, synchronously: the real metric as the headline stat (reusing `challengeMetricLabel`/`challengeShortLabel`) plus Treble Run's hit-number chips (`challengeTrebleNumbersFrom()`, shared with `GAME_TYPES.x01.liveModeState`'s live-scoreboard computation); and, patched in once the completion round-trip resolves (`renderChallengeResultExtra()`, `#challenge-result-extra`, replacing the old `#challenge-pb-banner`): the personal-best banner (unchanged), a `🔥 N-day streak` callout (2+ only), a "Your Best"/"Last Time" comparison shown on every attempt (not just new records), and a recent-attempts strip (skipped on a first-ever attempt at a format, since one chip shows no trend). `REFERENCE.md` §6 updated. Committed tests: 4 new cases in `backend/test/db.challenges.test.js` covering the new summary fields (first-ever attempt, a 3-day sequence with a worse-then-better result, format-scoping so a different format's history doesn't bleed in, and the already-completed retry path). Verified live: isolated checks of both new render functions across 3 formats and 2 data shapes, plus one full real playthrough (driven through the actual `startGame()`/`throwDart()`/`enterTurn()`/`finishUnit()` path, not synthetic state) confirming the headline stat and the async personal-best/comparison block both render correctly end-to-end | *(no separate roadmap doc — implemented directly)* |
| Daily Challenge: X01-coupling removed via a `CHALLENGE_FORMAT_DEFS` registry (`frontend/index.html`), the fix identified by a dedicated code-review of the Daily Challenge subsystem ahead of adding new non-X01-shaped formats. Previously every format's win condition, metric, and hero-swap logic was 6+ scattered `if(fmt==='...')`/`else if` chains nested inside X01-only functions (`enterTurn()`, `GAME_TYPES.x01.liveModeState`, `todaysChallenge()`, `buildChallengeResultPanel()`), and `activeChallenge` never recorded which `gameType` it belonged to. Now each of the 6 formats is one self-contained object (`gameType`, `label`/`shortLabel`/`genericName`/`metricLabel`, `pickTarget`, `startScore`, `checkCompletion(game,p,ev)`, `winOverrideMetric(p)`, `extraResultData(game)`, `liveMetric(game,p)`) and every dispatch site reads it generically — `enterTurn()`'s two hardcoded chains collapse to calling `checkCompletion`/`winOverrideMetric`; `liveModeState` computes its running metric via `liveMetric` instead of its own second copy of the same 4-way if-chain (closing the "live" vs "completion" metric-formula duplication the pre-review audit flagged); the results screen's Treble Run-only chip special case and the Player Profile history table's hand-maintained `FORMAT_NAME` map both now read `extraResultData`/`genericName` off the registry instead of their own parallel format list. `CHALLENGE_FORMATS` (the plain name array `todaysChallenge()` seeds a date-hash index into) stays its own explicit array, not `Object.keys(...)` on the registry, so its load-bearing append-only order can't be broken by a future reorder-for-readability. Backend mirror: new `CHALLENGE_FORMAT_GAME_TYPE` map (`backend/db.js`) + a `startChallengeAttempt()` check rejecting a format/game_type mismatch with `400` (db.js has no access to the frontend registry at runtime, so it keeps its own copy). `REFERENCE.md` §6 rewritten to describe the registry mechanism. Verified live: a real Treble Run attempt driven through the actual `startGame()`/`setMult()`/`throwDart()`/`enterTurn()` path (not synthetic state) across all 3 visits, confirming `liveModeState`'s in-progress metric/chips build up correctly visit-by-visit, `checkCompletion` correctly ends the attempt on the 3rd visit, and the completed result round-trips correctly through `/api/challenges/complete` into `daily_challenge_attempts` (`result_darts=2, completed=1`). Full backend test suite green (1275/1275) | *(no separate roadmap doc — implemented directly)* |
| Daily Challenge: 3 new formats (Doubles Gauntlet, Ton Hunter, Around the Horn) + a Home page household comparison board — the payoff of the `CHALLENGE_FORMAT_DEFS` X01-decoupling above, which existed specifically to make this addition a registry entry rather than a change to scattered functions. **Doubles Gauntlet**: most distinct doubles hit in 3 visits (9 darts), same shape as Treble Run but for doubles (D1-D20 and D-Bull all count). **Ton Hunter**: most 100+-point visits across a fixed `TON_HUNTER_VISITS` (6) visits. **Around the Horn**: hit every number 20 down to 1, strict descending order, singles only, open-ended visit count (a wrong number/ring/order dart is simply ignored, never a bust or DNF) — fewest total darts wins. All three are pure functions in `frontend/scoring.js` (`distinctDoubleSectors`, `countTonPlusVisits`, `aroundTheHornProgress`), each with committed tests in `backend/test/scoring.test.js` (7 new tests), wired into the registry's `checkCompletion`/`liveMetric`/`extraResultData`. Fixed the live-scoreboard hero-swap/pip-row logic to be genuinely generic in the same change: `display.html` previously hardcoded a `THREE_VISIT_CHALLENGE_FORMATS` Set of format names (a standalone file with no access to the registry) — replaced with two new registry fields the producer (`GAME_TYPES.x01.liveModeState`) sends generically: `usesFillerScore` (hero-swap to the metric instead of the meaningless filler countdown) and `visitCap` (pip-row count, `null` for open-ended formats), so a future format needs no `display.html` change at all. Backend mirrors `CHALLENGE_BETTER_DIRECTION`/`CHALLENGE_FORMAT_GAME_TYPE` with the 3 new entries. **Household board**: new `getTodaysChallengeBoard(dateStr, format)` (`backend/db.js`) + `GET /api/challenges/today-board` returns every completed attempt at today's format, ranked best-to-worst (direction-aware, no gating on having played it yourself — a deliberate design decision, confirmed with the user); rendered on the Home page under the existing "Today's Challenge" card via `renderHomeChallengeBoard()`. That card also gets a new `.challenge-spotlight` CSS class — an animated gold glow (`@keyframes challengeGlow`) making it visually distinct from every other page card, per an explicit "make it catch the eye immediately" request; respects `prefers-reduced-motion` via the existing page-wide animation-disable rule. Committed tests: 7 new in `scoring.test.js` (the 3 pure format-metric functions) + 4 new in `db.challenges.test.js` (the board query, both sort directions, exclusion of in-progress/other-date/other-format attempts, validation). `REFERENCE.md` §6 updated (all 9 formats' table, the registry field docs, the new board section). Verified live: all 3 new formats driven through the actual `startGame()`/`throwDart()`/`enterTurn()` path end-to-end (correct metric, correct live progress, correct DB persistence for each), plus a real Home page load showing the glowing card and a rendered, ranked household board. Full backend test suite green (1286/1286) | *(no separate roadmap doc — implemented directly)* |
| Player Profile code-review (mobile UX + stat-bubble accuracy, 4 finder angles) + achievement section consolidation. **Bugs fixed**: (1) the header's "X01 Average" bubble (directly under the player's name) silently changed value when toggling the H2H/Practice tab — it read the same mode-filtered `avg` field the tab's own bubble uses; fixed with a new mode-independent `x01Avg` backend field + `HEADER_BUBBLE_KEY_MAP`. (2) The stat-graph (avg chart) tooltip didn't stick on a mobile tap the way the dartboard heatmap tooltip does — a touch's `pointerup` fires its own `pointerleave` immediately; fixed by mirroring the heatmap's touch-vs-mouse handling (a document-level tap-elsewhere/scroll dismiss listener, wired once via a shared "current chart's hide function" pointer, not per redraw). (3) `getPlayerStatBubbles()`'s `pace` (Average Pace) was missing `NOT_CONTINUOUS_STREAM`, unlike its own comment's claimed parity with `getHomeExtra()`/`getMetricHistory()`'s pace — a Chuckin'/Checkout Trainer/Around the World session skewed it sharply faster than reality. (4) `getMetricHistory()`'s `'avgdartsperleg'` case was missing `X01_ONLY`, unlike its documented sibling `getPlayerStatBubbles().avgDartsPerLeg` — a Checkout Ladder/Dead Man Walking leg's dart count leaked into the Darts/Leg chart. (5) `_killerLegOutcomesForPlayer()` included every replayed Killer leg unconditionally, even undecided (in-progress/abandoned) ones with no `state.winner` — diluting `avgKillsPerLeg`/`avgLivesLostPerLeg` and incorrectly crediting `survivedWithoutKillerRate`; `_h2hWonLegs()` already gates the identical replay pipeline on `state.winner`, this sibling consumer was missing the same guard (one existing test's fixture had encoded the bug as expected behavior — fixed the fixture to a genuinely decided leg). (6) Four mobile touch-target/overflow issues: `.drill-btn` (~18px tall → ~36px), `.chart-period-btn` (~27px → ~40px, also used by the dart-weight filters), `.finish-dates` (no shrink/overflow handling — a two-date range could push the Drill button/expand chevron off a phone-width row — now truncates with an ellipsis), `.pp-name` (a long player name overflowed the header with no truncation — now ellipsizes). **Achievement consolidation** (designed via `/frontend-design`): the Badge Case previously showed every game type's badges unconditionally regardless of the profile's own game-type dropdown, unlike the stat bubbles right above it which already scope correctly — `renderPlayerBadges()` now filters to just the selected game type's section (`GAME_TYPE_BADGE_FLAG` maps `playerGameType` to the matching `BADGE_INFO` category flag) plus two always-shown cross-cutting sections (Household Rating, Tournaments) that aren't tied to one game mode. A new `🏆 N/M` lifetime achievement counter sits next to the player's name (`.ach-counter`, a gold pill matching the existing `.navbtn[aria-current]`/`.chart-period-btn.active` bordered-pill language) — always the GLOBAL earned/total count, unaffected by the game-type filter or the new All/Earned/Unearned filter control (`.seg`, reusing the exact component the Finish Rule toggle already uses) added to the Badge Case itself. Committed tests: 5 new/updated (2 in `db.x01-stats.test.js`, 1 in `db.checkout-stat-x01-isolation.test.js`, 2 in `db.killer-stats.test.js`). `REFERENCE.md` §2 (Achievements) updated with the new Badge Case scoping/filter/counter section. Verified live throughout: the header average confirmed unchanged across all three tabs, a real touch-tap confirmed the chart tooltip stays pinned, a 375px-viewport screenshot confirmed the Drill button/long-name truncation fixes, and the achievement counter/game-type filter/earned-unearned filter all confirmed correct via a real player with an earned badge (counter read "3/187"; selecting Cricket hid the X01 section and showed only Cricket + the two cross-cutting sections). Full backend test suite green (1290/1290) | *(no separate roadmap doc — implemented directly)* |
| Ghost Opponent: leg-picker sort + pagination. The New Game "Race a past leg" picker gets a "Sort by" dropdown (Most recent/Best average/Worst average) and a "Per page" dropdown (10/25/50/100, default 10), backed by new `sort`/`offset` params on `getGhostCandidateLegs()` (a whitelisted `GHOST_LEG_SORTS` map, never raw-interpolated) and a new `getGhostCandidateLegsCount()` for page-count/disabled-state. `GET /api/players/ghost-legs` now returns `{legs, total}` instead of a bare array. Committed tests (`backend/test/db.ghost.test.js`); `REFERENCE.md` updated. Verified live: sort-by-best/worst reorder correctly, page size changes reset to page 1, Prev/Next page correctly with no overlap, and re-selecting a leg after paging keeps the right one highlighted | `docs/archive/ghost-opponent-roadmap.md` (new addition, doc already fully archived) |
| Ghost Opponent: "🔁 Repeat this leg" button on the post-match GAME OVER screen, next to New Game/Share — `repeatGhostLeg()` re-calls `raceLeg()` with the just-finished race's own `game.ghostSourceLeg` triple, landing back on New Game with the same leg preselected one Start click away (same "navigate to setup pre-filled" shape as the existing Daily Challenge "Try Again" button, not a silent auto-restart). Hidden for tournament matches. `REFERENCE.md` updated. Verified live end-to-end: played a real ghost race to a checkout, confirmed the button appears, and confirmed clicking it lands on `setup.mode==='ghost'` with the identical `{gameId,setNo,legNo}` preselected | `docs/archive/ghost-opponent-roadmap.md` (new addition, doc already fully archived) |
| Code-quality item 59 — Conventions: badge predicates + DB wrapper boundary. The ten remaining inline `CHAIN_CHECKS` predicates (Hat Trick, Bullseye Gauntlet, Double Trouble, Where'd It Go, So Close, Busted Maximum, Ton-titled to Nothing, No Cigar, Triple Bull, Bullseye Finish) moved to `frontend/scoring.js` as pure functions with committed tests in `backend/test/scoring.test.js`, following the culture-badges precedent. The `DB.*`/`Backend.get()` wrapper boundary settled: dropped the three do-nothing pass-through GET wrappers (`dartComponentOptions`/`listSavedGames`/`getResumeState`) in favor of calling `Backend.get()` directly at their one-off call sites, with the rule (a GET belongs on `DB` only if it does something a bare call wouldn't) written down as a comment above the `Backend`/`DB` definitions | `docs/code-quality-roadmap.md` |
| Code-quality item 58 — Declarative settings field table. `SETTINGS_FIELDS` is now the single table driving both `renderSettings()`'s load and `saveSettings()`'s save, replacing two hand-maintained parallel lists (13 HA/webhook fields, 7 voice checkboxes, 8 misc). Five field `kind`s cover every previous special case, including the zero-vs-unset `admin_lockout_grace` edge case and `ha_url`'s trailing-slash strip. Verified live in a browser: created an admin account, set every field kind, saved, confirmed the server persisted the exact expected values, then reloaded the page fresh and confirmed every field round-trips | `docs/code-quality-roadmap.md` |
| Code-quality item 56 — Dart input/record helpers on the hottest path. `pushThrownDarts()` replaces the double/treble-Miss pad shortcut + dart-construction/zone-stamping copy pasted across `throwDart()`/`throwDartPressureChamber()`/`throwDartCheckoutTrainer()`; `recordSingleDartTurn()` replaces the single-dart `DB.recordTurn()` payload cloned across Killer/Doubles Practice/Just Chuckin' It/guided Around the Clock/World. Verified live in a browser: an X01 pad-mode 180 still scores/records correctly (and fires the 180 achievement, not Hat Trick), and a Killer per-dart throw still commits and updates legDarts/setDarts/gameDarts | `docs/code-quality-roadmap.md` |
| Code-quality item 52 — Small shared-pattern helper batch for index.html. Five helpers: `jsArg()` (the `escapeHtml(escapeJs(...))` composition, ~24 sites), `openModal()` (the innerHTML+unhide+focus tail, 12 modal builders), `registerMilestoneLadders()` (12 identical BADGE_INFO/ACH_LABELS/ACH_DURATION registration loops), `makeCountdownTimer()` (the Checkout Blitz and Pressure Chamber No Warmup wall-clock countdown engine), and `setPressed()` (~9 hand-enumerated aria-pressed segmented-control toggles). Verified live in a browser: modal focus/select behavior, all 12 milestone-ladder groups' badge data, both countdown timers ticking/announcing/expiring correctly, and `setPressed()` toggling the right button pair | `docs/code-quality-roadmap.md` |
| Code-quality item 38 — `savedGamePositionLabel()` now dispatches on `sg.gameType` instead of probing which fields exist on the position object, removing the reserved-field-name contract. Dead Man Walking's `_savedGamePosition()` fields dropped their `dmw`-prefix workaround (`round`/`totalRounds`/`target`/`walkedOutCount`/`dartsUsedThisRound`/`budget` replace the old `dmw*` names) since there's no longer a shared field namespace to collide in. Committed test added to `backend/test/db.saved-games.test.js`; `REFERENCE.md` updated | `docs/code-quality-roadmap.md` |
| Code-quality item 39 — a committed test (`backend/test/db.non-savable-parity.test.js`) pins frontend `NON_SAVABLE_GAME_TYPES` against the backend's derived non-savable set (`KNOWN_GAME_TYPES` minus `SAVABLE_GAME_TYPES`), the "cheap 80% version" the roadmap item called for, so a drifted entry fails loudly in CI instead of silently in production | `docs/code-quality-roadmap.md` |
| Code-quality item 41 — `games.category` as a registry member. Every `GAME_TYPES` entry (including X01, which relied on the old ternary's fallthrough) now declares its own `category(setup, config, startScore)`; `startGame()`'s call site collapses to `GAME_TYPES[gameType].category(...)`, so a future type that forgets to add one throws immediately at game creation instead of the old 14-branch ternary's fallthrough silently writing `category: String(startScore)` — a nonsense label — permanently into `games.category`. Verified live in a browser against every type, then a real Cricket game end-to-end confirming the server-persisted row | `docs/code-quality-roadmap.md` |
| Code-quality item 46 — per-mode option-section wiring as a registry member. New `updateGameTypeOptionSections(gameType)`/`contextsForMode(key)` registry-driven helpers replace `setMode()`/`setGameType()`'s duplicated 4-line option-section toggling and `isSpecialMode`'s 12-way OR chain; `NEW_GAME_MODE_OPTIONS` derives its `contexts` (practice/H2H visibility) from each entry's `soloOnly`/`h2hOnly` flags instead of a hand-maintained parallel list, kept explicit only for the 2 genuinely bespoke entries (challenge, ghost). Deliberately did NOT unify `drillGameTypes`/`drillModes` into an `isDrill` flag — confirmed each excludes a different real subset (Dead Man Walking's distinct leg shape, Marathon diverting before either list is built) for documented reasons, so forcing them into one field would silently change behavior for no simplification benefit | `docs/code-quality-roadmap.md` |
| Code-quality item 53 — game-start construction factory. All 4 construction sites (`startGame()`, `_reallyBeginTournamentMatch()`, `beginMarathonLeg()`, `resumeGame()`) now spread a shared `baseGameRuntimeState()` trailer factory, call `prefetchEarnedBadges(names)`, and (3 of them) call `beginGameSession(webhooks)` for the recordEvent/webhook/render tail. Fixed a real bug in the process: the tournament and marathon literals were missing `atcLastDart`/`atwLastDart`, silently breaking the Around the Clock/World throwbox for games started via those two paths. Verified live: all 5 constructed `game` objects (solo/H2H/resumed/tournament/marathon) now have zero missing trailer keys | `docs/code-quality-roadmap.md` |
| Code-quality item 54 — `advanceLegSetGame()` progression helper. 7 of the 8 `onLegWon*` handlers (Cricket, Baseball, Shanghai, Halve-It, Pressure Chamber, Bob's 27, Killer) now delegate their leg→set→match decision tree to one `advanceLegSetGame(w, opts)`, each mode keeping only its own badge checks and the narrow options it genuinely differs on (gate, checkElo, extraGameWonBadge, momentCard). X01's `onLegWon()` deliberately keeps its own copy — its badge logic (Nerves of Steel/Giant Slayer/Rematch/Grudge Match/Ghost Slayer) is interleaved inside the branches themselves, not cleanly before/after. Verified live across all three branches (leg/set/match-won) for all 7 modes, including each mode's own extra badge/moment-card logic | `docs/code-quality-roadmap.md` |
| Code-quality item 35 — consolidated the 16 `undoLastTurn*` trailers into one shared `_finishUndo(snap, renderFn, {msg, restoreCurrent, resetDarts, push})`, making the previously-silent drift (which modes restore `game.current`, reset darts, or call `pushLive()`) explicit instead of incidental. Doubles Practice's badge-less snapshot shape was folded in too since the badge-revert/cancel calls are verified no-ops on it. Verified live: a real X01 180-then-undo round-trip, plus Killer's custom field restore and Doubles Practice's now-shared trailer | `docs/code-quality-roadmap.md` |
| Code-quality item 47 — `h2hStatsHtml(winner, scope)` registry dispatch. The 5 near-identical `h2hStatsHtml*` functions collapse into one, dispatching each player's stat rows through a new `GAME_TYPES.h2hRows(p, scope)` member (the `legSummary` precedent), with `finishUnit()`'s two parallel 5-way ternary chains collapsing to direct calls. Verified live: all 5 modes at both scopes produce byte-identical output to the originals, including the multi-leg "Standing" row | `docs/code-quality-roadmap.md` |
| Code-quality item 55 — scoreboard/pad renderer scaffolding. `csHeadCellsHtml()`/`csTableInto()`/`roundBannerInto()` are now shared by the 5 chalkboard renderers (Cricket/Baseball/Shanghai/Halve-It/Pressure Chamber); `renderSingleTargetPad()` is shared by the 4 single-target pad renderers (Baseball/Shanghai/Halve-It/Bob's 27); `renderPad()`'s 6-branch dispatch collapses to a `MODE_PAD_RENDERERS` lookup plus one undo-button sync. Verified live: all 5 tables and all 4 pads render with correct structure/content matching the originals | `docs/code-quality-roadmap.md` |
| Code-quality item 48 — declarative personal-bests renderers. 16 of the 17 `renderXxxPersonalBests()` functions are replaced by a per-type spec (`{emptyMsg, stats:[{key,label,fmt,guard,always,truthy}], form}`) consumed by one generic `renderPersonalBestsFromSpec()`; X01's own `renderPersonalBests` stays hand-written since it holds the dispatch shim plus per-stat share/race buttons no other mode has. Verified live against the real Player Profile page across a representative sample including every flagged edge case (Checkout Trainer's non-uniform guard, Around the World's composed two-field row) | `docs/code-quality-roadmap.md` |
| Code-quality items 36+49 (implemented together, 49 supersedes 36) — leaderboard-row template helper. ~25 hand-rolled `rank/score/player/meta` `.hof-row` templates across the Home tab now go through `leaderboardRowHtml()`/`leaderboardSectionHtml(rows, {score, meta, extra, emptyMsg, limit, listStyle})`. `hofSection()` (Top Checkouts' checkout-route expand toggle) and `achievementSection()`'s count-only board stay their own hand-written functions — genuinely different shapes, not oversights. Verified live across every shape (win-rate, MPR/RPI/PPR, Pressure Chamber's nested-meta score, Gauntlet's ascending board, no-meta boards, the uncapped three-dart-average board, and the Household Elo board's custom wrapper spacing) | `docs/code-quality-roadmap.md` |
| Code-quality item 50 — one-shot badge award helper. All 11 hand-rolled `POST /api/badges/award {once:true}` call blocks now go through `awardOnceBadge(player, backendBadgeId, achId, snap, momentOpts, opts)`. Fixed real drift found by inspection: 3 (not 2) sites maintained an `earnedBadgeCache` pre-check, one site (Grudge Match's opponent side) intentionally skips the celebration (`opts.silent`), and 4 sites send a different badge id to the backend than the id used for `queueBadge()`/`fireMomentCard()` — kept as two explicit params rather than silently unified. Verified live against the real backend: normal award, once:true dedup on re-award, `silent`/`cacheCheck` behavior, and null-snap safety | `docs/code-quality-roadmap.md` |
| Code-quality item 42 — live-state keys as a per-mode `modeState` container. `liveSnapshot()` replaces ~22 individual per-mode top-level fields with one opaque `modeState` object built via a new `GAME_TYPES.<type>.liveModeState(game)` registry member (13 modes declare one); `ALLOWED_LIVE_KEYS` allow-lists `modeState` itself as an opaque value, the same "unrestricted shape, sanitized as a whole" treatment `players` already gets. Closes the exact failure class that shipped twice (`docs/bug-roadmap.md` BUG-28's 7 keys, then `killerLives`/`checkoutLadderTarget`/`checkoutLadderVisits`) — a new mode's live fields silently missing the backend allowlist entry while the producer and `display.html`'s reader both already had them — by removing that sync point entirely; a new mode's per-mode live fields now only ever need touching in two places, never `ALLOWED_LIVE_KEYS`. `REFERENCE.md` §7 and every scattered per-mode reference to the old flat key names updated in the same change. Verified live in a browser: pushed real `game` objects for Killer/Halve-It/Pressure Chamber/Cricket through the actual `liveSnapshot()` → `POST /api/live` → `GET /api/live` round-trip against a running server, and confirmed `display.html` rendered each pushed state via its live SSE connection with zero errors. Also fixed two real regressions this refactor exposed: a SEC-26 test's payload shape, and a badge-value-parity test's brace-matching helper (didn't understand `//` comments, was already coincidentally over-scanning by luck) | `docs/code-quality-roadmap.md` |
| Code-quality item 43 — id-keyed killer configs. `games.config.numbers` is now keyed by `players.id` instead of player name, migrated by a one-time boot function (`migrateKillerConfigsToIdKeys()`) that also heals any config a pre-fix rename/merge had already orphaned before converting it. `renamePlayer()` no longer touches `config.numbers` at all (an id never changes on rename) and the old boot reconciler is fully replaced by the migration; `mergePlayers()`'s rewrite (renamed `_rewriteKillerConfigIds()`) and `importPlayerExport()`'s re-key (now reusing the existing `idMap`, dropping the separate `nameMap` it used to need) both survive in simplified form, since a merge intentionally changes which id owns a participation and an import always needs to translate ids across two databases' independent autoincrement histories — neither is actually a compensator for the old bug class. `rebuildKillerState()` (`frontend/scoring.js`) now looks up `numbers[id]` via a `participants` ({id, name}) list instead of bare names; its own replay and `evaluateDartKiller()` stay name-based, matching the live game object. The wire format `createGame()` returns to the client is unchanged, so `frontend/index.html` needed zero changes. Verified live in a browser (client-side numbers still resolve by name; the server-persisted config confirmed id-keyed by direct query). Committed tests: a new `backend/test/db.killer-config-migration.test.js` (legacy-config migration, orphan-healing, idempotence) plus updated assertions in `db.killer-stats.test.js`, `db.merge.test.js`, `db.export.test.js`, and `scoring.test.js` | `docs/code-quality-roadmap.md` |
| Code-quality item 40 — declared `newMatchPlayer`'s second-arg shape on the registry. `cricket`/`doubles_practice`/`checkout_trainer` each declare `ctorArg: 'config'` on their `GAME_TYPES` entry; `startGame()` and `resumeGame()` both read `GAME_TYPES[gameType].ctorArg === 'config'` instead of each hand-maintaining their own (already-drifted) hardcoded type list. Folded into item 37's implementation | `docs/code-quality-roadmap.md` |
| Code-quality item 37 — registry-driven resume dispatch. Every savable `GAME_TYPES` entry now declares a `resume(ctx) => {overlay, statusMsg}` member; `resumeGame()` (`frontend/index.html`) collapses its old 13-branch else-if chain to build players via the type's declared `ctorArg`, call `entry.resume(ctx)`, and spread the returned `overlay` onto the shared `game` defaults — a type with no `resume` member can't reach the resume path at all, alerting "can't be resumed" before ever touching `getResumeState()`'s already-consumed `saved_games` row. The backend mirrors this: every savable `GAME_TYPE_REGISTRY` entry now declares `rebuild(game, participants, turns)` and `position(game, r)` members; `_savedGamePosition()` (`backend/db.js`) collapses its old parallel 12-branch copy to a 4-line generic dispatch. Verified live: a real running server + headless Chromium drove the actual `startGame()`/`throwDart()`/`enterTurn()`/`DB.saveGame()`/`resumeGame()` code paths for all 12 savable types (x01, cricket, baseball, shanghai, halve_it, pressure_chamber, around_the_clock, around_the_world, bobs_27, checkout_ladder, gauntlet, dead_man_walking), confirming resumed state, turn order, and status messages all matched, plus the Saved Games list's position summary for every type. Backend test suite unaffected (1244 tests, 1238 pass, 6 pre-existing unrelated `dart-heatmap` failures). `REFERENCE.md` §23 updated | `docs/code-quality-roadmap.md` |
| Code-quality item 57 — frontend efficiency batch. A new public `GET /api/settings/ha-webhook-status` endpoint (`db.getHaWebhookStatus()`) exposes whether each of the 12 HA webhook events is configured (never the webhook IDs); `sendHaWebhook()` skips its POST entirely when the event isn't configured, and `fireMomentCard()` only JPEG-encodes + sends its ~250KB card image when `momentcard` specifically is configured (the canvas itself still always builds/caches for the in-app Share button). `renderPad()`'s generic 1-20+Bull(+Miss) fallback now builds its button DOM once per game (`pad.dataset.padKind` marker) and toggles `.disabled` instead of tearing down/rebuilding on every dart — the pad's DOM identity resets fresh every new game via `renderGameShell()`, so a stale build can never carry over. `playerSnapshotChuckin()` caches its heatmap serialization, invalidated via a `p.heatmapVersion` counter bumped on every heatmap mutation. Verified live: pad button DOM identity persists across throws/undo while toggling correctly; Chuckin's cache returns the same array reference across same-version calls and rebuilds correctly after a bump; webhook-status gating confirmed skipping the network call when unconfigured | `docs/code-quality-roadmap.md` |
| Code-quality item 51 — badge-progress fetch-baseline-once + profile refetch scoping. Around the World and Doubles Practice's Ring Master badges now fetch their lifetime dart-outcome baseline once at game start (`newMatchPlayer()`'s `atwBaselineHitSet`, `newMatchPlayerDoublesPractice()`'s `baselineHitSectors`) and track this session's own new outcomes locally, instead of re-querying the server every X01 visit / every hit dart. `show('player')` now paints instantly from `stats[currentPlayer]` if already cached instead of unconditionally re-awaiting the full `/api/stats` refresh; of the 17 profile-page loaders, the 9 that don't depend on `playerPageTab`/`playerGameType` now go through a `cachedProfileLoad()` helper serving from a per-player `profileSectionCache` after their first fetch each profile-visit-session (cleared on an actual player switch), while the 7 mode-parameterized ones and badges (deliberately excluded from caching — a mid-session-earned badge must never look missing) still refetch on every tab/game-type switch. A new `GET /api/players/personal-bests-batch` endpoint (`db.getPersonalBestsBatch()`) replaces tournament average-seeding's one-request-per-player fan-out with a single batch call. Verified live: ATW/Ring Master local-set counting confirmed to match server semantics; profile-page network requests confirmed via interception — first open fires each static endpoint once, tab/game-type switches fire zero additional requests for the 9 cached sections, switching players still refetches everything | `docs/code-quality-roadmap.md` |
| Code-quality item 45 — Home page lazy per-combo fetches. `HOME_COMBO_SPECS` (one entry per `GAME_TYPES` id) + `ensureHomeCombo(type, mode)` replace `renderHome()`'s old single ~47-endpoint `Promise.all` — only the tab+game-type combo actually selected fetches, lazily, into the same `homeData` shape every `homeTabRenderer` already reads (no renderer changed). `/api/summary`/`/api/home-extra` (read by every combo) stay fetched every visit. `switchHomeTab()`/`switchHomeGameType()` call `ensureHomeCombo()` for the combo they land on; a selection paints instantly from cache if present, then always kicks a silent de-duped background refetch so a game finished mid-session doesn't freeze that combo's board at its first fetch. Verified live: cold start fires 6 requests instead of ~47; all 16 game-type × tab combos confirmed to fetch only their own endpoints on selection; leaderboard values spot-checked against direct endpoint calls matched exactly | `docs/code-quality-roadmap.md` |
| Halve-It custom target editor (item 19) — the v2 split-out of the core Halve-It game, letting a player replace the classic 7-round default (`config.targets`) with their own sector+ring sequence via a "customize targets" screen reusing Cricket's custom-numbers picker pattern. New Game setup gets a Classic/Custom toggle (`#halve-it-options-section`); Classic omits `config.targets` (server falls back to `HALVE_IT_DEFAULT_TARGETS`), Custom reveals a per-round editor building an ordered 1–20-round sequence of `{sector, ring?}` rows — a native `<select>` for the sector (Bull 25, or 20…1) and one for the required ring (Any / Single / Double / Treble, Treble hidden for Bull, and a Bull+Treble pick snapped back to Double client-side); a custom set makes the game's category label read "Custom Halve-It". `createGame()` (`backend/db.js`) validates and normalises `config.targets` server-side — array of 1–20 entries, sector 1–20 or 25, ring single/double/treble, treble-Bull rejected as unwinnable, extra fields stripped; frontend wiring (`setHalveItPreset`/`addHalveItTargetRow`/`updateHalveItTarget`/`renderHalveItTargetRows`/`resolveHalveItTargets`) in `frontend/index.html`; committed validation tests in `backend/test/db.halve-it-stats.test.js`; headless-verified the editor renders, seeds from classic, adds/edits/removes rows, snaps treble-Bull to double, and resolves classic→omit vs custom→list. Completes the Halve-It roadmap, now moved to `docs/archive/` | `docs/archive/halve-it-roadmap.md` |
| Pressure Chamber self-declare honesty mechanic (item 32) — the v2 split-out of the core Pressure Chamber game (item 28): a before-the-throw self-declaration and an informational Honesty% stat. A new nullable `turns.declared_hit` column (`1`=declared hit, `0`=declared miss, `NULL` otherwise, purely additive like `target_score`/`declared_unsolvable`/`affected_player_id`); a declare screen (`renderPadPressureChamber()` shows two declare buttons and hides the number pad / S·D·T multi-row until a call is made, No Warmup's 5-second clock only arming once the call is committed) driven by `declarePressureHit()` + transient per-visit `game.pressureDeclared` state (reset each turn, never saved/resumed, restored by undo); `enterTurnPressureChamber()` sends `declaredHit` to `DB.recordTurn()`; `getPressureChamberStatBubbles`' new `honestyPct`/`declaredRounds` compare each declaration against the round's real outcome (declared hit honest iff `checkout=1`, declared miss iff `bust=1`), surfaced as a new "Honesty %" Player-Profile bubble. Explicitly NOT a scoring input and carries no leaderboard weight; the one part of the design that can never be made tamper-resistant by a single atomic write (a determined client can submit a declaration matching the outcome in hindsight), so it ships as a best-effort honor-system signal with **no consistency guard** — `addTurn()` validates only the `0`/`1` shape and rejects it outside `pressure_chamber` games, the same gating `declared_unsolvable` uses for Checkout Trainer. Resolves the roadmap doc's own "does Honesty% feed the Composure Rating" open question in favour of **informational only**. Committed tests in `backend/test/db.pressure-chamber-stats.test.js` (honest→100%, mixed→50%, undeclared→null, plus the game-type/shape guards); headless-verified the full declare→throw→next-round flow end-to-end (declare buttons show, pad appears after the call, declaration resets each round, Honesty% computes to 50% on a mixed pair). `REFERENCE.md` §34 updated. Completes the Pressure Chamber roadmap, now moved to `docs/archive/` | `docs/archive/pressure-chamber-roadmap.md` |
| Tournament mode: double-elimination bracket support (item 13) — losers bracket + grand final/reset logic layered on the shipped single-elimination, on the same schema (the `winner_next_*`/`loser_next_*` pointer pair). Restricted to exact powers of two (4/8/16/32/64/128, `TOURNAMENT_DOUBLE_ELIM_COUNTS`) per the roadmap's §2 de-risking, so there are zero byes and no cascading-bye complexity. A shared `doubleElimStructure(k)` (`frontend/scoring.js`, used by both backend generation and the frontend format table so counts/labels can't drift) lays out `k` winners rounds, `2k−2` alternating minor/drop losers rounds, and the grand final + conditional reset decider (`3k` rounds, `2N−1` matches). `_generateDoubleElimBracket()` (`backend/db.js`) wires the pointer pairs; `_advanceTournamentMatch()` drops a winners-bracket loser via `loser_next_match_id` (rather than eliminating) and `_resolveGrandFinal()` handles the bracket reset — if the losers-bracket champion (grand-final slot 2) wins game one, both finalists hold one loss and the pre-created reset match is played; if the winners-bracket champion wins game one, the tournament ends. Giant Slayer is now awarded per real match (so a winners-bracket upset counts even though that loser only drops), except never on a game-one grand-final loss that triggers a reset. Frontend: a Single/Double toggle on the setup screen (double-elim blocked unless the count is a valid power of two; per-round format table derives from `doubleElimStructure`), plus a functional grouped-column bracket view (Winners / Losers / Grand Final sections; single-elim renders unchanged). `getTournamentStats()` reports best-finish from the stored round label so a double-elim "Losers Final"/"Grand Final" reads correctly. Committed tests in `backend/test/tournament-double-elim.test.js` (structure, generation, validation, a full play-through forcing a bracket reset, the no-reset path, an 8-player play-to-completion proving no dead-end pointers, and best-finish labels); headless-verified the setup toggle, power-of-two gating, and the grouped bracket view. `REFERENCE.md` §15 updated. The winners/losers-**tabbed** visual tree (build-order step 3) shipped separately as item 33 (its own ledger row below), completing the tournament roadmap — now moved to `docs/archive/` | `docs/archive/tournament-mode-roadmap.md` |
| Tournament mode: tabbed visual bracket view (item 33, build-order step 3) — the winners/losers-tabbed double-elimination bracket view that finishes tournament mode. `renderTournamentDetail()` (`frontend/index.html`) now renders a **Winners / Losers / Grand Final** tab switcher (`.tourney-tabs`, `role="tablist"`) for double-elim, showing one bracket panel (`role="tabpanel"`) at a time so the deep double-elim tree — up to ~19 rounds combined at 128 players — stays readable rather than one long stacked scroll (the roadmap's §4 "two scrollable panels with a tab switcher"). `tournamentBracketTab` holds the active tab (module-level, persists across the re-renders an action triggers, reset in `openTournament()`); `setTournamentBracketTab()` re-renders from `tournamentDetailCache` with no network round-trip; `tournamentTabKeydown()` gives the tablist standard roving-tabindex WAI-ARIA keyboard support (ArrowLeft/Right + Home/End move both selection and focus). Single-elimination keeps its single-column view with no tabs (unchanged). The always-visible "Up Next" list and the linearized "Full bracket (list view)" remain the non-spatial way to follow either bracket type. Headless-verified: the double-elim tabs render with correct ARIA/roving-tabindex, switching tabs swaps the panel to that bracket's rounds, keyboard ArrowRight moves selection+focus, and single-elim renders without tabs. `REFERENCE.md` §15 and README updated. Completes the tournament roadmap → archived | `docs/archive/tournament-mode-roadmap.md` |
| Saved games / pause & resume — `saved_games` context table (`game_id UNIQUE ... ON DELETE CASCADE`, per CLAUDE.md's convention); "⏸ Save for later" in both Pad and Dartboard modes (header + footer, survives the leg-complete transition screen); one saved game per (participant set, game type); a resume-or-abandon prompt at New Game Start time plus a browsable Saved Games list (both driven by `GET /api/saved-games`, whose one-line position summaries reuse the real rebuild functions); resume via `rebuildX01State()`/`rebuildCricketState()`/`rebuildBaseballState()`/`rebuildAroundTheClockState()`/`rebuildAroundTheWorldState()` (`frontend/scoring.js`) — pure functions replaying every recorded turn through the same `evaluateVisit*`/`evaluateDartAroundTheClock` engines that scored it live, including a trailing-leg-win edge case (saved before "Next leg" was tapped); a two-device divergence guard (`GET .../resume-state` atomically consumes the pause); tournament/league-fixture matches savable, with tournament-linked abandons routed to the bracket's walkover control instead of a plain delete; player-deletion guard; a merge-collision block (`_savedGameCollisions()`); committed tests (`backend/test/scoring.test.js`'s rebuild-function suite plus `backend/test/db.saved-games.test.js`'s full endpoint/guard coverage) | `docs/archive/saved-games-roadmap.md` |
| Guided Around the Clock / Around the World practice drill mode — two new game types (`around_the_clock`, `around_the_world`) turning the existing passive completion tracking into active drills with live progress feedback, reusing the Around the World Progress grid's UI investment; new `guided_clock`/`guided_world` badges; Home page leaderboards; committed tests | `docs/game-modes-roadmap.md` |
| Tournament mode: single-elimination (schema, bracket generation with cascading byes, match lifecycle/walkover, setup screen, bracket tree + Up Next view, live-scoreboard round label, player-deletion guard, committed tests) — double-elimination remains a separate open item above | `docs/archive/tournament-mode-roadmap.md` |
| Tournament: two new badges — 🏆 Champion (win a bracket) and ⚔️ Giant Slayer (Tournament) (beat an opponent seeded 3+ slots better), both awarded inline from `_advanceTournamentMatch()`, live-celebration detected via an `earnedBadgeCache` diff since neither the award nor the completion hook has a response channel back to the frontend; committed tests | `docs/archive/tournament-mode-roadmap.md` |
| Tournament: "Tournaments" stat block on the Player Profile (wins, runner-up count, best finish reached) — `getTournamentStats()`, `GET /api/players/tournament-stats`, committed tests | `docs/archive/tournament-mode-roadmap.md` |
| League mode (schema — `leagues`/`league_players` plus a nullable `games.league_id`; live-computed standings with no maintained tally; an `onGameCreated` auto-tag hook with a New Game "log to league?" picker for genuine multi-league ambiguity; season lifecycle (active/ended, reversible); a Leagues nav tab (list/setup/detail); a Home page teaser; a Player Profile "Leagues" stat block; committed tests) — X01 and Cricket support, and league fixtures/pending matches, both now shipped, see below | `docs/archive/league-mode-roadmap.md` |
| Dartboard zone/miss/bounce-out tracking — `darts.zone`/`miss_zone`/`miss_depth`/`bounced` columns; the generalized dartboard heatmap (`getDartHeatmap()`, `GET /api/players/dart-heatmap`) now shown on all four game-type Player Profile tabs instead of Just Chuckin' It only, with a "zone unspecified" hatch overlay for Pad-mode/pre-feature singles; Dartboard mode's flat Miss button replaced by a two-band (near/far) positional miss ring; a v1 flat-count "Bounce Out" button in every game type and both input modes except Checkout Trainer, which hides it (along with Miss and Undo Last Turn) as a live-match-only control (`getBounceOutCount()`, `GET /api/players/bounce-outs`) — v2 positional bounce-out capture remains gated on `docs/camera-scoring-roadmap.md`; committed tests, verified end-to-end with Playwright | `docs/archive/dartboard-zone-tracking-roadmap.md` |
| Coaching Insights — weak-number, checkout-route, bust-parity, and form-trend insights (`getCoachingInsights`, Player Profile X01 tab, committed tests) | `docs/archive/coaching-insights-roadmap.md` |
| Home Assistant automation recipe book (`docs/home-assistant-recipes.md`, linked from README) | `docs/archive/ha-recipes-roadmap.md` |
| Colorblind-friendly palette | `docs/archive/colorblind-mode-roadmap.md` |
| Admin login rate limiting + `OCHE_REQUIRE_AUTH` zero-trust default; SEC-7 webhook auth | `docs/security-hardening-roadmap.md` |
| Full-database admin JSON export (Settings → Data Export) | `docs/archive/data-export-roadmap.md` |
| Per-player data export AND import (admin-only, JSON) — "Export a player…"/"Import a player" on the same `#screen-player-export` admin page (Settings → Data Export); `getPlayerExport()`/`GET /api/players/export` bundles games/turns/darts for every game the player is in, including opponents' turns within those same games (H2H isn't stored anywhere, only derivable) plus minimal `{id,uuid,name}` opponent stubs; new `players.uuid` (v4 UUID) portable identity backing it; `importPlayerExport()`/`POST /api/players/import` resolves players by uuid first (uniquifying a name collision rather than merging), inserts games/turns/darts directly (bypassing lifecycle hooks), and skips already-present games by fingerprint so re-import is a safe no-op and an opponent stub upgrades in place when their own export is imported later; committed tests plus a real two-independent-server browser verification | `docs/archive/data-export-roadmap.md` |
| Per-player CSV export ("your own stats as a spreadsheet", admin-only) — `getPlayerCsvExport()`/`GET /api/players/export-csv?name=...&kind=games|turns`, "Games CSV" (one row per game with player-scoped aggregates: points, avg/turn, best turn, busts, checkouts, highest checkout, opponents, result) and "Turns CSV" (one row per turn thrown, with per-dart `T20 S5 D16` notation) buttons on the same `#screen-player-export` admin page; deliberately non-portable (no uuids, no opponents' turns, no import path — the JSON export above is the portable one); RFC-4180 encoding with CSV-formula-injection neutralization for hostile player names; committed tests — the last of this doc's items, doc now archived | `docs/archive/data-export-roadmap.md` |
| Player merge tool (admin-only) — `getMergePreview()`/`mergePlayers()`, `GET /api/players/merge-preview` + `POST /api/players/merge` (rate-limited, logged), Settings → Admin & Danger Zone → Merge Players with a REQUIRED preview modal (per-table move counts, auto-resolutions, ⛔ conflict list); reassigns every FK-into-`players` table in one transaction (including `league_fixtures` with its canonical player1<player2 ordering re-established, a table added after the doc's original inventory); hard-blocks on a shared game/tournament/league or an ambiguous same-day Daily Challenge pair; auto-resolves shared badges (MAX count, MIN earned_at) and exactly-one-completed challenge pairs; target's name/finish rule/PIN/uuid always win; new `player_uuid_aliases` table + `resolveStub()` fallback keeps old exports of the merged-away player importable onto the survivor (chained merges resolve in one hop); committed tests — the doc's only item, doc archived | `docs/archive/player-merge-roadmap.md` |
| Standalone `backend/admin-recovery.js` CLI (`list`/`reset-password`/`clear-lockout`) for a forgotten admin password or a stuck lockout via direct filesystem/container access; `changeAdminPassword()` now also clears lockout | `docs/archive/admin-account-recovery-roadmap.md` |
| Progressive admin-login lockout delay — replaces the flat 5-minute lockout with a doubling-per-failure backoff past a grace window, never fully blocking a correct password | `docs/archive/admin-login-backoff-roadmap.md` |
| Voice announcements (browser speech synthesis call-outs) | `docs/archive/voice-announcements-roadmap.md` |
| Backups v1: `backend/backup.js` script, retention pruning, documented restore procedure | `docs/archive/backups-roadmap.md` |
| Backups v2: Settings → Backups (download/retention/on-demand backup/restore-from-existing/upload-restore) | `docs/archive/backups-roadmap.md` |
| Backups: opt-in Docker Compose-profile sidecar (`docker-compose.yml`'s `backups` service, `profiles: ["backups"]`) | `docs/archive/backups-roadmap.md` |
| Shareable Moments: card generation, every trigger point (achievements, match win, Personal Bests), HA webhook | `docs/archive/shareable-moments-roadmap.md` |
| Shareable Moments: Player Profile "Moments" gallery (found already built, 2026-07 — every earned Badge Case tile has a Share button, `shareEarnedBadge()`) | `docs/archive/shareable-moments-roadmap.md` |
| Shareable Moments — every item, now archived (card generation + Moments gallery above; X auto-post explicitly rejected, not a gap) | `docs/archive/shareable-moments-roadmap.md` |
| 22 of 22 achievements/badges shipped (21 original candidates + Staircase Finish) | `docs/archive/achievements-badges-roadmap.md` |
| Checkout Trainer: difficulty tiers (Under 40 / Under 100 / Over 100 / Full Range 2-170), a session-scoped setup-screen toggle baked into `games.config.difficulty`; committed tests | `docs/archive/checkout-trainer-roadmap.md` |
| League mode: Cricket support (`leagues.game_type` column alongside X01, a setup-screen game-type selector, X01/Cricket cross-isolation in the auto-tag hook and `getEligibleLeagues()`, standings computation unchanged since it was already game-type-agnostic; committed tests) | `docs/archive/league-mode-roadmap.md` |
| League fixtures / pending matches — a new `league_fixtures` table (`game_id` FK into `games`, status derived not stored, same precedent as `tournament_matches`), single round-robin generation at league creation and again for new pairings on enrollment, `GET /api/leagues/pending-fixture` (order-independent pending-fixture lookup by player pair), `createGame()`'s `leagueFixtureId` (explicit fixture-to-game linking that bypasses the fuzzy `onGameCreated` auto-tag hook), and a read-only Fixtures list on the League detail screen; committed tests | `docs/archive/league-mode-roadmap.md` |
| New Game page revamp — the single all-controls-visible setup screen replaced by a 3-step wizard (Who's playing? → Choose a game → More options, Back buttons restoring not resetting prior-step state, `aria-live` step announcements + focus management); Step 1's select → "Add someone else?" loop reuses the existing player-add handlers unmodified; Step 2's one flat, player-count-filtered `NEW_GAME_MODE_OPTIONS` dropdown (including Baseball, shipped after this doc was first written) replaces the old Mode row/Practice-type sub-toggle/X01-Cricket-Baseball toggle, with Daily Challenge's already-attempted check moved to selection time and a how-to-play blurb per entry; Step 3 relocates every mode's existing options block unmodified; the "League Game" top-of-dropdown entry consumes `GET /api/leagues/pending-fixture` and `createGame()`'s `leagueFixtureId` (both shipped above); the old H2H record banner and category-based "log to league?" picker were both deleted as dead code (`GET /api/players/h2h` HTTP route removed with it, `getH2HRecord()` itself kept — still used internally); every mode verified end-to-end with ad hoc Playwright against a live server (no committed Playwright suite exists in this repo yet) — **superseded by the 2026-07 reorder below** | `docs/archive/new-game-flow-roadmap.md` |
| New Game page reorder (2026-07) — flipped the wizard order again to **Choose a game → Who's playing? → More options**; Step 1's flat dropdown replaced by a categorized "chalkboard ledger" picker (`GAME_LEDGER_CATEGORIES`: Traditional/Practice & Drills/Solo Challenges/Head-to-Head Only/Special Modes, one monoline SVG icon per category, an accordion open to whichever category holds the current selection) plus a `.challenge-spotlight`-styled Daily Challenge section above it (reuses the existing `GET /api/challenges/today-board` endpoint — no new backend endpoint needed — listing every player who's already completed today's challenge format with a ✓); player-count caps now derive from the *chosen game* instead of the reverse (`maxPlayersForSetup()`/`resyncSetupModeForPlayerCount()`): solo-only modes force exactly 1 player, X01 H2H stays capped at 2, every other dual-capable/H2H-only type allows the existing global 6-player cap; League Game moved from a Step 2 dropdown entry to a reactive Step 2 opt-in banner (re-validates the pending fixture on every player-pair change, since a fixture is tied to a specific pair unknowable before players are picked); Daily Challenge's per-player "already attempted today" check moved to Step 2 (targets the one chosen player, distinct from Step 1's all-players informational list); verified live via Playwright (Cricket 3-player, X01/Gauntlet truncation, Killer's existing min-2 floor) and the full 1301-test backend suite with zero regressions (no backend code touched — 100% frontend). 2-4 player H2H X01 deliberately NOT included — split out as item 71 (`docs/archive/multiplayer-x01-roadmap.md`, since shipped), with the stats-handling question (merge into existing H2H win-rate) already resolved there | `frontend/index.html`, `REFERENCE.md` §20 |
| New Game page: fixed two visible Continue buttons stacking on Step 2 the instant every player slot was filled (`renderPlayers()`'s own `promptHtml` used to render a second, full-width Continue button on top of the always-present static one) — `#setup-step2-continue` is now the sole Continue action for every fill state; added a subtle "VS" chip (`.setup-ledger-vs`, non-color-only via `aria-label`) next to every ledger row whose `chosenGameContexts()` includes `'h2h'`, after exploring 5 distinct visual directions via `/frontend-design` and having the owner pick; then folded the entire "Step 3: More options" page into Step 1 itself (a direct request, not a planned roadmap item) — every per-mode option that doesn't need to know who's playing (X01 starting score, Cricket targets/variant, Killer lives, Shanghai rounds, Halve-It preset, Checkout Trainer sub-mode/difficulty, Doubles Practice targets, H2H Format) now renders inline inside whichever ledger row is selected (`mountSetupInlineOptions()` physically relocates the existing option-section DOM nodes rather than duplicating their handlers), each row also gaining a collapsed-by-default "Rules" disclosure reusing the existing `blurb` text instead of a separate always-visible box; a `position:fixed` Continue bar now stays pinned to the bottom of the viewport the whole time Step 1 is showing, fixing the "have to scroll to find Continue" complaint; Ghost's leg picker and the Handicap disclosure couldn't move (they need to know who's playing) so they stay reactive Step 2 sections instead; the wizard is now 2 steps, with Step 2's Continue (`setupFinishAndStart()`) doubling as Start; verified live via Playwright across every mode (options mount/hide correctly, Killer's 2-player floor, Ghost/Handicap reactivity, the `raceLeg()`/`drillCheckout()` preset entry points landing on the right step) and the full 1304-test backend suite with zero regressions (100% frontend) | `frontend/index.html`, `REFERENCE.md` §20 |
| New Game page follow-up: a mode with nothing in its inline options (Bob's 27, 121 Checkout Ladder, Chuckin, ATC/ATW, Gauntlet, Dead Man Walking, Marathon, Ghost) now auto-expands its Rules disclosure on selection (`keyHasInlineOptions(key)`) instead of leaving nothing new to look at; the `position:fixed` sticky Continue bar from the Step-3 fold above was replaced with `#setup-step1-continue` rendered inline right after the Rules disclosure, inside whichever row/card is selected, per a direct follow-up request; Daily Challenge's spotlight card gained the same filled check-circle (`.setup-dc-selectcheck`) every ordinary row already used on selection — the card's own permanent gold border/glow made an earlier "extra inset shadow" selected-state nearly invisible against it, explored via `/frontend-design` (5 directions) before the owner picked the check-circle — plus its own Rules disclosure and inline Continue, matching ordinary rows; also added an "X01 mode" filter (All/501/301/170/101) to the Ghost leg picker's toolbar (`GET /api/players/ghost-legs`'s new `category` param, `GHOST_LEG_CATEGORIES` whitelist, committed test coverage), with the toolbar staying visible even when a filter comes up empty so the player can switch modes without losing the picker; verified live via Playwright across every mode plus the DC-select/deselect and Ghost-filter-empty cases, and the full 1305-test backend suite with zero regressions | `frontend/index.html`, `backend/db.js`, `backend/server.js`, `REFERENCE.md` §20 |
| New Game Step 2 ("Who's playing?") redesign: fixed a real bug where Ghost mode (a solo race against your own past leg) showed the "h2h" average for any player who'd ever played a real head-to-head match elsewhere — the average source is now gated on `setup.mode==='h2h'` (an actual head-to-head game), not on whether h2h stats merely exist for that player; redesigned the per-player stat line into a right-edge "rail" (`.player-rail`, explored via `/frontend-design`, 5 directions, owner picked this one) with a large Bebas Neue numeral and a spelled-out trend line ("▼ 5.0 recent") instead of an 11px line with a bare, unexplained arrow+delta; redesigned the Ghost leg picker's rows into a fixed-height 4-column grid (date/category+VS-chip/average/darts) so nothing wraps onto a second line on an iPhone-width screen the way the old plain flex-row buttons did; verified live via Playwright (h2h-vs-avg fix, row heights) and the full 1305-test backend suite | `frontend/index.html`, `REFERENCE.md` §20 |
| Badge award count threaded into the live achievement overlay (`patchAchievementCount()`, both `index.html` and `display.html`), not just the shareable moment card | `docs/archive/achievements-badges-roadmap.md` |
| Expanded Achievements & Badges — every item, now archived (all 22 badges + the notifications/count work above) | `docs/archive/achievements-badges-roadmap.md` |
| Simultaneous-achievements overlay fix (multi-badge queue) | `docs/archive/simultaneous-achievements-roadmap.md` |
| Daily/Weekly Challenge — all 6 formats, streak tracking, shareable card, Player Profile history tab, 3 challenge-specific badges | `docs/archive/daily-challenge-roadmap.md` |
| Ghost Opponent (X01 only) — leg-script replay, New Game leg picker, opponent-badge suppression (win/loss tracking is a separate, not-yet-built open item above) | `docs/archive/ghost-opponent-roadmap.md` |
| Server-side error logging + persistent rotating log + Settings "Server Errors" view (Part A); committed `node:test` suite + CI (Part B) | `docs/testing-and-observability-roadmap.md` |
| Colorblind mode, WCAG contrast audit fixes, `aria-live` announcements, accessible-input-path framing, type-size pass (standing checklist, not archived — ongoing per CLAUDE.md) | `docs/accessibility-roadmap.md` |
| Mobile: phone-responsive CSS pass (step 1) | `docs/mobile-app-roadmap.md` |
| All 14 security-audit findings (SEC-1 through SEC-14) fixed | `docs/security-audit-roadmap.md` |
| All 3 functional-defect findings (BUG-1 through BUG-3) fixed | `docs/bug-roadmap.md` |
| BUG-8: static frontend files served with no `Cache-Control` header, plus an unguarded `await DB.loadAll()` in the boot sequence — together could make a live server with intact data look fully broken to a user on a stale-cached device (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-16: `importPlayerExport()`'s duplicate-game guard correctly skipped re-inserting an already-imported game, but not its turns/darts underneath it — re-importing the same player export doubled their darts/turns silently (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-17: Ghost mode's past-leg picker (and race label) showed "Invalid Date" — two call sites read a raw SQLite timestamp straight into `new Date()` instead of using the sanitization pattern already established elsewhere in the same file (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-18: Ghost mode incorrectly offered a "Next leg" button instead of ending the race (game.practice being unconditionally true for Ghost mode blocked the set/match-win gate), and never marked its game complete server-side as a result (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| SEC-25 (seventh-pass audit): `addTurn()`'s SEC-22 scored-vs-darts consistency guard was never extended to Baseball, so a hostile client could poison every Baseball stat with a `scored` the darts can't produce (max real runs per visit is 9, but only the X01-shaped 0-180 range guarded it) — now derives the inning server-side and rejects a mismatch | `docs/security-audit-roadmap.md` |
| BUG-19 (seventh-pass audit): `getPlayerExport()` built one SQL bound variable per turn/dart, so exporting a player with more than ~32k turns threw "too many SQL variables" and 500'd — now batches the `IN (...)` reads | `docs/bug-roadmap.md` |
| BUG-20: Just Chuckin' It's live scoreboard heatmap lit up both single regions (inner/outer) of a number for any single hit — the live session tally and `buildChuckinLiveHeatmap()` were zone-blind even though dart recording and the lifetime Player Profile heatmap were already zone-aware (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-21: re-sharing an already-earned badge from the Badge Case produced a moment card with no explanation at all — `shareEarnedBadge()` never read the badge's own `desc` field; every card-building path now resolves a real explanation via `achDescFor()` (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-22: practice-mode Baseball games never called `DB.completeGame()` (a `!game.practice` gate copy-pasted from X01/Cricket's open-ended-practice template, which Baseball's own `completed_at`-dependent stat functions can't tolerate) — Games Played/Win Rate/every Personal Best stayed empty no matter how many practice games were played; practice Baseball now completes after its single forced leg, like Ghost mode (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-23: Cricket's scoring pad had no way to log a real off-target number hit (e.g. 1-14 in classic Cricket) — every one was forced through `Miss`, corrupting Dart Analytics' sector/treble-rate stats; added a collapsible "hit a different number" picker that records the real sector/multiplier instead, with zero scoring-logic changes needed (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| BUG-24: Cricket's (and Baseball's) lifetime dartboard heatmap silently hid every single hit, including on real target numbers, because `buildDartHeatmap()`'s "zone-unspecified single: excluded" rule assumed that always meant a Pad-mode choice — untrue for these two game types, which have no Dartboard-mode input at all; both single sub-regions now merge into one honest total instead (found while verifying BUG-23's fix) | `docs/bug-roadmap.md` |
| BUG-25: the New Game wizard's Step 2 "Continue" always advanced to Step 3 ("More options"), even for modes with nothing to configure there (X01/Baseball practice, Chuckin, Around the Clock, Around the World, Daily Challenge) — landing on an effectively blank page requiring an extra click; `setupGoToStep3()` now skips straight to `startGame()` when none of Step 3's five conditional sections are visible, leaving Cricket/Ghost/Doubles Practice/Checkout Trainer/H2H unaffected (found via a live bug report, not an audit pass) | `docs/bug-roadmap.md` |
| Game Modes step 1: X01-to-plugin refactor, no behavior change | `docs/game-modes-roadmap.md` |
| Game Modes step 2: Cricket engine + customizable numbers, dedicated scoring screen, live scoreboard | `docs/game-modes-roadmap.md` |
| Game Modes step 3: Cricket stats parity (MPR, 9-Marks, Personal Bests, metric history, 2 achievements) | `docs/game-modes-roadmap.md` |
| Game Modes step 4: Home/Player Profile game-type navigation (X01/Cricket toggles + Cricket leaderboard set) | `docs/game-modes-roadmap.md` |
| Game Modes step 6: generalized N-way Player Profile/Home page game-type toggle mechanism | `docs/game-modes-roadmap.md` |
| Game Modes step 7: Doubles Practice (per-dart evaluation, stats, Personal Bests, undo, Home leaderboard set) | `docs/game-modes-roadmap.md` |
| Game Modes step 8: 101 as a fourth X01 starting score | `docs/game-modes-roadmap.md` |
| Game Modes step 9: Just Chuckin' It (heatmap-first stats, 18 laddered milestone achievements) | `docs/game-modes-roadmap.md` |
| Game Modes step 5: Baseball — core playable game (turn engine, fixed-9-innings New Game setup, dedicated scoring screen, `renderers.baseball` live scoreboard, 16-case committed unit suite for the scoring/round/match-completion rules). Stats/achievements parity split out as its own open item (#12) | `docs/game-modes-roadmap.md` |
| Game Modes: Baseball — Player Profile stats parity (RPI/Perfect Innings/Win Rate/Games Played/Darts Thrown/Best Inning stat bubbles, 5-field Personal Bests, 6 metric-history cases, `getBaseballWonLegs()`'s query-time leg-winner derivation in place of a stored `leg_won` signal) | `docs/game-modes-roadmap.md` |
| Game Modes: Baseball — achievements (🔥 Perfect Inning, 🏆 Perfect Game), matchwin moment card + Share button + per-leg comparison table (`h2hStatsHtmlBaseball()`), and Home page leaderboards (RPI/Perfect Innings/Wins/Perfect Game, `renderHomeTabBodyBaseball()`) — the last open item (#12) for Baseball, full stats/achievements parity now shipped (a `pracStatsHtmlBaseball()` practice-mode panel shipped alongside this too, later removed by BUG-22 once practice Baseball was fixed to always complete in 1 leg) | `docs/game-modes-roadmap.md` |
| Orientation-aware live scoreboard (portrait vs. landscape), built ahead of Cricket's scoreboard | `docs/archive/existing-app-prep-roadmap.md` item 11 |
| Stats query scope helper (`_scope()` in `backend/db.js`), now used by every stat query in the file | `docs/archive/existing-app-prep-roadmap.md` item 1 |
| Player-deletion-guard extensibility (`registerDeletePlayerGuard`) | `docs/archive/existing-app-prep-roadmap.md` item 6 |
| Game-lifecycle hook mechanism (`onGameCreated`/`onGameCompleted`) | `docs/archive/existing-app-prep-roadmap.md` item 4 |
| Settings page regrouping into 4 tabs (Account & Access, Gameplay & Display, Integrations, Admin & Danger Zone) | `docs/archive/existing-app-prep-roadmap.md` item 7 |
| `games.game_type`/`games.config` columns | `docs/archive/existing-app-prep-roadmap.md` item 2 |
| Context tables link into `games` via FK, never a new boolean on `games` (adopted convention) | `docs/archive/existing-app-prep-roadmap.md` item 3, see `CLAUDE.md` |
| Docker Compose profiles for future optional services (adopted convention) | `docs/archive/existing-app-prep-roadmap.md` item 9 |
| `throwDart(sector, multiplier)` input-source separation, confirmed protected through the plugin refactor | `docs/archive/existing-app-prep-roadmap.md` item 5 |
| Preparing the Existing App for Future Roadmaps — all 11 of its own items | `docs/archive/existing-app-prep-roadmap.md` |
| Dart Builder — every item, now archived: v1 (`dart_components`/`loadouts` schema, component/loadout CRUD, `game_players.loadout_id` game-creation integration, PIN-gated default-loadout + loadout-customization actions, per-loadout stats, Dart Builder screen, Player Profile "Default Loadout" selector, New Game "Change Loadout" picker), the loadout comparison view ("⚖️ Compare Loadouts", side-by-side stats via the existing `getLoadoutStats()`, no new backend query), the accessibility icon set (barrel shape/grip + flight shape as an accessible icon-button group, closing the "torpedo"/"knurled"/"kite" self-explanatory-name gap), and "⚡ Quick Add Full Set" (name + every barrel/shaft/flight field on one screen, one save). Optional photo upload per component was considered and explicitly dropped, not built — it was framed as an alternative to the icon set, not additive to one | `docs/archive/dart-builder-roadmap.md` |
| Cricket badge parity: Night Owl/Early Bird now fire from Cricket turns too, via a shared `awardTimeOfDayBadges(p)` helper called from both `enterTurn()` and `enterTurnCricket()` — previously X01-only by accident of code structure, not design | `docs/game-modes-roadmap.md` |
| Cricket-native badges: 🧹 Whitewash (won without the opponent closing a single number) and 🔥 Comeback Kid (Cricket) (won after trailing on points by 20+, `p.legWorstPointsDeficit` tracked per-visit in `enterTurnCricket()`) — both 2-player only, both checked in `onLegWonCricket()`, both with pure trigger-condition functions (`isCricketWhitewash()`/`cricketComebackAchieved()`) unit-tested in `backend/test/scoring.test.js` | `docs/game-modes-roadmap.md` |
| Ghost Opponent — every item, now archived: the original race feature, race win/loss tracking (`ghost_races` table, `recordGhostRace()`/`getGhostRaceRecord()` server-side re-validating the source leg so a client can't fabricate a fake win history, `POST /api/ghost-races` + `GET /api/players/ghost-race-record`, a "👻 Ghost races: W–L" line on the Player Profile), and the 👻 Ghost Slayer first-win badge (awarded inline from `recordGhostRace()`, idempotent via `awardBadge()`'s existing `once` mode) | `docs/archive/ghost-opponent-roadmap.md` |
| Checkout Trainer, both sub-modes (schema — `turns.target_score`, `games.config.mode`/`durationSec`, the generalized `NOT_HYPOTHETICAL_DARTS` physical-stat exclusion; `pickCheckoutTarget()`/`gradeCheckoutAttempt()` pure grading logic; untimed Freeform plus the 60-second Checkout Blitz sprint with its wall-clock countdown and server-derived scoring; `getCheckoutBlitzLeaderboard()`; 4 Freeform + 1 Blitz milestone ladders (28 tiers) plus 5 one-off badges, all reusing the existing Chuckin milestone engine; New Game screen integration; Home page teaser + Player Profile stat bubbles/Personal Bests; committed tests) — difficulty tiers and the trick-question/Bogey Buster variant now also shipped, see their own entries | `docs/archive/checkout-trainer-roadmap.md` |
| "Drill this checkout" deep link — a 🎯 Drill button on Top Finishes rows (Player Profile and Home) and on Coaching Insights' `checkout_route` card jumping into Checkout Trainer with `games.config.pinnedTarget` set (every round serves that number, Freeform only, trick questions force-disabled); `raceLeg()`'s preselect-then-confirm pattern reused for the deep link (`drillCheckout()`, one-shot `_checkoutDrillPin`/`_enteringSetupFromDrill`, jump straight to setup Step 3 with focus on Start); setup-screen "Drilling: N ✕" chip (`renderCheckoutTrainerPinChip()`/`clearCheckoutTrainerPin()`); server-side `pinnedTarget` integer-2-170 validation in `createGame()`; Toughest Checkout Solved excludes pinned rounds via a `games.config` scope, no schema change; committed tests | `docs/archive/checkout-drill-link-roadmap.md` |
| First-9 average — the standard pro scoring stat (3-dart avg over each leg's first 9 darts), scoped to 501/301/170/101 (`OPENING_CATS`, now module-level in `backend/db.js` and shared by all 4 surfaces): the `first9avg` Player Profile bubble and `getMetricHistory()` chart metric already existed from an earlier "opening exchange" stats pass but had a stale "(501/301 only)" UI label (fixed) and no Personal Best or leaderboard; this change added `bestFirst9` to `getPersonalBests()` (MAX per-leg, deliberately not won-legs-only unlike `bestLegAvg`, no Ghost Opponent race-this-leg button) and a "Best First-9 Average" Home leaderboard (`first9Rows`, `HAVING legs >= 20`, ranked descending); committed tests | `docs/archive/first-nine-average-roadmap.md` |
| Darts-culture badges + 180s ladder (Badge Expansion 2 Part A) — 🍳 Bed & Breakfast (exact S20/S5/S1 visit), 🏚️ Madhouse (won on double 1), 🀄 Shanghai (S+D+T of one number in an X01 visit), each a pure predicate in `frontend/scoring.js` (`isBedAndBreakfast()`/`isMadhouseFinish()`/`isShanghaiVisit()`, unit-tested) wired into `enterTurn()`'s existing `CHAIN_CHECKS`; plus a 5-tier lifetime-180s milestone ladder (10/25/50/100/250, `ONE_EIGHTY_MILESTONE_LADDERS`) reusing `CHUCKIN_MILESTONE_LADDERS`'s data-driven shape and `checkChuckinMilestoneTier()` wholesale, permanent/non-revocable tiers, no category flag (folds into the existing X01 Badge Case section); badge total 89→99 (X01 23→33), updated in REFERENCE.md §4 and all 3+1 README mentions | `docs/archive/culture-badges-roadmap.md` |
| Badge-coverage parity for Baseball & Doubles Practice (Badge Expansion 2 Part B) — Baseball: ⚾ Walk-Off (`game.baseballInning > 9` at leg-win), 🔄 The Cycle (`isBaseballCycle()`, `frontend/scoring.js`, unit-tested — S+D+T of the inning's own target number in one visit), and a 4-tier lifetime-runs ladder (100/500/1,500/5,000, `BASEBALL_RUNS_MILESTONE_LADDERS`); Doubles Practice (previously zero badges): a 4-tier lifetime doubles-hit ladder (50/250/1,000/5,000, `DOUBLES_HIT_MILESTONE_LADDERS`) plus 🎪 Ring Master (every double D1–D20 + bull hit lifetime, `getDoublesPracticeHitSectors()`/`GET /api/players/doubles-hit-sectors`, direct structural analog of `getAroundTheWorldProgress()`), both explicitly one-off/permanent/non-undo-revocable per the roadmap doc; new `doublesPractice:true` Badge Case section (`renderPlayerBadges()`); badge total 99→110 (Baseball 2→8, Doubles Practice 0→5), updated in REFERENCE.md §4 and README's Achievements/Player Profile sections; doc fully done, moved to `docs/archive/` | `docs/archive/culture-badges-roadmap.md` |
| Checkout Trainer: trick-question/bogey-number difficulty variant — opt-in New Game toggle (`games.config.trickQuestions`, off by default) serving an actual bogey number ~1 round in 8 (`listUnsolvableTargets()` derived from `checkoutHint()`'s own unfinishable signal, difficulty-tier-aware); a "🚫 No possible checkout" answer button (`declareUnsolvable()`/`gradeCheckoutDeclaration()`) whose correct call grades as that round's optimal answer (2 Blitz points, counts toward every ladder/streak/percentage) and whose wrong call reveals the real route; declarations recorded as `turns.declared_unsolvable=1` with zero dart rows (the one `addTurn()` shape allowed to, locked to this game type) and excluded from Toughest Checkout Solved and the route-specific one-offs; the conditional 💣 Bogey Buster badge (first correct call); committed tests — the last of this doc's items, doc now archived | `docs/archive/checkout-trainer-roadmap.md` |
| Cut-throat Cricket — the variant deferred at Cricket v1: `config.variant: 'standard' \| 'cutthroat'` flag on the existing `game_type='cricket'` (not a new game type), same marks/closing rules but bonus points land on EVERY open opponent instead of the shooter (full amount each, not a split), lowest score (all closed) wins instead of highest (`evaluateVisitCricket()`/`rebuildCricketState()`, `frontend/scoring.js`); Standard/Cut-throat toggle in New Game Step 3 (blurb nudges toward 3+ players without blocking 2); a "puts N on X and Y" screen-reader announcement + "Pts (lowest wins)" scoreboard label (live screen and `/display`, via a new `cricketVariant` live-payload field); Comeback Kid (Cricket)'s deficit direction flips per variant, Whitewash unchanged; server-side `config.variant` validation in `createGame()`; the 🔪 Stone Cold badge (win a 3+ player cut-throat game having received zero points across the whole match, tracked via a game-scoped `gamePointsReceived` that survives leg resets); mark-based Cricket stats (MPR, 9 Marks) unaffected — no points-based Cricket leaderboard existed to need variant-scoping; found and fixed a pre-existing, unrelated gap while adding the new badge — `display.html`'s own hand-copied ACH_LABELS/ACH_DURATION/ACH_DESC maps were missing 9 static badges added by earlier features, silently blanking their live-overlay headline on the /display screen (BUG-26, `docs/bug-roadmap.md`); committed engine tests (both open questions from the design doc resolved with tests either way) plus a `display.html`/`index.html` badge-map parity test | `docs/archive/cutthroat-cricket-roadmap.md` |
| Bob's 27 doubles routine (Part A) — new solo-only `bobs_27` game type mirroring Baseball's structure: start on 27, 3 darts per round at that round's own double D1→D20, every hit adds double its value and a miss-all round subtracts it instead, dead at ≤0 or survived past D20; store-the-gain/derive-the-penalty data model (`turns.scored` only ever holds the round's positive gain, the penalty derived at read time from `scored===0` — a fresh pattern for this codebase, proposed but not yet built for `docs/archive/halve-it-roadmap.md`); round derived from prior-turn count (SEC-25/Baseball-inning pattern), a write-time consistency guard re-deriving both the expected gain and the `bust` flag from replayed prior turns; "a run IS the game" (`legsPerSet`/`setsPerGame` forced to 1, practice-Baseball's BUG-22 shape); 5 stat bubbles, 2-field Personal Bests, a peak-single-run Home leaderboard (no minimum floor); 7 badges — 🎯 Full House, 🏔️ The Full Anderson (a perfect 1287), and a 5-tier survival/score ladder checked against each run's own final score rather than a lifetime total (`checkChuckinMilestoneTier()` reused wholesale — confirmed to be value-source-agnostic); savable/resumable (`rebuildBobs27State()`, pure replay); badge total 111→118; found and fixed a second instance of BUG-26 while wiring badges into `display.html` — three entire pre-existing ladders (X01 180s, Baseball runs, Doubles Practice hits, 13 badge IDs) had never been mirrored there at all (only Chuckin's ladder had the mirror treatment), so their live-overlay headlines had been rendering blank on the `/display` screen since each shipped; fixed alongside Bob's 27's own ladder via a shared mirrored `forEach` + a new `extractLadderIds()`-based parity test hardened to catch a whole-ladder omission, not just a drifted threshold; committed tests (`scoring.test.js`, `db.turn-consistency-guard.test.js`, `db.bobs27-stats.test.js`, `display.ach-labels-parity.test.js`); verified end-to-end with Playwright (setup, Full House, miss, undo, save/resume, death path, badges, stats, leaderboard) — see Part B (the 121 Checkout Ladder) below, also done | `docs/archive/practice-ladders-roadmap.md` (Part A) |
| Household Elo rating (Part A) — live-computed, never stored: `getEloRatings()` walks every completed, non-practice, 2-player game (across every competitive game type combined into one rating) in `(created_at, id)` order, K=32 from 1000, `expected=1/(1+10^((theirs-mine)/400))`, a zero-sum delta (not two independently-rounded formulas); Home page "📈 Household Ratings" leaderboard (rating+W/L, min 5 rated games, piggybacked onto `getHomeExtra()`'s payload so it's visible regardless of the per-game-type toggle); Player Profile rating/rank/rating-over-time sparkline section (a bespoke minimal chart rather than reusing `getMetricHistory()`'s period-bucketed one, which doesn't fit an event-driven per-game series); a match-win delta patched into a `#elo-delta-banner` placeholder once the async post-match fetch resolves (the same "celebrate now, patch in extra detail once confirmed" pattern `#challenge-pb-banner` uses); 👑 Top of the House (reach #1, once-badge) and 🗡️ Upset (beat someone rated 150+ above you, recurring) badges, checked via the same fetch, wired into X01/Cricket/Baseball's own "game (match) won" branches; heals automatically after undo/merge/game-deletion/import since nothing is stored; badge total 118→120; `game_players.start_score` column added as shared groundwork for Part B (Handicapping), which the rating walk already excludes on; committed tests (`backend/test/db.elo.test.js` — hand-verified K=32 arithmetic for a single win and a rematch, plus the Upset threshold/games-floor/every exclusion) and a live Playwright pass (a real 2-player X01 match through the actual UI, confirming the rating/Home/Profile all match the hand-computed numbers exactly) — see Part B (Handicapping) below, also done | `docs/archive/rating-and-handicap-roadmap.md` (Part A) |
| Handicapping (Part B) — nullable `game_players.start_score` per-player, per-game X01 starting-score override (`NULL` = the game's own `config.startingScore`), server-side-validated in `createGame()` (integer, `101 <= startScore < category`, X01-only, the same "never trust the client" precedent `pinnedTarget`/Cricket's variant validation set); a collapsed-by-default "Handicap" disclosure in the X01 New Game options step (`renderHandicapOptions()`, one per-player `<select>`); `newMatchPlayer()`/`resetPlayerForNextLegX01()` seed/reset from each player's own start instead of the shared category; a "STARTED 401" live-scoreboard tag (`renderers.x01.card()`); win-based stats count a handicapped win normally (the whole point is a fair contest), but a new `NOT_HANDICAPPED` SQL fragment excludes a handicapped leg's shortened-start finish from all 6 nine-darter-detection call sites plus `fewestDartsCheckout` (mechanically easier, not a skill feat) while leaving average-based stats untouched (starting-score-agnostic already); Household Elo's own exclusion (shipped with Part A) reuses the same column; committed tests (`backend/test/db.handicap.test.js`) plus a live Playwright pass (a real 2-player X01 match set up through the actual New Game UI, one player handicapped to 401, confirming both starting scores, match completion, and the handicapped winner's Elo correctly excluding the game) — both halves of this roadmap doc are now done, moved to `docs/archive/` | `docs/archive/rating-and-handicap-roadmap.md` (Part B) |
| 121 Checkout Ladder (Part B) — new solo-only `checkout_ladder` game type: start on 121, double out, up to 3 visits (9 darts) per attempt; each attempt is its own leg (`leg_no` increments per attempt), reusing `evaluateVisit()` — the shared X01 visit evaluator — completely unmodified; ladder movement (+1 rung on a win, −1 on a fail, floored at 61, capped at 170 since `turns.target_score`'s valid range tops out there) is never stored, re-derived at write time (a new consistency-guard branch validating the 3-visit cap and the derived `targetScore`), at read time (`_checkoutLadderAttempts()`), and at resume time (`rebuildCheckoutLadderState()`, pure replay) — three independent call sites that must agree; leg transitions reuse `startNextLeg()`, the same generic function X01 itself uses, rather than a bespoke reimplementation; 4 stat bubbles (Attempts, Success Rate, Current Ladder Position, Darts Thrown), a 2-field Personal Bests (Highest Target Reached counts a failed peak attempt; Fewest Darts on the Highest Checkout only looks at the highest attempt actually won), a peak-single-run Home leaderboard (no minimum floor); 7 badges — a 6-tier highest-rung ladder (125/130/140/150/160/170, checked against the position just climbed to) plus 🧗 Peak Bagged (recurring — actually checking out 170 itself, not just reaching that rung); a `renderers.checkout_ladder` live-scoreboard card in `display.html` (mirrors X01's own card, swapping the leg/set standing line for target/attempt/visit); savable/resumable; badge total 120→127; committed tests (`backend/test/scoring.test.js`, `backend/test/db.turn-consistency-guard.test.js`, `backend/test/db.checkout-ladder-stats.test.js`); verified end-to-end with Playwright (climbing 121→126 across 6 real checkouts confirming the 125 badge, a failed attempt dropping the target, undo mid-attempt, save/resume round-tripping a mid-attempt position exactly, and the live `/display` scoreboard) — both halves of this roadmap doc are now done, moved to `docs/archive/` | `docs/archive/practice-ladders-roadmap.md` (Part B) |
| The Gauntlet — new solo-only `gauntlet` game type: 20 stations in the fixed, hardcoded `GAUNTLET_STATION_ORDER` (the standard clockwise dartboard walk, no seed/generation needed); 3 positionally-graded darts per station via a new pure `evaluateGauntletStation(stationNumber, darts)` (dart 1 the single, dart 2 the treble, dart 3 the double, strictly by throw order, no re-matching across positions); a Scar/repeat rule (2 misses → one repeat, the retry's own result final regardless; 3 misses → an immediate Deep Scar, doubled at tally time, `gauntletTotalScars()`) landing on a derived result tier (`gauntletResultTier()`: Unmarked → The Gauntlet Wins); reuses `turns.target_score`/`scored` only, no new columns — station identity lives in `target_score` directly rather than being derived from `leg_no`/prior-turn-count the way every other new game type this batch used, since a repeat attempt breaks that trick; a single shared `rebuildGauntletState()` (frontend/scoring.js) derives "which station is next, is one awaiting its repeat, is the run done" for the write-time guard, every stats query, AND saved-game resume, so it's computed exactly once; the write-time guard collapses the sequence + repeat-count checks into one comparison against that shared derivation, plus a separate 0-3 scored-range check; a run always completes after all 20 stations settle (`legsPerSet`/`setsPerGame` forced to 1, the practice-Baseball/Bob's 27 "a run IS the game" shape) — unlike Checkout Ladder's perpetual shape, this is the one new game type this batch that reaches a real `finishUnit('game', ...)` ending; 5 stat bubbles (Runs Completed, Avg Total Scars, Clean Station Rate, Deep Scar Rate, Retry Rate), an ascending-is-better Personal Best (Lowest Total Scars, completed runs only) and Home leaderboard (the one leaderboard in this app sorted ascending); **the Scar Map** (`getGauntletScarMap()`) — average final miss count per station across every completed run, rendered as a severity-shaded grid in `GAUNTLET_STATION_ORDER`'s own order (already the standard clockwise board walk) plus a text-table accessibility fallback; 14 badges — a 4-tier lifetime-runs ladder, a 4-tier lifetime-clean-stations ladder, a 3-tier per-run clean-streak ladder (checked once at run end against that run's own peak streak, the Bob's 27 final-score pattern, not a lifetime count), plus 💎 Flawless Gauntlet, 🥋 Unmarked, and 🩹 Second Wind (all three recurring); no live-scoreboard sync needed (same reasoning Checkout Trainer's own reached — no `renderers.gauntlet` in `display.html`); savable/resumable; badge total 127→141; found and fixed an unrelated bug while wiring this game type's own `bubbleKeyMap` onto the shared assignment list — Checkout Ladder's (item 22, shipped just before this one) had never actually been added, silently blanking its own Player Profile stat bubbles; committed tests (`backend/test/scoring.test.js`, `backend/test/db.turn-consistency-guard.test.js`, `backend/test/db.gauntlet-stats.test.js`); verified end-to-end with Playwright (a full 20-station run — 17 clean, one 2-miss station resolved by a clean repeat earning Second Wind, one Deep Scar, one final clean station — landing on 6 total Scars/"Scarred but Standing" and the correct streak-ladder badges; a partial run saved and resumed with its exact station/repeat/miss state; undo mid-attempt) | `docs/archive/gauntlet-roadmap.md` |
| Killer — new H2H-only `killer` game type (elimination format, real best-of-N legs/sets, unlike every solo drill this batch): server-assigned per-player numbers (`assignKillerNumbers()`, a Fisher-Yates shuffle of 1-20 zipped to player names, computed inside `createGame()` and never trusted from the client, returned back to the client as `config.numbers` and assigned onto `game.players[i].number` before the scoreboard's first render); a per-dart evaluator (`evaluateDartKiller()`) since one visit's three darts can each affect a *different* player — own-number hits build lives toward the configurable become-a-killer threshold (2/3/5, standard 3) at the ring-scaled rate every dart scores at elsewhere in this app, then a killer's hits on an opponent's number remove lives at the same scaled rate, and a self-kill (own double, already a killer) costs a flat 1 life; a genuine schema addition — a new nullable `turns.affected_player_id` column, since a single `turns` row per visit could never represent per-dart cross-player effects, making Killer the one game type in this app storing one `turns` row per dart, not per visit; the write-time guard replays the whole leg via the shared `rebuildKillerState()` and rejects any turn whose claimed `affectedPlayer`/`scored` doesn't match `evaluateDartKiller()`'s own independent recomputation; the existing interactive Dartboard SVG scoring screen reused unmodified (`throwDartKiller()` has the same per-dart-commit signature Doubles Practice/Chuckin already use); 5 stat bubbles (Games Played, Win Rate, Avg Kills/Leg, Avg Lives Lost/Leg, Survived Without Killer Rate), a Personal Best (Most Kills in a Leg), and a win-rate Home leaderboard reusing `getBaseballWinLeaderboard()`'s exact shape unmodified; a new `renderers.killer` live-scoreboard card (number, lives as pips with an `aria-label`, killer/eliminated status) plus a Killer-shaped `game.legSummary` branch; 3 recurring one-off badges — 🩸 First Blood (match's first elimination), 🛡️ Untouchable (won without ever losing a life), 🙈 Own Worst Enemy (self-eliminated via your own double); a new `h2hOnly` flag on `GAME_TYPES` (the inverse of the existing `soloOnly` flag) keeps it off the practice/1-player New Game list entirely; badge total 141→144; **deliberately no save/resume support** (`SAVABLE_GAME_TYPES` excludes `killer`) — a scope decision, not a technical limitation, since mid-match state is fully re-derivable from replaying `turns` the same way stats already do; committed tests (`backend/test/scoring.test.js`, `backend/test/db.turn-consistency-guard.test.js`, `backend/test/db.killer-stats.test.js`); verified end-to-end with Playwright (New Game setup flow through the lives-threshold selector, random distinct number assignment, building lives to become a killer including a one-treble instant threshold clear, attacking and eliminating an opponent, First Blood/Untouchable badges, a full multi-leg best-of-N match to completion with the correct winner and GAME OVER summary, stat bubbles/Personal Best/win leaderboard via the API, and the live `/display` `renderers.killer` card showing number/lives-pips/throwing state); this doc (`docs/game-modes-roadmap.md`) is **not** archived since it still covers other not-yet-built variants (Shanghai, Halve-It, Round the Clock, etc. — tracked separately elsewhere on this list) | `docs/game-modes-roadmap.md` |
| End-of-Night Session Recap — a pure read-time aggregation over a single local calendar date (`getSessionRecap(date)`, nothing stored): every completed H2H game that date fully itemized (per-2-player-matchup win/loss records, a flat list for 3+ player games), non-H2H activity folded into a light per-player+game-type `soloActivity` summary rather than itemized; `perPlayer`'s best-visit/best-leg-average scoped to X01 only (same scope `getPersonalBests()`'s own `bestLegAvg` uses); `personalBestsSetTonight` computes 3 well-defined single-number X01 records (leg average, fewest darts to check out, highest checkout) twice per player — once for tonight, once for every day strictly before it — and only flags a record that beats the pre-tonight baseline in the correct direction, never re-firing for a worse leg played later the same night; a chronological `moments` timeline merging 180s/ton-plus-checkouts/match-wins/badges; a Home page teaser (`renderHomeRecapTeaser()`, a genuine network round-trip since "did anyone finish a game tonight" can't be derived client-side) hidden until at least one H2H game has completed that date, wired into both `show('home')` and the boot sequence (the initial load never calls `show('home')`, the same reason the Daily Challenge teaser needed its own explicit boot-time call); a recap screen with a date picker (any past night recomputable for free) and a **📤 Share** button rendering the night through the existing shareable-moment card generator as one new card layout, no new canvas code; committed tests (`backend/test/db.session-recap.test.js`); verified end-to-end with Playwright (the Home teaser appearing after a completed match, the full recap screen render, and Share producing a real downloaded card image); moved to `docs/archive/` | `docs/archive/session-recap-roadmap.md` |
| Marathon Mode — a 45-minute solo endurance SESSION WRAPPER, not a new game type: `marathon_sessions`/`marathon_session_legs` (the `league_fixtures`-style "context table with a `game_id` FK" pattern per CLAUDE.md), every leg a completely unmodified solo practice 501 X01 game (`game.gameType` stays `'x01'` throughout, contributing to lifetime X01 stats/Personal Bests/Nine-Darter exactly like any other practice leg); each leg's `game_id` created server-side by `startMarathonSession()`/`startNextMarathonLeg()` themselves, never accepted from the client; the wall clock checked only at leg boundaries (`finishMarathonLeg()`), never mid-leg — real session length can run past 45 minutes by however long the final leg takes, a deliberate tradeoff; a manual "End Marathon" control ends the session immediately (not "wait for this leg"), leaving the in-progress leg unfinished but its stats kept; two new pure analysis functions in `frontend/scoring.js` (`computeFatigueSplit()` → first-half-vs-second-half average dart count, clamped at zero, landing on an Iron/Tested/Fading/Running-on-Empty tier; `classifyMarathonTrend()` → a 3-segment early/middle/late read landing on The Cliff/The Warm Machine/Flat Line/Inconclusive, floored at 6 legs); a persistent session banner (elapsed/remaining, leg count, `aria-live`) layered above the completely unmodified `renderers.x01` scoreboard, no new live-scoreboard work; 6 stat bubbles, 2 Personal Bests (one ascending, one descending), a lowest-fatigue-split Home leaderboard (ascending, same direction as The Gauntlet's); 11 badges (two lifetime ladders — sessions completed, legs completed — plus 🛡️ Iron/📉 Flat Line/⏱️ Full Distance); **deliberately no save/resume support**, the same scope decision Killer already made (a resumed leg would rebuild through the generic X01 resume path with no marathon linkage at all); badge total 144→155; **a real BUG-18-class bug found and fixed along the way**: the generic X01 `onLegWon()` only cascades a leg win into `finishUnit('game',...)` when `!game.practice`, exactly the same carve-out Ghost Opponent races already needed (`|| game.hasGhost`) — Marathon legs are `practice=1` too, so without extending that same condition to `|| game.marathonSessionId`, every leg would show an endless "Next leg" panel and the session would never auto-chain or end; committed tests (`backend/test/scoring.test.js`, `backend/test/db.marathon-mode.test.js`); verified end-to-end with Playwright (the full setup flow, the persistent banner, multi-leg auto-chaining, a manual End Marathon stop, the analysis screen, badges, and Player Profile/Home wiring); moved to `docs/archive/` | `docs/archive/marathon-mode-roadmap.md` |
| Shanghai game mode — `game_type='shanghai'`, structurally a sibling of Baseball: a fixed round sequence 1..`config.rounds` (default 7, a 20-round long-form option), all players in lockstep on one shared live round (`game.shanghaiRound`); only the round's own number scores (S/D/T = 1×/2×/3× the round number), evaluated by `evaluateVisitShanghai()` in `frontend/scoring.js`; a Shanghai (single, double, AND treble of the round's own number, one visit, any order) wins the WHOLE match instantly, mid-round; absent a Shanghai, the match ends once the final round completes with a single unique points leader, else extra rounds repeat the final round's own number (Baseball's extra-innings precedent); the SEC-25-style consistency guard in `addTurn()`, with a corrected 9× (not the source doc's stated 6×) ceiling — three trebles of the round's number is a real, legal, non-Shanghai visit worth more than a Shanghai itself, a correctness fix over the doc's literal wording, not a deviation from its intent; `turns.leg_won=1` set ONLY for a genuine instant Shanghai (self-referential, Cricket/Killer-style) — a final-round win decided by point totals is never flagged, so `getShanghaiWonLegs()` derives that winner at query time via `SUM(scored)` comparison instead, exactly `getBaseballWonLegs()`'s own derivation (also a refinement over the source doc's literal "or on the final-round winner's closing visit" wording, which isn't always the same turn as the actual points leader); saved-games-compatible by construction (`rebuildShanghaiState()`, pure replay of recorded turns); a `renderers.shanghai` per-round score grid on both the live scoreboard and `/display` (row labels show the round's own TARGET NUMBER, not the round index, so the grid stays meaningful once extra rounds begin); a single-button `renderPadShanghai()` mirroring Baseball's own; the instant-Shanghai moment fires `announce()` plus an icon+text 🀄 banner; 6 stat bubbles (Points/Round, Shanghais Thrown, Win Rate, Games Played, Darts Thrown, Best Round), Personal Bests, a 3-board Home leaderboard set (Points Per Round, Shanghais Thrown, Most Shanghai Wins), and the 🀄 Shanghai! badge (badge total 155→156); no lifetime-points ladder built (the source doc left it optional, "if play shows appetite"); committed tests (`backend/test/scoring.test.js`, `backend/test/db.shanghai-stats.test.js`, `backend/test/db.turn-consistency-guard.test.js`); verified end-to-end with Playwright (practice and H2H setup, an instant mid-round Shanghai win, a final-round decider correctly awarded to the higher-points player rather than whoever's turn ended the round, badges/stat bubbles/personal bests/win leaderboard, and the live `/display` scorecard); moved to `docs/archive/` | `docs/archive/shanghai-roadmap.md` |
| Halve-It game mode (core game only — the custom target editor is split out as its own separate open item above) — `game_type='halve_it'`, structurally another Baseball/Shanghai sibling: a fixed round sequence (the classic 7-round default — 20, 16, double 7, 14, treble 10, 17, Bull — `config.targets`, an ordered `{sector, ring?}` array), all players in lockstep on one shared live round (`game.halveItRound`); only the round's own target scores (`halveItDartValue()` in `frontend/scoring.js` — any ring at face value when unrestricted, only the named ring when restricted; Bull falls out of the same `mult*sector` formula for free since there's no treble-bull ring); a visit that gains exactly 0 halves the running total, rounding UP (`Math.ceil(total/2)` — round-down risks a permanent 1→0 spiral, round-up's floor is 1→1); highest total after the final round wins, with no instant-win condition at all (ties continue into extra rounds repeating the final target, the same Baseball/Shanghai precedent, filling a gap this doc's own design left unaddressed); `turns.scored` stores the gain only (0 on a halved visit) and `turns.bust=1` is repurposed as the halving flag (the same column-repurposing precedent Doubles Practice/guided Around the Clock set), validated by a SEC-25-style consistency guard that checks `bust` for consistency with the derived gain rather than rejecting it outright; a genuine technical wrinkle beyond the source doc's own anticipation — the running total is order-dependent (halving interspersed with additions), so it can't be read back with a single `SUM(scored)` aggregate the way every other fixed-round game type's stats can, requiring a one-off `_replayHalveItLegTotals()` (`backend/db.js`) that replays the raw turns once instead; savable/resumable (`rebuildHalveItState()`, including per-player `everHalved`/`lastVisitHalved` reconstruction so a resumed leg's badge check still sees the whole leg's halving history); a per-round chalkboard grid on both the live scoreboard and `/display` showing the round's own TARGET LABEL as the row header and the RUNNING TOTAL (not a per-round delta) with a ½ marker on a halved round; 6 stat bubbles, 5-field Personal Bests (best single round living in the stat bubbles instead, per Baseball's own Best Inning split), a 2-board Home leaderboard set (Highest Final Total, Most Halve-It Wins), and 2 flavor badges (🪓 Halved at the Death, 🛡️ No Half Measures — badge total 156→158); committed tests (`backend/test/scoring.test.js`, `backend/test/db.halve-it-stats.test.js`, `backend/test/db.turn-consistency-guard.test.js`); verified end-to-end with Playwright (practice setup, a hit, a halving with correct round-up math, a ring-restricted round, a full 7-round game to completion, the No Half Measures badge, stat bubbles/personal bests/leaderboard, live scorecard target labels, and the `/display` scorecard); the custom target editor split out above shipped later (its own ledger row above), so this doc is now fully done and moved to `docs/archive/` | `docs/archive/halve-it-roadmap.md` |
| Dead Man Walking — new solo-only `dead_man_walking` game type, structurally the closest existing precedent is 121 Checkout Ladder (real X01-shaped visits, reusing X01's own bust/win legality from a non-501 deficit), but with a personalized, frozen-at-creation deficit and budget rather than a shared climbing target, and a bust here immediately fatal to the round (no up-to-3-visits retry the Ladder allows); `getWeakestCheckouts(playerName, count)` (`backend/db.js`) reuses Coaching Insight #3's own remaining-score reconstruction (the `SUM() OVER (... ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING)` window-function trick) to rank a player's own double-out X01 remaining-scores (32–170) by `0.5*bustRate + 0.5*nonCompletionRate`, floored at 8 samples, bogey numbers excluded via `checkoutHint()`'s own unfinishable signal, cold-start falling back to `CHALLENGE_CHECKOUTS` (moved to `frontend/scoring.js` so this server-side path can share it with Daily Challenge); `deadManWalkingParForTarget()` computes `par = max(historicalAverage, objectiveOptimal+1)` or `objectiveOptimal+2` with no history — the floor is the one concrete correctness property this doc adds (the round's actual budget, `par-1`, can never drop below the objective-optimal dart count), verified by an exhaustive test across every finishable score 2–170; `games.config.rounds` (15 `{target,par}` pairs) computed once, server-side, inside `createGame()` and frozen for the whole session — never client-supplied or live-recomputed, the same security posture as Killer's number assignment; a new per-dart (not per-visit) `evaluateDeadManDart()`/`resolveDeadManDart()` pair (`frontend/scoring.js`) since the budget won't generally be a multiple of 3 and a bust/finish must end the round the instant it happens; no new columns — `bust`/`checkout` reused in their ordinary X01 sense (Walked Out iff any turn has `checkout=1`, Executed otherwise whether by a real bust or by exhausting the budget without one — the two Executed flavors distinguished live in `announce()`/status text but not stored distinctly, matching this doc's own framing); the write-time guard reuses the X01/Checkout Ladder dart-sum/bust/checkoutPoints arithmetic plus a new per-round **variable** dart-budget guard generalizing the Ladder's flat 9-dart cap to `config.rounds[leg-1].par-1`; round progression is its own dedicated `resolveDeadManWalkingRound()` (`frontend/index.html`), deliberately NOT an X01 `onLegWon()` carve-out the way Marathon Mode's practice-leg exception was, since two of the three ways a round ends (a bust, running out of darts) are never an X01 leg-win EVENT at all — there's no hook to carve into for them; derived result tiers (Pardoned down to Executed, never stored); stats replayed via `_replayDeadManWalkingLegs()` (Halve-It's own "nothing pre-aggregated" complication, since a round's termination reason isn't `SUM()`-able) — 6 stat bubbles, one higher-is-better Personal Best (Most Walked Out), a Home leaderboard, and a lifetime cross-run Longest Walked-Out Streak figure; 14 badges (a 4-tier lifetime-runs ladder, a 4-tier lifetime-Walked-Out ladder, a 3-tier lifetime cross-run streak ladder, plus 🕊️ Full Reprieve, ⚰️ Pardoned, 💀 Last Request — badge total 158→172); no `/display` renderer needed (single-device, same conclusion Checkout Trainer/Gauntlet reached), but its badge ids are still hand-copied into `display.html`'s `ACH_LABELS`/`ACH_DURATION`/`ACH_DESC` maps per the standing convention; **a real isolation bug found and fixed along the way**: `getPersonalBests()`'s `bestLegAvg`/`bestLeg`/`recentFormAvg`/`lifetimeAvg`/`fewestDartsCheckout` and `getPlayerStatBubbles()`'s `avgDartsPerLeg` all relied on `t.checkout=1` with no explicit `game_type='x01'` filter — true only as long as X01 (and Checkout Trainer, separately excluded) were the only game types ever setting `checkout=1`; Checkout Ladder broke that assumption when it shipped, and this drill's own real Walked Out checkouts made it concrete (a committed isolation-regression test caught X01's Personal Bests changing from `null` to real, wrong values); fixed by adding `X01_ONLY` to all three queries; the "Drill this number" cross-link into Checkout Trainer's existing pinned-target flow was explicitly left unbuilt — the source doc's own "Suggested build order" step 9 and its own "Open questions" both frame it as optional, v2-only work once the core loop is proven, so it does not get its own tracked row here; committed tests (`backend/test/scoring.test.js`, `backend/test/db.dead-man-walking-stats.test.js`, `backend/test/db.turn-consistency-guard.test.js`, `backend/test/display.ach-labels-parity.test.js`); verified end-to-end with Playwright against both the real-history-driven path and the cold-start `CHALLENGE_CHECKOUTS` fallback path (a Walked Out round, an Executed-by-bust round, an Executed-by-out-of-darts round, a full 15-round completion landing on the correct result tier and badges, save/resume mid-run); moved to `docs/archive/` | `docs/archive/dead-man-walking-roadmap.md` |
| The Pressure Chamber (core game — item 28; the self-declare honesty mechanic shipped separately as item 32, its own ledger row above) — new `game_type='pressure_chamber'` (`config.rounds: 15`, fixed and server-overridden regardless of client input, the same never-trust-the-client precedent Killer's number assignment already set), 1-4 players (`contexts: ['practice', 'h2h']`); the single load-bearing design decision, a round's Pressure Card (target + one of 8 situational modifiers) as a PURE function of `(gameId, roundIndex)` — `generatePressureCard()`, `frontend/scoring.js` — never stored, so H2H's "identical card sequence" falls out for free and no `target_sector`/`modifier_id` column exists anywhere; two grading paths, sector/ring targets via a new `gradePressureSectorRound()` ("best of the round's darts": full/partial/miss) and finish targets reusing `evaluateVisit()` completely unmodified (always double-out, a judgment call this build settled since the roadmap doc itself didn't pin it down); all 3 modifiers requiring real engine work built in full — Match Dart (only the round's 3rd dart counts, and a finish checkout must land specifically on dart 3), Sudden Death (a genuine per-dart early stop via `evaluateDartPressureSector()`, reusing the Doubles Practice per-dart-live-feedback pattern), and No Warmup (a real 5-second `Date.now()`-based wall-clock deadline, the Checkout Blitz precedent, enforced client-side only — the same limitation Checkout Blitz's own deadline already has — with a genuine miss dart auto-committed on timeout so the server's guard never needs to trust an unverifiable "this was late" claim); the Composure Points formula (base-by-difficulty × modifier-multiplier, half-value partials, a separate smaller miss-penalty doubled again under Double Down/Comeback, Comeback also adding a flat bonus on a hit) and the Composure Rating table (Ice 120+/Steel 90-119/Copper 60-89/Tin 30-59/Rattled below 30) both derived at read time, never stored; reuses Checkout Trainer's exact `bust`/`checkout`/`leg_won` three-way outcome verbatim (`turns.scored` = CP gained only, never negative; a run's total CP = `SUM(scored)` minus a derived total miss penalty, re-rolling `generatePressureCard()` for every missed round, the same "derive the rest, don't store it" philosophy Halve-It's halving rule already established — simpler here since the total isn't order-dependent); the SEC-25-style consistency guard in `addTurn()`, rejecting a round past 15 (no extra-rounds extension exists for this game type); a deterministic solo-vs-H2H tie-break chosen and documented as this build's own judgment call on the roadmap doc's last explicit open question (`pressureChamberDecideWinnerIndex()`: highest CP, then fewest misses, then fewest darts, then turn order — always a definite winner, never a new "draw" UI class); savable/resumable (`rebuildPressureChamberState()`, pure replay); 6 stat bubbles, 3-field Personal Bests (Best Run CP — a peak, no minimum floor, the Checkout Blitz precedent; Best Composure Rating, derived from the peak since the thresholds are monotonic; Longest Full-Hit Streak), a 2-board Home leaderboard set (Best Run CP with its Composure Rating, Most Pressure Chamber Wins); 15 badges — 3 data-driven ladders (lifetime runs completed, lifetime CP earned, and a per-run longest-full-hit-streak ladder reusing `CHUCKIN_MILESTONE_LADDERS`'s engine wholesale) plus 4 one-off flavor badges (🥶 Ice, 🎯 Nerves of Steel, ⏱️ No Warmup Needed, 🃏 Dead Calm — Steady Hands), the last three checked for the ROUND's own graded outcome and (a deliberate judgment call, since Composure Rating is a personal achievement even in H2H per the roadmap doc's own Goal section) 🥶 Ice and every lifetime ladder checked for EVERY player once their run settles, not just the leg winner — badge total 158→173; a `renderPadPressureChamber()` deliberately Pad-mode-only (never the Dartboard SVG, since Sudden Death/No Warmup both need `throwDart()` to stay the single live-input path), extending the BUG-24 `noZoneTracking` fix to this game type too; a `renderers.pressure_chamber` live-scoreboard card in `display.html` (a per-round ✅/➗/❌ chalkboard grid plus a large Pressure Card banner and a live No Warmup countdown, `display.html`'s own copy of the full 15-round card sequence sent since it has no shared `scoring.js` module); committed tests (`backend/test/scoring.test.js`, `backend/test/db.turn-consistency-guard.test.js`, `backend/test/db.pressure-chamber-stats.test.js`, `backend/test/display.ach-labels-parity.test.js` extended for the 4 new one-offs + 3 new ladders); verified end-to-end with Playwright (practice and H2H setup, a full 15-round solo run to a Composure Rating, Sudden Death's early stop, Match Dart grading, badges/stat bubbles/personal bests/leaderboards via the API, and the live `/display` scorecard); the self-declare honesty mechanic split out above shipped later (its own ledger row above), so this doc is now fully done and moved to `docs/archive/` | `docs/archive/pressure-chamber-roadmap.md` |
| Scoring screen ("Pad"/Dartboard entry) visual redesign — landscape "Tablet Split" / portrait "Focus Mode" — via `/frontend-design`: four distinct mockup directions were built and screenshotted (two rounds of dartboard-proportion corrections along the way — number-label size, then the outer-single band width, both fixed and re-screenshotted), the owner picked landscape direction C and portrait direction D, and both were implemented into the real `frontend/index.html` — no new backend endpoint or data shape, purely a layout/CSS restructure of the existing `#screen-game`/`renderGameShell()` markup. Portrait ("Focus Mode"): Bounce Out merged into `.turn-actions .undo-row` as a third flex button (relabeling "Undo Last Turn" to "Undo Turn"), reclaiming the standalone full-width row it used to occupy — otherwise pixel-identical to the pre-redesign layout (verified via explicit `order` values, see below). Landscape ("Tablet Split", `@media (orientation:landscape) and (min-width:700px)`): a single-row, two-column grid splits the screen into a narrow rail (`#scoreboard`/`#slots`/`#status`/`.turn-actions`, wrapped in one real flex-column `.rail` box) and a full-height board column (`.oche`). A genuine CSS Grid gotcha was found and fixed along the way (verified live via Playwright, not assumed): an earlier version grid-placed the four rail elements as separate row-1..5 items with `.oche` spanning `grid-row:1/-1` across all of them — a multi-row-spanning item's min-size need gets redistributed into every intrinsically-sized ("auto"-family) row it spans, which inflated row 1 to ~875px tall in an 820px-tall viewport for Around the Clock's one-line scoreboard card, squeezing the flexible row to 0px. Fixed by grouping the rail into one real box instead (sidesteps the whole bug class), which in turn required moving `#scoreboard` inside `.game-play-area`/`.rail` (previously a permanent sibling never touched by `renderGameShell()`) and moving `.turn-actions` earlier in the markup so `.rail` could wrap it contiguously with scoreboard/slots/status — portrait's original visual order is preserved via two `order` CSS values (`.oche{order:1}`, `.turn-actions{order:2}`) rather than document order, since `#screen-game` was already `display:flex` for portrait. Also fixed along the way: Around the Clock/Around the World's solo scoreboard card (`.pscore`, a flex row packing a name block, a meta block, a remaining-count, and a wrapping 20/63-cell outcome grid) squeezed the outcome grid to near-zero width in the narrow rail, wrapping it into dozens of rows and ballooning the card to the same ~875px — fixed with a landscape-only `flex-direction:column` override. `#slots`' three dart-slot cards go from a 3-column grid to a single compact column of horizontal rows in landscape. The 4 full-page-takeover overlay call sites (`finishUnit()`/`finishMarathonLeg()`/`renderMarathonAnalysisScreen()`/Checkout Blitz's "TIME'S UP" screen) were redirected from `.oche` to `.game-play-area` so GAME OVER/LEG COMPLETE/MARATHON COMPLETE results screens still correctly replace the ENTIRE play area (not just the board) in both orientations, plus a `.game-play-area > *:only-child{grid-column:1/-1;grid-row:1}` rule so that results div spans the full landscape grid area instead of the narrow rail column. `REFERENCE.md` updated (new "Scoring-screen layout: portrait 'Focus Mode' / landscape 'Tablet Split'" subsection). Verified live via Playwright across every affected mode: X01 and Cricket H2H (both orientations, a full 3-dart turn, `enterTurn()`), a real driven-to-completion X01 leg confirming the LEG COMPLETE overlay correctly spans the full grid area in landscape (not just the rail), Around the Clock (a genuine per-dart mode with `#slots` hidden) and Checkout Trainer (portrait, confirming the staged-visit UI and correct button labels); full 1305-test backend suite passes (frontend-only change) | *(no separate roadmap doc — implemented directly)* |
| DNF survives a save/resume — closing the "Known limitation, actively blocked rather than silently allowed" clause on the forfeiting item above. Found by reading `frontend/scoring.js` front to back during a full-app review. `game_players.dnf` was always persisted and correctly honoured live, but nothing ever read it back: `getResumeState()` didn't return it, `resumeGame()` didn't apply it, and every `rebuild*State()` built players from names alone and advanced with a plain `(index + 1) % length`. Two consequences, one visible and one not — the departed player was reinstated as active, and (the silent one) Baseball/Shanghai/Halve-It/Pressure Chamber resumed on the **wrong round**, because `isRoundComplete()` finds a fixed-round mode's boundary by walking to the next ACTIVE player. `saveGame()` blocked the whole situation with a 409 rather than half-support it, so this was a walled-off gap rather than a shipped bug. Now: `getResumeState()` returns `gp.dnf`, `resumeGame()` applies it, and the seven multi-player rebuilds take a `dnfs` array and advance via a new `nextActivePlayerIndex()`; the 409 and its test are gone. The non-obvious part is *when* the player left: `dnf` is a terminal flag with no timestamp, and marking them out from turn 1 back-dates the departure over rounds they genuinely threw in, landing the replay a round **ahead** instead of behind — the first version of this fix did exactly that and the new test caught it. `_dnfTracker()` derives the moment exactly instead: a bowed-out player throws no further turns, so their last recorded turn is the last one before they left. Solo-only rebuilds take no `dnfs` — one seat can't bow out of its own drill. Committed tests: `backend/test/scoring.resume-dnf.test.js` (7 cases, pinning all three numbers — pre-fix, back-dated, and live), plus the rewritten saved-games case asserting the save now succeeds and the resume payload carries the flag. New `resume-fidelity` verify-ui case (4 assertions) drives a real 3-player Baseball bow-out through the real save/resume path. `REFERENCE.md` §13 rewritten | *(no separate roadmap doc — implemented directly)* |
| Making `backend/db.js`'s map honest, and moving `index.html`'s CSS out — the first two steps of a "should the 38,606 lines be split up" review. **db.js banners.** The file's `/* ---------- section ---------- */` headings had drifted badly, which matters more than tidiness: they are how anyone navigates a 10,000-line file, and a heading that lies is worse than none. Ten corrections, all comment-only (51 lines added, 2 removed, zero code moved): the game-type registry was flying the "Saved games / pause & resume" banner while the actual saved-games code sat under "turns.checkout_points, derived"; ~600 lines of the data export/import/wipe subsystem lived under "Dead Man Walking" with nothing announcing them; the whole of Marathon mode sat under "dart builder / loadouts"; `getMetricHistory`/`_mf`/`_scope` — three of the most widely-called things in the file — sat at the tail of the Around the Clock section, making shared infrastructure look mode-specific; the dart heatmaps sat under "Killer"; the head-to-head records under "Home Assistant webhook proxy"; and "server error log" carried six unrelated functions (top finishes, On This Day, and two destructive edits). The two identically-named "Dead Man Walking" sections were investigated and deliberately **kept apart** — the round-generation half is placed against Coaching Insights because it reuses that section's remaining-score reconstruction and its comment leans on the adjacency, so the fix was to name them distinctly, not to merge them. Section count 37 → 47, every one now naming what it actually contains. **CSS extraction.** `index.html`'s inline `<style>` block (1,464 of its 19,160 lines) moved verbatim to `frontend/app.css`, linked from the head; `index.html` is now 17,697 lines. This is the one part of that file that splits with nothing to get wrong — CSS has no shared-scope or load-order hazard the way the classic `<script src>` files do. It does have one silent failure mode, though, and it got two guards in the same change: `serveStatic()` falls back to `index.html` for any unknown non-API path, so a typo'd `href` does not 404 — the browser receives a whole HTML page in answer to a CSS request, discards it with a clean console, and renders every screen unstyled while every behavioural assertion still passes. New `check.js` rule `missing-stylesheet` (check count 8 → 9) catches the typo statically; a new `home-settings` verify-ui assertion catches the case where the file exists and is served but isn't reaching the page, asserting two independent signals (`--board` resolving, proving the file parsed, and body padding, proving a rule matched). Both proven to fail against a deliberately broken `href`. `display.html` deliberately keeps its own inline styles — a standalone page with its own layout language, free to diverge. Backend 1735/1735, verify-ui 472/472, check + typecheck clean. `REFERENCE.md` and `README.md` file trees updated | *(no separate roadmap doc — implemented directly)* |
| Splitting `backend/db.js` into a core plus leaf modules — step 3 of the "should the 38,606 lines be split up" review, and the payoff of the banner-honesty work above (you cannot cut a file along its section lines while the section lines are wrong). Four true leaves lifted out, 10,100 → 8,979 lines: `tournaments.js` (517), `leagues.js` (347), `marathon.js` (175), `coaching.js` (146). "True leaf" was established by measurement, not by name — a comment-stripped call graph in both directions showed these sections have **zero inbound** calls from the rest of the file, so the cut moves code without moving behaviour. (The naive scan said otherwise; nearly every apparent inbound edge turned out to be a mention in a comment.) **The shape: each leaf is a FACTORY**, `module.exports = function initX(deps)`, that db.js calls with exactly the names that section needs. Chosen over the two obvious alternatives for concrete reasons: `require('./db.js')` from a leaf is a circular require that works only while nothing is read at load time — fine by accident, broken the day someone adds a top-level read, and no foundation for ten more modules; and re-deriving the handle would open a second connection to a WAL database. The injected argument list is also the honest record of each section's coupling, which a shared global scope never is — `leagues.js` visibly takes three tournament functions because a league can generate a play-off bracket. **All wiring lives in one block at the bottom of db.js**, which took two attempts to get right: load order squeezes from both sides, since `const`s (X01_ONLY, CHECKOUT_POINTS) are not hoisted the way `function` declarations are, and wiring each leaf where its section used to live made every extraction ask "do my dependencies exist yet?" — twice the answer was no. At the bottom everything is initialised and the question never arises again. The one cost is that `GAME_TYPE_REGISTRY` is built far above and its `marathon` entry names two functions that now live in a leaf; that entry alone reaches them through a thunk, documented at the entry so the shape isn't copied without cause. **New `check.js` rule `leaf-missing-dep`** (check count 9 → 10), which is the lesson of the exercise made permanent: a leaf naming something it neither injects nor requires is a ReferenceError only when that function is CALLED — the module loads, `node --check` parses it, the typechecker passes it. This happened five times during the extraction (`CHECKOUT_POINTS`, `X01_ONLY`, `computeFatigueSplit`, `checkoutHint`, `dartLabel`), each caught only because a test happened to cover that path. The rule checks two exactly-extractable surfaces (db.js's top-level SCREAMING_CASE constants and scoring.js's export list) and is proven against all five. Its comment-stripper deliberately KEEPS `${...}` interpolations, without which it missed the SQL-fragment constants that are the main case — the first version did exactly that and caught only three of five. Backend 1735/1735, verify-ui 472/472, check + typecheck clean. `REFERENCE.md` and `README.md` file trees updated | *(no separate roadmap doc — implemented directly)* |
| Reassessed `docs/frontend-module-split-roadmap.md`'s remaining steps 3 and 4 against what the `backend/db.js` split actually taught, as step 4 of the "should the 38,606 lines be split up" review. Conclusion unchanged — **do step 3 (per-mode game logic, one file per game type), do not do step 4 (the core stays one file)** — but both reasons are now evidenced rather than asserted, and the doc's status header says so. What transfers from db.js: measure each candidate's inbound edges with comments stripped and cut only true leaves, because the naive scan is badly wrong (nearly every apparent inbound edge there was a section name appearing in a comment) — the same lesson step 2 here learned three times as "a banner marks where a topic starts, not where it ends". What does NOT transfer, and matters more: db.js's leaves are real Node modules, so that split produced an **enforceable dependency contract** (a factory taking exactly what it needs, plus the new `leaf-missing-dep` checker rule that fails the build when one names something it was never given). No equivalent rule can exist for `frontend/js/` — classic scripts share one global scope, so there is no missing dependency to detect and every name stays in scope for every file forever. Step 3 therefore buys navigability, reviewable diffs and per-file `node --check`, and buys no encapsulation at all; worth knowing before starting so the result is not mistaken for architecture it doesn't have. Current sizes recorded for whoever picks it up: `index.html` 17,697 lines (~16,578 JS — 576 top-level functions, 69 mutable top-level `let`s, 284 inline handler attributes), of which the per-mode turn-commit and leg/set-progression blocks step 3 targets are ~900-1,400. Even done perfectly the core stays ~15,000 lines, which is exactly why step 4 is a decision not to act rather than an unfinished task | `docs/frontend-module-split-roadmap.md` |
| Per-game-type frontend split — `docs/frontend-module-split-roadmap.md` **step 3, done**, which closes that doc's last open work (step 4 is a deliberate decision not to split the core). One file per `GAME_TYPES` key: 164 functions into 15 files (`cricket.js`, `baseball.js`, `shanghai.js`, `halve-it.js`, `pressure-chamber.js`, `doubles-practice.js`, `chuckin.js`, `checkout-trainer.js`, `around-the-clock.js`, `around-the-world.js`, `bobs-27.js`, `checkout-ladder.js`, `gauntlet.js`, `killer.js`, `dead-man-walking.js`), `index.html` 17,529 → 14,112 lines. Each file holds exactly what its registry entry names — turn-loop members, leg-win handler, completion-panel spec, the three player-lifecycle hooks, the Home leaderboard renderer and the mode's own setup controls — while the registry entry itself stays in `index.html`, so there is still one place to see every mode at once. Classification came from the registry's own keys rather than from section banners or a hand-list, per house discipline. Cut per declaration, verbatim, each function carrying its preceding comment block; the extractor asserted every requested name resolved to exactly one top-level declaration and that no two spans overlapped, so a shared comment block could not be claimed twice. **Verified rather than trusted**: all 164 moved functions are byte-identical to their pre-split text, and the counts reconcile exactly — 554 before, 390 left in `index.html`, 164 moved, nothing lost and nothing declared in two places. Two safety nets came out of real mistakes made doing it. New `check.js` rule **`orphan-script`** (check count 10 → 11): extracting into a file with no `<script src>` left all ten of the first mode's functions dead while *every existing check passed*, because `missing-handler` cannot see functions the `GAME_TYPES` registry calls by identifier rather than an `on*=` attribute; the rule compares `frontend/js/` on disk against what the pages load, and was proven by watching it fire before the tag was added. And `backend/test/frontend-source.js` was generalised to return the whole page scope — markup, `app.css` and every script `index.html` loads, with the list read from the page's own tags so the next extraction needs no edit there. Six tests brace-match functions out of the page; when a mode moved, their dynamically generated cases did not fail, they **stopped existing** — 34 of them at once — and a suite that silently shrinks still prints green. The remaining at-risk tests were then found by matching all 187 per-mode function names against every test reading `index.html`, instead of waiting for each batch to break one. Backend 1735/1735, verify-ui 472/472 with every mode driven through a real game, check + typecheck clean | `docs/frontend-module-split-roadmap.md` |
